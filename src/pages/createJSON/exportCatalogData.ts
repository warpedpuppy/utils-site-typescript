// AUTO-GENERATED by scripts/generate-export-catalog.mjs — DO NOT EDIT.
// Regenerate with:  node scripts/generate-export-catalog.mjs
//
// Function-grained Copy Code catalog data: for every copyable
// @utilspalooza/core export, its canonical TypeScript snippet and the
// TypeScript-compiler-transpiled JavaScript snippet, plus the catalog
// keys it depends on and the shared interfaces it references. Generated
// at build time so the TypeScript compiler never ships to the browser
// and production minification cannot corrupt the snippets.

export interface ExportCatalogDataEntry {
  key: string;
  module: string;
  tsSource: string;
  jsSource: string;
  dependencyKeys: string[];
  interfaceNames: string[];
}

export const EXPORT_CATALOG_DATA: ExportCatalogDataEntry[] = [
  {"key":"DEFAULT_BOIDS_OPTIONS","module":"Boids","tsSource":"/** Sensible starting values; spread-override any field you like. */\nconst DEFAULT_BOIDS_OPTIONS: BoidsOptions = {\n    perceptionRadius: 50,\n    separationRadius: 24,\n    maxSpeed: 3,\n    maxForce: 0.05,\n    separationWeight: 1.6,\n    alignmentWeight: 1.0,\n    cohesionWeight: 0.9,\n    targetWeight: 0,\n};","jsSource":"/** Sensible starting values; spread-override any field you like. */\nconst DEFAULT_BOIDS_OPTIONS = {\n    perceptionRadius: 50,\n    separationRadius: 24,\n    maxSpeed: 3,\n    maxForce: 0.05,\n    separationWeight: 1.6,\n    alignmentWeight: 1.0,\n    cohesionWeight: 0.9,\n    targetWeight: 0,\n};","dependencyKeys":[],"interfaceNames":[]},
  {"key":"HUE_FAMILIES","module":"Color","tsSource":"/**\n * Hue ranges (degrees) for each named family. `red` uses a negative start so it\n * straddles the 0°/360° seam cleanly — {@link hslToRgb} wraps the hue for you.\n */\nconst HUE_FAMILIES: Record<HueFamily, [\n    number,\n    number\n]> = {\n    all: [0, 360],\n    red: [-12, 18],\n    orange: [18, 45],\n    yellow: [45, 70],\n    green: [80, 160],\n    cyan: [160, 200],\n    blue: [200, 250],\n    purple: [255, 290],\n    pink: [300, 345],\n};","jsSource":"/**\n * Hue ranges (degrees) for each named family. `red` uses a negative start so it\n * straddles the 0°/360° seam cleanly — {@link hslToRgb} wraps the hue for you.\n */\nconst HUE_FAMILIES = {\n    all: [0, 360],\n    red: [-12, 18],\n    orange: [18, 45],\n    yellow: [45, 70],\n    green: [80, 160],\n    cyan: [160, 200],\n    blue: [200, 250],\n    purple: [255, 290],\n    pink: [300, 345],\n};","dependencyKeys":[],"interfaceNames":[]},
  {"key":"ballBounce","module":"BallBounce","tsSource":"/**\n * Advance a ball one frame under gravity, bouncing it off the container walls.\n *\n * A simple Euler-integration physics step: applies gravity to vertical velocity,\n * moves the ball, then reflects and damps its velocity (restitution + friction)\n * whenever it hits a wall. **Mutates `ball`** in place.\n *\n * @param ball - The ball to advance (its `x`, `y`, `vx`, `vy` are mutated).\n * @param stage - The bounding container (`width`/`height`) the ball bounces inside.\n * @param restitution - Bounciness: fraction of speed kept on each bounce (0–1).\n *   `1` is a perfectly elastic, never-tiring bounce; lower values lose energy faster.\n *   Defaults to `0.72`.\n * @returns Nothing — `ball` is updated in place.\n * @example\n * ballBounce(ball, { x: 0, y: 0, width: 800, height: 600 });      // default bounce\n * ballBounce(ball, stage, 0.95);                                   // extra bouncy\n */\nfunction ballBounce(ball: Ball, stage: Container, restitution: number = 0.72): void {\n    const gravity = 0.4;\n    const friction = 0.985;\n    ball.vy += gravity;\n    ball.x += ball.vx;\n    ball.y += ball.vy;\n    if (ball.y >= stage.height - ball.radius) {\n        ball.y = stage.height - ball.radius;\n        ball.vy = -Math.abs(ball.vy) * restitution;\n        ball.vx *= friction;\n    }\n    if (ball.y <= ball.radius) {\n        ball.y = ball.radius;\n        ball.vy = Math.abs(ball.vy);\n    }\n    if (ball.x >= stage.width - ball.radius) {\n        ball.x = stage.width - ball.radius;\n        ball.vx = -Math.abs(ball.vx) * restitution;\n    }\n    if (ball.x <= ball.radius) {\n        ball.x = ball.radius;\n        ball.vx = Math.abs(ball.vx) * restitution;\n    }\n}","jsSource":"/**\n * Advance a ball one frame under gravity, bouncing it off the container walls.\n *\n * A simple Euler-integration physics step: applies gravity to vertical velocity,\n * moves the ball, then reflects and damps its velocity (restitution + friction)\n * whenever it hits a wall. **Mutates `ball`** in place.\n *\n * @param ball - The ball to advance (its `x`, `y`, `vx`, `vy` are mutated).\n * @param stage - The bounding container (`width`/`height`) the ball bounces inside.\n * @param restitution - Bounciness: fraction of speed kept on each bounce (0–1).\n *   `1` is a perfectly elastic, never-tiring bounce; lower values lose energy faster.\n *   Defaults to `0.72`.\n * @returns Nothing — `ball` is updated in place.\n * @example\n * ballBounce(ball, { x: 0, y: 0, width: 800, height: 600 });      // default bounce\n * ballBounce(ball, stage, 0.95);                                   // extra bouncy\n */\nfunction ballBounce(ball, stage, restitution = 0.72) {\n    const gravity = 0.4;\n    const friction = 0.985;\n    ball.vy += gravity;\n    ball.x += ball.vx;\n    ball.y += ball.vy;\n    if (ball.y >= stage.height - ball.radius) {\n        ball.y = stage.height - ball.radius;\n        ball.vy = -Math.abs(ball.vy) * restitution;\n        ball.vx *= friction;\n    }\n    if (ball.y <= ball.radius) {\n        ball.y = ball.radius;\n        ball.vy = Math.abs(ball.vy);\n    }\n    if (ball.x >= stage.width - ball.radius) {\n        ball.x = stage.width - ball.radius;\n        ball.vx = -Math.abs(ball.vx) * restitution;\n    }\n    if (ball.x <= ball.radius) {\n        ball.x = ball.radius;\n        ball.vx = Math.abs(ball.vx) * restitution;\n    }\n}","dependencyKeys":[],"interfaceNames":["Ball","Container"]},
  {"key":"ballToBallBounce","module":"BallToBallBounce","tsSource":"/**\n * Push two overlapping balls apart with a spring-like force (soft collision response).\n *\n * When the balls overlap, applies an equal-and-opposite force proportional to the\n * overlap, easing them apart rather than resolving the collision instantly.\n * **Mutates both balls'** velocities in place.\n *\n * @param ball1 - First ball (its `vx`/`vy` are mutated).\n * @param ball2 - Second ball (its `vx`/`vy` are mutated).\n * @param spring - Stiffness of the separating force, 0–1. Defaults to `0.05`.\n * @returns Nothing — both balls' velocities are updated in place.\n * @example\n * ballToBallBounce(a, b); // call for each pair, once per frame\n */\nfunction ballToBallBounce(ball1: Ball, ball2: Ball, spring: number = 0.05): void {\n    if (ball1 === ball2)\n        return;\n    let dx = ball2.x - ball1.x;\n    let dy = ball2.y - ball1.y;\n    let distance = Math.sqrt(dx * dx + dy * dy);\n    let minDist = ball2.radius + ball1.radius;\n    if (distance < minDist) {\n        let angle = Math.atan2(dy, dx);\n        let targetX = ball1.x + Math.cos(angle) * minDist;\n        let targetY = ball1.y + Math.sin(angle) * minDist;\n        let xOverlap = targetX - ball2.x;\n        let yOverlap = targetY - ball2.y;\n        let ax = xOverlap * spring;\n        let ay = yOverlap * spring;\n        ball1.vx -= ax;\n        ball1.vy -= ay;\n        ball2.vx += ax;\n        ball2.vy += ay;\n    }\n}","jsSource":"/**\n * Push two overlapping balls apart with a spring-like force (soft collision response).\n *\n * When the balls overlap, applies an equal-and-opposite force proportional to the\n * overlap, easing them apart rather than resolving the collision instantly.\n * **Mutates both balls'** velocities in place.\n *\n * @param ball1 - First ball (its `vx`/`vy` are mutated).\n * @param ball2 - Second ball (its `vx`/`vy` are mutated).\n * @param spring - Stiffness of the separating force, 0–1. Defaults to `0.05`.\n * @returns Nothing — both balls' velocities are updated in place.\n * @example\n * ballToBallBounce(a, b); // call for each pair, once per frame\n */\nfunction ballToBallBounce(ball1, ball2, spring = 0.05) {\n    if (ball1 === ball2)\n        return;\n    let dx = ball2.x - ball1.x;\n    let dy = ball2.y - ball1.y;\n    let distance = Math.sqrt(dx * dx + dy * dy);\n    let minDist = ball2.radius + ball1.radius;\n    if (distance < minDist) {\n        let angle = Math.atan2(dy, dx);\n        let targetX = ball1.x + Math.cos(angle) * minDist;\n        let targetY = ball1.y + Math.sin(angle) * minDist;\n        let xOverlap = targetX - ball2.x;\n        let yOverlap = targetY - ball2.y;\n        let ax = xOverlap * spring;\n        let ay = yOverlap * spring;\n        ball1.vx -= ax;\n        ball1.vy -= ay;\n        ball2.vx += ax;\n        ball2.vy += ay;\n    }\n}","dependencyKeys":[],"interfaceNames":["Ball"]},
  {"key":"bezierPoint","module":"BezierCurve","tsSource":"/**\n * Find the point at parameter `t` along a cubic Bézier curve.\n *\n * Implemented via de Casteljau's algorithm — repeated linear interpolation between\n * the control points, which is the geometric definition of a Bézier curve.\n *\n * @param p0 - Start anchor point (returned at `t = 0`).\n * @param p1 - First control handle.\n * @param p2 - Second control handle.\n * @param p3 - End anchor point (returned at `t = 1`).\n * @param t - Position along the curve, 0–1.\n * @returns The `{ x, y }` point on the curve at `t`.\n * @example\n * bezierPoint({x:0,y:0}, {x:0,y:50}, {x:50,y:50}, {x:50,y:0}, 0.5);\n */\nfunction bezierPoint(p0: Point, p1: Point, p2: Point, p3: Point, t: number): Point {\n    return deCasteljau([p0, p1, p2, p3], t).point;\n}","jsSource":"/**\n * Find the point at parameter `t` along a cubic Bézier curve.\n *\n * Implemented via de Casteljau's algorithm — repeated linear interpolation between\n * the control points, which is the geometric definition of a Bézier curve.\n *\n * @param p0 - Start anchor point (returned at `t = 0`).\n * @param p1 - First control handle.\n * @param p2 - Second control handle.\n * @param p3 - End anchor point (returned at `t = 1`).\n * @param t - Position along the curve, 0–1.\n * @returns The `{ x, y }` point on the curve at `t`.\n * @example\n * bezierPoint({x:0,y:0}, {x:0,y:50}, {x:50,y:50}, {x:50,y:0}, 0.5);\n */\nfunction bezierPoint(p0, p1, p2, p3, t) {\n    return deCasteljau([p0, p1, p2, p3], t).point;\n}","dependencyKeys":["deCasteljau"],"interfaceNames":["Point"]},
  {"key":"boidsStep","module":"Boids","tsSource":"/**\n * Advance a flock by one tick, in place, using Craig Reynolds' three boids rules.\n *\n * Accelerations are computed for every boid first (reading the *old* state), then\n * applied — so the result is independent of array order and fully deterministic for\n * the same inputs (no RNG, no clock). Neighbor search is the naive O(n²) all-pairs\n * scan: clear and correct, but for thousands of boids swap in a spatial grid.\n *\n * @param boids - The flock; each boid's `position` and `velocity` are mutated.\n * @param options - Weights/radii; merged over {@link DEFAULT_BOIDS_OPTIONS}.\n * @param bounds - Optional toroidal wrap so the flock never flies off-screen.\n * @returns Nothing; `boids` is updated in place.\n * @example\n * const flock = [{ position: { x: 0, y: 0 }, velocity: { x: 0, y: 0 } },\n *                { position: { x: 10, y: 0 }, velocity: { x: 0, y: 0 } }];\n * boidsStep(flock, { cohesionWeight: 1, alignmentWeight: 0, separationWeight: 0 });\n * // the two boids now steer toward each other\n */\nfunction boidsStep(boids: Boid[], options: Partial<BoidsOptions> = {}, bounds?: FlockBounds): void {\n    const o: BoidsOptions = { ...DEFAULT_BOIDS_OPTIONS, ...options };\n    const perceptionSq = o.perceptionRadius * o.perceptionRadius;\n    const separationSq = o.separationRadius * o.separationRadius;\n    const accelerations: Vector[] = new Array(boids.length);\n    for (let i = 0; i < boids.length; i++) {\n        const b = boids[i];\n        let sep: Vector = { x: 0, y: 0 };\n        let ali: Vector = { x: 0, y: 0 };\n        let coh: Vector = { x: 0, y: 0 };\n        let aliCount = 0;\n        let cohCount = 0;\n        for (let j = 0; j < boids.length; j++) {\n            if (i === j)\n                continue;\n            const other = boids[j];\n            const offset = vecSubtract(b.position, other.position); // points away from neighbor\n            const distSq = offset.x * offset.x + offset.y * offset.y;\n            if (distSq === 0)\n                continue;\n            if (distSq < perceptionSq) {\n                ali = vecAdd(ali, other.velocity);\n                coh = vecAdd(coh, other.position);\n                aliCount++;\n                cohCount++;\n            }\n            if (distSq < separationSq) {\n                // weight by closeness: the nearer the neighbor, the harder we shove away\n                const dist = Math.sqrt(distSq);\n                sep = vecAdd(sep, vecScale(vecNormalize(offset), 1 / dist));\n            }\n        }\n        let accel: Vector = { x: 0, y: 0 };\n        if (sep.x !== 0 || sep.y !== 0) {\n            const f = steer(sep, b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.separationWeight));\n        }\n        if (aliCount > 0) {\n            const f = steer(vecScale(ali, 1 / aliCount), b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.alignmentWeight));\n        }\n        if (cohCount > 0) {\n            const center = vecScale(coh, 1 / cohCount);\n            const f = steer(vecSubtract(center, b.position), b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.cohesionWeight));\n        }\n        if (o.target && o.targetWeight !== 0) {\n            const f = steer(vecSubtract(o.target, b.position), b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.targetWeight));\n        }\n        accelerations[i] = accel;\n    }\n    for (let i = 0; i < boids.length; i++) {\n        const b = boids[i];\n        b.velocity = vecLimit(vecAdd(b.velocity, accelerations[i]), o.maxSpeed);\n        b.position = vecAdd(b.position, b.velocity);\n        if (bounds) {\n            if (b.position.x < 0)\n                b.position.x += bounds.width;\n            else if (b.position.x >= bounds.width)\n                b.position.x -= bounds.width;\n            if (b.position.y < 0)\n                b.position.y += bounds.height;\n            else if (b.position.y >= bounds.height)\n                b.position.y -= bounds.height;\n        }\n    }\n}","jsSource":"/**\n * Advance a flock by one tick, in place, using Craig Reynolds' three boids rules.\n *\n * Accelerations are computed for every boid first (reading the *old* state), then\n * applied — so the result is independent of array order and fully deterministic for\n * the same inputs (no RNG, no clock). Neighbor search is the naive O(n²) all-pairs\n * scan: clear and correct, but for thousands of boids swap in a spatial grid.\n *\n * @param boids - The flock; each boid's `position` and `velocity` are mutated.\n * @param options - Weights/radii; merged over {@link DEFAULT_BOIDS_OPTIONS}.\n * @param bounds - Optional toroidal wrap so the flock never flies off-screen.\n * @returns Nothing; `boids` is updated in place.\n * @example\n * const flock = [{ position: { x: 0, y: 0 }, velocity: { x: 0, y: 0 } },\n *                { position: { x: 10, y: 0 }, velocity: { x: 0, y: 0 } }];\n * boidsStep(flock, { cohesionWeight: 1, alignmentWeight: 0, separationWeight: 0 });\n * // the two boids now steer toward each other\n */\nfunction boidsStep(boids, options = {}, bounds) {\n    const o = { ...DEFAULT_BOIDS_OPTIONS, ...options };\n    const perceptionSq = o.perceptionRadius * o.perceptionRadius;\n    const separationSq = o.separationRadius * o.separationRadius;\n    const accelerations = new Array(boids.length);\n    for (let i = 0; i < boids.length; i++) {\n        const b = boids[i];\n        let sep = { x: 0, y: 0 };\n        let ali = { x: 0, y: 0 };\n        let coh = { x: 0, y: 0 };\n        let aliCount = 0;\n        let cohCount = 0;\n        for (let j = 0; j < boids.length; j++) {\n            if (i === j)\n                continue;\n            const other = boids[j];\n            const offset = vecSubtract(b.position, other.position); // points away from neighbor\n            const distSq = offset.x * offset.x + offset.y * offset.y;\n            if (distSq === 0)\n                continue;\n            if (distSq < perceptionSq) {\n                ali = vecAdd(ali, other.velocity);\n                coh = vecAdd(coh, other.position);\n                aliCount++;\n                cohCount++;\n            }\n            if (distSq < separationSq) {\n                // weight by closeness: the nearer the neighbor, the harder we shove away\n                const dist = Math.sqrt(distSq);\n                sep = vecAdd(sep, vecScale(vecNormalize(offset), 1 / dist));\n            }\n        }\n        let accel = { x: 0, y: 0 };\n        if (sep.x !== 0 || sep.y !== 0) {\n            const f = steer(sep, b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.separationWeight));\n        }\n        if (aliCount > 0) {\n            const f = steer(vecScale(ali, 1 / aliCount), b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.alignmentWeight));\n        }\n        if (cohCount > 0) {\n            const center = vecScale(coh, 1 / cohCount);\n            const f = steer(vecSubtract(center, b.position), b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.cohesionWeight));\n        }\n        if (o.target && o.targetWeight !== 0) {\n            const f = steer(vecSubtract(o.target, b.position), b.velocity, o.maxSpeed, o.maxForce);\n            accel = vecAdd(accel, vecScale(f, o.targetWeight));\n        }\n        accelerations[i] = accel;\n    }\n    for (let i = 0; i < boids.length; i++) {\n        const b = boids[i];\n        b.velocity = vecLimit(vecAdd(b.velocity, accelerations[i]), o.maxSpeed);\n        b.position = vecAdd(b.position, b.velocity);\n        if (bounds) {\n            if (b.position.x < 0)\n                b.position.x += bounds.width;\n            else if (b.position.x >= bounds.width)\n                b.position.x -= bounds.width;\n            if (b.position.y < 0)\n                b.position.y += bounds.height;\n            else if (b.position.y >= bounds.height)\n                b.position.y -= bounds.height;\n        }\n    }\n}","dependencyKeys":["DEFAULT_BOIDS_OPTIONS","vecAdd","vecLimit","vecNormalize","vecScale","vecSubtract"],"interfaceNames":["Vector"]},
  {"key":"centerOnParent","module":"CenterOnParent","tsSource":"/**\n * Compute the top-left position that centers one box inside another.\n *\n * @param item - The child box being centered (its `width`/`height` are used).\n * @param parent - The container box to center within.\n * @returns The `{ x, y }` top-left coordinate that centers `item` in `parent`.\n * @example\n * centerOnParent({ width: 20, height: 10 }, { width: 100, height: 50 }); // => { x: 40, y: 20 }\n */\nfunction centerOnParent(item: Container, parent: Container): Point {\n    let x = (parent.width - item.width) / 2;\n    let y = (parent.height - item.height) / 2;\n    return { x, y };\n}","jsSource":"/**\n * Compute the top-left position that centers one box inside another.\n *\n * @param item - The child box being centered (its `width`/`height` are used).\n * @param parent - The container box to center within.\n * @returns The `{ x, y }` top-left coordinate that centers `item` in `parent`.\n * @example\n * centerOnParent({ width: 20, height: 10 }, { width: 100, height: 50 }); // => { x: 40, y: 20 }\n */\nfunction centerOnParent(item, parent) {\n    let x = (parent.width - item.width) / 2;\n    let y = (parent.height - item.height) / 2;\n    return { x, y };\n}","dependencyKeys":[],"interfaceNames":["Container","Point"]},
  {"key":"circleCircle","module":"CollisionObjectAPI/CircleCircle","tsSource":"/**\n * Test whether two circles overlap (object-argument form of `circleToCircle`).\n *\n * @param circle1 - First circle (`x`, `y`, `radius`).\n * @param circle2 - Second circle (`x`, `y`, `radius`).\n * @returns `true` if the circles touch or overlap, including exact tangency.\n * @remarks\n * This object-argument form is the **recommended** one; {@link circleToCircle} is the\n * flat, six-number equivalent that runs the same squared-distance comparison.\n * @example\n * circleCircle({ x: 0, y: 0, radius: 5 }, { x: 6, y: 0, radius: 5 }); // => true\n */\nfunction circleCircle(circle1: Circle, circle2: Circle): boolean {\n    const dx = circle1.x - circle2.x;\n    const dy = circle1.y - circle2.y;\n    const radii = circle1.radius + circle2.radius;\n    return dx * dx + dy * dy <= radii * radii;\n}","jsSource":"/**\n * Test whether two circles overlap (object-argument form of `circleToCircle`).\n *\n * @param circle1 - First circle (`x`, `y`, `radius`).\n * @param circle2 - Second circle (`x`, `y`, `radius`).\n * @returns `true` if the circles touch or overlap, including exact tangency.\n * @remarks\n * This object-argument form is the **recommended** one; {@link circleToCircle} is the\n * flat, six-number equivalent that runs the same squared-distance comparison.\n * @example\n * circleCircle({ x: 0, y: 0, radius: 5 }, { x: 6, y: 0, radius: 5 }); // => true\n */\nfunction circleCircle(circle1, circle2) {\n    const dx = circle1.x - circle2.x;\n    const dy = circle1.y - circle2.y;\n    const radii = circle1.radius + circle2.radius;\n    return dx * dx + dy * dy <= radii * radii;\n}","dependencyKeys":[],"interfaceNames":["Circle"]},
  {"key":"circleFromThreePoints","module":"CircleFromThreePoints","tsSource":"/**\n * Find the unique circle (the circumcircle) passing through three points.\n *\n * @param point1 - First point on the circle.\n * @param point2 - Second point on the circle.\n * @param point3 - Third point on the circle.\n * @returns The circle's `radius` and `center` `{ x, y }`. (Degenerate for collinear points.)\n * @example\n * circleFromThreePoints({ x: 5, y: 0 }, { x: 0, y: 5 }, { x: -5, y: 0 });\n * // => { radius: 5, center: { x: 0, y: 0 } }\n */\nfunction circleFromThreePoints(point1: Point, point2: Point, point3: Point): {\n    radius: number;\n    center: Point;\n} {\n    let x12 = point1.x - point2.x;\n    let x13 = point1.x - point3.x;\n    let y12 = point1.y - point2.y;\n    let y13 = point1.y - point3.y;\n    let x31 = point3.x - point1.x;\n    let x21 = point2.x - point1.x;\n    let y31 = point3.y - point1.y;\n    let y21 = point2.y - point1.y;\n    let sx13 = Math.pow(point1.x, 2) - Math.pow(point3.x, 2);\n    let sy13 = Math.pow(point1.y, 2) - Math.pow(point3.y, 2);\n    let sx21 = Math.pow(point2.x, 2) - Math.pow(point1.x, 2);\n    let sy21 = Math.pow(point2.y, 2) - Math.pow(point1.y, 2);\n    let f = (sx13 * x12 + sy13 * x12 + sx21 * x13 + sy21 * x13) /\n        (2 * (y31 * x12 - y21 * x13));\n    let g = (sx13 * y12 + sy13 * y12 + sx21 * y13 + sy21 * y13) /\n        (2 * (x31 * y12 - x21 * y13));\n    let c = -Math.pow(point1.x, 2) -\n        Math.pow(point1.y, 2) -\n        2 * g * point1.x -\n        2 * f * point1.y;\n    let x = -g;\n    let y = -f;\n    let sqr_of_r = x * x + y * y - c;\n    let radius = Math.sqrt(sqr_of_r);\n    return { radius, center: { x, y } };\n}","jsSource":"/**\n * Find the unique circle (the circumcircle) passing through three points.\n *\n * @param point1 - First point on the circle.\n * @param point2 - Second point on the circle.\n * @param point3 - Third point on the circle.\n * @returns The circle's `radius` and `center` `{ x, y }`. (Degenerate for collinear points.)\n * @example\n * circleFromThreePoints({ x: 5, y: 0 }, { x: 0, y: 5 }, { x: -5, y: 0 });\n * // => { radius: 5, center: { x: 0, y: 0 } }\n */\nfunction circleFromThreePoints(point1, point2, point3) {\n    let x12 = point1.x - point2.x;\n    let x13 = point1.x - point3.x;\n    let y12 = point1.y - point2.y;\n    let y13 = point1.y - point3.y;\n    let x31 = point3.x - point1.x;\n    let x21 = point2.x - point1.x;\n    let y31 = point3.y - point1.y;\n    let y21 = point2.y - point1.y;\n    let sx13 = Math.pow(point1.x, 2) - Math.pow(point3.x, 2);\n    let sy13 = Math.pow(point1.y, 2) - Math.pow(point3.y, 2);\n    let sx21 = Math.pow(point2.x, 2) - Math.pow(point1.x, 2);\n    let sy21 = Math.pow(point2.y, 2) - Math.pow(point1.y, 2);\n    let f = (sx13 * x12 + sy13 * x12 + sx21 * x13 + sy21 * x13) /\n        (2 * (y31 * x12 - y21 * x13));\n    let g = (sx13 * y12 + sy13 * y12 + sx21 * y13 + sy21 * y13) /\n        (2 * (x31 * y12 - x21 * y13));\n    let c = -Math.pow(point1.x, 2) -\n        Math.pow(point1.y, 2) -\n        2 * g * point1.x -\n        2 * f * point1.y;\n    let x = -g;\n    let y = -f;\n    let sqr_of_r = x * x + y * y - c;\n    let radius = Math.sqrt(sqr_of_r);\n    return { radius, center: { x, y } };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"circleToCircle","module":"CircleToCircle","tsSource":"/**\n * Test whether two circles overlap (or touch).\n *\n * @param x1 - First circle center x.\n * @param y1 - First circle center y.\n * @param r1 - First circle radius.\n * @param x2 - Second circle center x.\n * @param y2 - Second circle center y.\n * @param r2 - Second circle radius.\n * @returns `true` if the distance between centers is `<= r1 + r2`.\n * @remarks\n * This is the flat, six-number form. For new code the **recommended** shape is the\n * object-argument {@link circleCircle}, which reads `circleCircle({ x, y, radius }, …)`.\n * Both share the same squared-distance comparison (no `sqrt`, since comparing squared\n * lengths gives the identical answer for non-negative radii).\n * @example\n * circleToCircle(0, 0, 5, 6, 0, 5); // => true\n */\nfunction circleToCircle(x1: number, y1: number, r1: number, x2: number, y2: number, r2: number): boolean {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const radii = r1 + r2;\n    return dx * dx + dy * dy <= radii * radii;\n}","jsSource":"/**\n * Test whether two circles overlap (or touch).\n *\n * @param x1 - First circle center x.\n * @param y1 - First circle center y.\n * @param r1 - First circle radius.\n * @param x2 - Second circle center x.\n * @param y2 - Second circle center y.\n * @param r2 - Second circle radius.\n * @returns `true` if the distance between centers is `<= r1 + r2`.\n * @remarks\n * This is the flat, six-number form. For new code the **recommended** shape is the\n * object-argument {@link circleCircle}, which reads `circleCircle({ x, y, radius }, …)`.\n * Both share the same squared-distance comparison (no `sqrt`, since comparing squared\n * lengths gives the identical answer for non-negative radii).\n * @example\n * circleToCircle(0, 0, 5, 6, 0, 5); // => true\n */\nfunction circleToCircle(x1, y1, r1, x2, y2, r2) {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const radii = r1 + r2;\n    return dx * dx + dy * dy <= radii * radii;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"circleToRect","module":"CircleToRect","tsSource":"/**\n * Test whether a circle overlaps an axis-aligned rectangle.\n *\n * Works by clamping the circle's center to the rectangle to find the closest\n * point on the rect, then checking if that point is within the radius.\n *\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param cr - Circle radius.\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @returns `true` if the circle and rectangle overlap or touch.\n * @example\n * circleToRect(3, 0, 5, 0, 0, 10, 10); // => true\n */\nfunction circleToRect(cx: number, cy: number, cr: number, rx: number, ry: number, rw: number, rh: number): boolean {\n    let px = Math.max(rx, Math.min(cx, rx + rw));\n    let py = Math.max(ry, Math.min(cy, ry + rh));\n    let dx = cx - px, dy = cy - py;\n    return dx * dx + dy * dy <= cr * cr;\n}","jsSource":"/**\n * Test whether a circle overlaps an axis-aligned rectangle.\n *\n * Works by clamping the circle's center to the rectangle to find the closest\n * point on the rect, then checking if that point is within the radius.\n *\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param cr - Circle radius.\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @returns `true` if the circle and rectangle overlap or touch.\n * @example\n * circleToRect(3, 0, 5, 0, 0, 10, 10); // => true\n */\nfunction circleToRect(cx, cy, cr, rx, ry, rw, rh) {\n    let px = Math.max(rx, Math.min(cx, rx + rw));\n    let py = Math.max(ry, Math.min(cy, ry + rh));\n    let dx = cx - px, dy = cy - py;\n    return dx * dx + dy * dy <= cr * cr;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"clamp","module":"Clamp","tsSource":"/**\n * Constrain a value to a range, so it never goes below `min` or above `max`.\n *\n * @param value - The value to constrain.\n * @param min - Lower bound.\n * @param max - Upper bound.\n * @returns `value` limited to the range `[min, max]`.\n * @example\n * clamp(15, 0, 10); // => 10\n * clamp(-3, 0, 10); // => 0\n */\nfunction clamp(value: number, min: number, max: number): number {\n    return Math.min(max, Math.max(min, value));\n}","jsSource":"/**\n * Constrain a value to a range, so it never goes below `min` or above `max`.\n *\n * @param value - The value to constrain.\n * @param min - Lower bound.\n * @param max - Upper bound.\n * @returns `value` limited to the range `[min, max]`.\n * @example\n * clamp(15, 0, 10); // => 10\n * clamp(-3, 0, 10); // => 0\n */\nfunction clamp(value, min, max) {\n    return Math.min(max, Math.max(min, value));\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"colorFamily","module":"Color","tsSource":"/**\n * Build an ordered palette of colors that all belong to one named hue family.\n *\n * The spiritual successor to a \"give me a random blue\" helper: instead of one\n * random color, you get an evenly-spread *range* within the family (all blues,\n * all greens, …). Hue marches across the family's band while lightness arcs up\n * toward the middle of the ramp, so the set reads as a coherent, lively palette\n * rather than a flat gradient. Deterministic — same args, same palette.\n *\n * @param family - One of {@link HUE_FAMILIES} (`\"blue\"`, `\"yellow\"`, `\"all\"`, …).\n * @param count - How many swatches to produce (>= 1).\n * @returns An array of {@link RGB} colors, ordered across the family's hue range.\n * @example\n * colorFamily(\"blue\", 5);   // five blues, light→deep→light across the band\n * colorFamily(\"all\", 12);   // a full evenly-spaced rainbow\n */\nfunction colorFamily(family: HueFamily, count: number): RGB[] {\n    const [min, max] = HUE_FAMILIES[family] ?? HUE_FAMILIES.all;\n    const span = max - min;\n    const out: RGB[] = [];\n    for (let i = 0; i < count; i++) {\n        const f = count <= 1 ? 0.5 : i / (count - 1);\n        const h = min + f * span;\n        const s = 72;\n        const l = 50 + Math.sin(f * Math.PI) * 16; // brighter through the middle of the ramp\n        out.push(hslToRgb({ h, s, l }));\n    }\n    return out;\n}","jsSource":"/**\n * Build an ordered palette of colors that all belong to one named hue family.\n *\n * The spiritual successor to a \"give me a random blue\" helper: instead of one\n * random color, you get an evenly-spread *range* within the family (all blues,\n * all greens, …). Hue marches across the family's band while lightness arcs up\n * toward the middle of the ramp, so the set reads as a coherent, lively palette\n * rather than a flat gradient. Deterministic — same args, same palette.\n *\n * @param family - One of {@link HUE_FAMILIES} (`\"blue\"`, `\"yellow\"`, `\"all\"`, …).\n * @param count - How many swatches to produce (>= 1).\n * @returns An array of {@link RGB} colors, ordered across the family's hue range.\n * @example\n * colorFamily(\"blue\", 5);   // five blues, light→deep→light across the band\n * colorFamily(\"all\", 12);   // a full evenly-spaced rainbow\n */\nfunction colorFamily(family, count) {\n    const [min, max] = HUE_FAMILIES[family] ?? HUE_FAMILIES.all;\n    const span = max - min;\n    const out = [];\n    for (let i = 0; i < count; i++) {\n        const f = count <= 1 ? 0.5 : i / (count - 1);\n        const h = min + f * span;\n        const s = 72;\n        const l = 50 + Math.sin(f * Math.PI) * 16; // brighter through the middle of the ramp\n        out.push(hslToRgb({ h, s, l }));\n    }\n    return out;\n}","dependencyKeys":["HUE_FAMILIES","hslToRgb"],"interfaceNames":[]},
  {"key":"createRect","module":"Rectangle","tsSource":"/**\n * Compute the four corner vertices of a rectangle, centered on the origin.\n *\n * @param width - Rectangle width.\n * @param height - Rectangle height.\n * @param angle - Rotation in radians about the center. Defaults to `0`.\n * @param options - Optional spin animation: `{ rotate, rotateSpeed, clockwise, time }`. When\n *   `rotate` is true the angle advances with `options.time` (ms, e.g. `performance.now()`),\n *   keeping the function pure — pass the time in rather than reading the clock here.\n * @returns `{ vertices }` — the four corners as points.\n * @example\n * createRect(10, 10).vertices.length; // => 4\n */\nfunction createRect(width: number, height: number, angle: number = 0, options: {\n    rotate?: boolean;\n    rotateSpeed?: number;\n    clockwise?: boolean;\n    time?: number;\n} = {\n    rotate: false,\n    rotateSpeed: 1000,\n    clockwise: true,\n}): {\n    vertices: Point[];\n} {\n    let vertices: Point[] = [];\n    let x = width / 2;\n    let y = height / 2;\n    let center: Point = { x: 0, y: 0 };\n    let dist = lineLength({\n        startPoint: center,\n        endPoint: { x, y },\n    });\n    let atan2 = getRotation(center, {\n        x,\n        y,\n    });\n    const rotate = options.rotate ?? false;\n    const rotateSpeed = options.rotateSpeed ?? 1000;\n    const clockwise = options.clockwise ?? true;\n    const time = options.time ?? 0;\n    let rotateQ = rotate\n        ? time / rotateSpeed\n        : 0;\n    let spinDirection = clockwise\n        ? [-rotateQ, rotateQ]\n        : [rotateQ, -rotateQ];\n    x = -dist * Math.cos(atan2 + angle + spinDirection[0]);\n    y = dist * Math.sin(atan2 + angle + spinDirection[0]);\n    vertices.push({ x, y });\n    x = dist * Math.cos(atan2 - angle + spinDirection[1]);\n    y = dist * Math.sin(atan2 - angle + spinDirection[1]);\n    vertices.push({ x, y });\n    x = dist * Math.cos(atan2 + angle + spinDirection[0]);\n    y = -dist * Math.sin(atan2 + angle + spinDirection[0]);\n    vertices.push({ x, y });\n    x = -dist * Math.cos(atan2 - angle + spinDirection[1]);\n    y = -dist * Math.sin(atan2 - angle + spinDirection[1]);\n    vertices.push({ x, y });\n    return { vertices };\n}","jsSource":"/**\n * Compute the four corner vertices of a rectangle, centered on the origin.\n *\n * @param width - Rectangle width.\n * @param height - Rectangle height.\n * @param angle - Rotation in radians about the center. Defaults to `0`.\n * @param options - Optional spin animation: `{ rotate, rotateSpeed, clockwise, time }`. When\n *   `rotate` is true the angle advances with `options.time` (ms, e.g. `performance.now()`),\n *   keeping the function pure — pass the time in rather than reading the clock here.\n * @returns `{ vertices }` — the four corners as points.\n * @example\n * createRect(10, 10).vertices.length; // => 4\n */\nfunction createRect(width, height, angle = 0, options = {\n    rotate: false,\n    rotateSpeed: 1000,\n    clockwise: true,\n}) {\n    let vertices = [];\n    let x = width / 2;\n    let y = height / 2;\n    let center = { x: 0, y: 0 };\n    let dist = lineLength({\n        startPoint: center,\n        endPoint: { x, y },\n    });\n    let atan2 = getRotation(center, {\n        x,\n        y,\n    });\n    const rotate = options.rotate ?? false;\n    const rotateSpeed = options.rotateSpeed ?? 1000;\n    const clockwise = options.clockwise ?? true;\n    const time = options.time ?? 0;\n    let rotateQ = rotate\n        ? time / rotateSpeed\n        : 0;\n    let spinDirection = clockwise\n        ? [-rotateQ, rotateQ]\n        : [rotateQ, -rotateQ];\n    x = -dist * Math.cos(atan2 + angle + spinDirection[0]);\n    y = dist * Math.sin(atan2 + angle + spinDirection[0]);\n    vertices.push({ x, y });\n    x = dist * Math.cos(atan2 - angle + spinDirection[1]);\n    y = dist * Math.sin(atan2 - angle + spinDirection[1]);\n    vertices.push({ x, y });\n    x = dist * Math.cos(atan2 + angle + spinDirection[0]);\n    y = -dist * Math.sin(atan2 + angle + spinDirection[0]);\n    vertices.push({ x, y });\n    x = -dist * Math.cos(atan2 - angle + spinDirection[1]);\n    y = -dist * Math.sin(atan2 - angle + spinDirection[1]);\n    vertices.push({ x, y });\n    return { vertices };\n}","dependencyKeys":["getRotation","lineLength"],"interfaceNames":["Point"]},
  {"key":"criticalDamping","module":"Animate","tsSource":"/**\n * The damping coefficient that makes {@link springValue} *critically* damped — the\n * fastest approach to the target with no overshoot — for a given `stiffness` and `mass`.\n *\n * Critical damping is `c = 2·√(k·m)`. Pass less to {@link springValue}'s `damping` for\n * bounce (underdamped), more for a sluggish crawl (overdamped). With the default\n * stiffness of 170 this is `≈ 26`, which is why 26 is the default damping.\n *\n * @example\n * criticalDamping(170); // => ~26.08\n */\nfunction criticalDamping(stiffness: number, mass = 1): number {\n    return 2 * Math.sqrt(stiffness * mass);\n}","jsSource":"/**\n * The damping coefficient that makes {@link springValue} *critically* damped — the\n * fastest approach to the target with no overshoot — for a given `stiffness` and `mass`.\n *\n * Critical damping is `c = 2·√(k·m)`. Pass less to {@link springValue}'s `damping` for\n * bounce (underdamped), more for a sluggish crawl (overdamped). With the default\n * stiffness of 170 this is `≈ 26`, which is why 26 is the default damping.\n *\n * @example\n * criticalDamping(170); // => ~26.08\n */\nfunction criticalDamping(stiffness, mass = 1) {\n    return 2 * Math.sqrt(stiffness * mass);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"deCasteljau","module":"DeCasteljau","tsSource":"/**\n * Evaluate any-degree Bézier curve at parameter `t` using de Casteljau's algorithm.\n *\n * De Casteljau works by repeatedly lerping between adjacent control points until\n * a single point remains. At each level of reduction you get the intermediate\n * \"construction\" points that make the algorithm visual — pass the returned `levels`\n * array to your canvas code to draw the animated hull the way a textbook would.\n * Works for quadratic (3 pts), cubic (4 pts), quartic (5 pts), or any degree.\n *\n * @param points - The ordered control points defining the curve (any length >= 2).\n * @param t - Position along the curve, 0–1 (0 = start, 1 = end).\n * @returns An object `{ point, levels }` where `point` is the `{ x, y }` on the\n *   curve at `t`, and `levels` is an array of point arrays — `levels[0]` is the\n *   original control polygon, each subsequent level is the next reduction step,\n *   and the last level contains only `point`.\n * @example\n * deCasteljau([{x:0,y:0},{x:0,y:50},{x:50,y:50},{x:50,y:0}], 0.5).point;\n */\nfunction deCasteljau(points: Point[], t: number): {\n    point: Point;\n    levels: Point[][];\n} {\n    const levels: Point[][] = [points.map((p) => ({ ...p }))];\n    let current = points.map((p) => ({ ...p }));\n    while (current.length > 1) {\n        const next: Point[] = [];\n        for (let i = 0; i < current.length - 1; i++) {\n            next.push({\n                x: (1 - t) * current[i].x + t * current[i + 1].x,\n                y: (1 - t) * current[i].y + t * current[i + 1].y,\n            });\n        }\n        levels.push(next);\n        current = next;\n    }\n    return { point: current[0], levels };\n}","jsSource":"/**\n * Evaluate any-degree Bézier curve at parameter `t` using de Casteljau's algorithm.\n *\n * De Casteljau works by repeatedly lerping between adjacent control points until\n * a single point remains. At each level of reduction you get the intermediate\n * \"construction\" points that make the algorithm visual — pass the returned `levels`\n * array to your canvas code to draw the animated hull the way a textbook would.\n * Works for quadratic (3 pts), cubic (4 pts), quartic (5 pts), or any degree.\n *\n * @param points - The ordered control points defining the curve (any length >= 2).\n * @param t - Position along the curve, 0–1 (0 = start, 1 = end).\n * @returns An object `{ point, levels }` where `point` is the `{ x, y }` on the\n *   curve at `t`, and `levels` is an array of point arrays — `levels[0]` is the\n *   original control polygon, each subsequent level is the next reduction step,\n *   and the last level contains only `point`.\n * @example\n * deCasteljau([{x:0,y:0},{x:0,y:50},{x:50,y:50},{x:50,y:0}], 0.5).point;\n */\nfunction deCasteljau(points, t) {\n    const levels = [points.map((p) => ({ ...p }))];\n    let current = points.map((p) => ({ ...p }));\n    while (current.length > 1) {\n        const next = [];\n        for (let i = 0; i < current.length - 1; i++) {\n            next.push({\n                x: (1 - t) * current[i].x + t * current[i + 1].x,\n                y: (1 - t) * current[i].y + t * current[i + 1].y,\n            });\n        }\n        levels.push(next);\n        current = next;\n    }\n    return { point: current[0], levels };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"degToRad","module":"DegToRad","tsSource":"/**\n * Convert an angle from degrees to radians.\n *\n * @param degree - Angle in degrees.\n * @returns The equivalent angle in radians.\n * @example\n * degToRad(180); // => 3.141592653589793\n */\nfunction degToRad(degree: number): number {\n    return degree * (Math.PI / 180);\n}","jsSource":"/**\n * Convert an angle from degrees to radians.\n *\n * @param degree - Angle in degrees.\n * @returns The equivalent angle in radians.\n * @example\n * degToRad(180); // => 3.141592653589793\n */\nfunction degToRad(degree) {\n    return degree * (Math.PI / 180);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"delay","module":"Animate","tsSource":"/**\n * Delay a progress value by `delayMs`, returning 0 until the delay has elapsed.\n *\n * @example\n * delay(300, 200, 500); // => 0.2 (100ms into a 500ms tween)\n */\nfunction delay(elapsedMs: number, delayMs: number, durationMs: number): number {\n    if (elapsedMs <= delayMs)\n        return 0;\n    if (durationMs <= 0)\n        return 1;\n    return clamp((elapsedMs - delayMs) / durationMs, 0, 1);\n}","jsSource":"/**\n * Delay a progress value by `delayMs`, returning 0 until the delay has elapsed.\n *\n * @example\n * delay(300, 200, 500); // => 0.2 (100ms into a 500ms tween)\n */\nfunction delay(elapsedMs, delayMs, durationMs) {\n    if (elapsedMs <= delayMs)\n        return 0;\n    if (durationMs <= 0)\n        return 1;\n    return clamp((elapsedMs - delayMs) / durationMs, 0, 1);\n}","dependencyKeys":["clamp"],"interfaceNames":[]},
  {"key":"dft","module":"DFT","tsSource":"/**\n * Discrete Fourier Transform of a list of 2D points (treated as complex numbers).\n *\n * Decomposes a path into a sum of rotating circles (epicycles) — the math behind\n * \"draw anything with spinning vectors\" animations. Each output entry is one\n * circle: its frequency, radius (`amp`), and starting angle (`phase`). Results are\n * sorted by amplitude so the most significant circles come first.\n *\n * @param pts - The input path as an array of points (`x` = real, `y` = imaginary).\n * @returns One `{ freq, amp, phase }` per input sample, sorted by descending `amp`.\n * @example\n * const circles = dft(pathPoints); // feed into an epicycle drawer\n */\nfunction dft(pts: Point[]): {\n    freq: number;\n    amp: number;\n    phase: number;\n}[] {\n    const N = pts.length;\n    const X: {\n        freq: number;\n        amp: number;\n        phase: number;\n    }[] = [];\n    for (let k = 0; k < N; k++) {\n        let re = 0, im = 0;\n        for (let n = 0; n < N; n++) {\n            const phi = (2 * Math.PI * k * n) / N;\n            re += pts[n].x * Math.cos(phi) + pts[n].y * Math.sin(phi);\n            im += -pts[n].x * Math.sin(phi) + pts[n].y * Math.cos(phi);\n        }\n        re /= N;\n        im /= N;\n        X.push({ freq: k, amp: Math.sqrt(re * re + im * im), phase: Math.atan2(im, re) });\n    }\n    return X.sort((a, b) => b.amp - a.amp);\n}","jsSource":"/**\n * Discrete Fourier Transform of a list of 2D points (treated as complex numbers).\n *\n * Decomposes a path into a sum of rotating circles (epicycles) — the math behind\n * \"draw anything with spinning vectors\" animations. Each output entry is one\n * circle: its frequency, radius (`amp`), and starting angle (`phase`). Results are\n * sorted by amplitude so the most significant circles come first.\n *\n * @param pts - The input path as an array of points (`x` = real, `y` = imaginary).\n * @returns One `{ freq, amp, phase }` per input sample, sorted by descending `amp`.\n * @example\n * const circles = dft(pathPoints); // feed into an epicycle drawer\n */\nfunction dft(pts) {\n    const N = pts.length;\n    const X = [];\n    for (let k = 0; k < N; k++) {\n        let re = 0, im = 0;\n        for (let n = 0; n < N; n++) {\n            const phi = (2 * Math.PI * k * n) / N;\n            re += pts[n].x * Math.cos(phi) + pts[n].y * Math.sin(phi);\n            im += -pts[n].x * Math.sin(phi) + pts[n].y * Math.cos(phi);\n        }\n        re /= N;\n        im /= N;\n        X.push({ freq: k, amp: Math.sqrt(re * re + im * im), phase: Math.atan2(im, re) });\n    }\n    return X.sort((a, b) => b.amp - a.amp);\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"distance","module":"Distance","tsSource":"/**\n * Euclidean (straight-line) distance between two points.\n *\n * @param p1 - First point.\n * @param p2 - Second point.\n * @returns The distance between `p1` and `p2`.\n * @remarks\n * This is the canonical point-to-point distance. The same Euclidean length shows up in\n * two other shapes across the library: {@link lineLength} (a `{ startPoint, endPoint }`\n * segment) and `vecMagnitude(vecSubtract(a, b))` (the vector idiom). All three compute\n * `√(Δx² + Δy²)` via `Math.hypot`; reach for the one whose argument shape you already have.\n * @example\n * distance({ x: 0, y: 0 }, { x: 3, y: 4 }); // => 5\n */\nfunction distance(p1: Point, p2: Point): number {\n    return Math.hypot(p2.x - p1.x, p2.y - p1.y);\n}","jsSource":"/**\n * Euclidean (straight-line) distance between two points.\n *\n * @param p1 - First point.\n * @param p2 - Second point.\n * @returns The distance between `p1` and `p2`.\n * @remarks\n * This is the canonical point-to-point distance. The same Euclidean length shows up in\n * two other shapes across the library: {@link lineLength} (a `{ startPoint, endPoint }`\n * segment) and `vecMagnitude(vecSubtract(a, b))` (the vector idiom). All three compute\n * `√(Δx² + Δy²)` via `Math.hypot`; reach for the one whose argument shape you already have.\n * @example\n * distance({ x: 0, y: 0 }, { x: 3, y: 4 }); // => 5\n */\nfunction distance(p1, p2) {\n    return Math.hypot(p2.x - p1.x, p2.y - p1.y);\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"distributeAroundCircle","module":"DistributeAroundCircle","tsSource":"/**\n * Evenly space N points around a circle (e.g. for a radial menu or clock face).\n *\n * @param circleCenter - Center of the circle.\n * @param radius - Radius of the circle.\n * @param totalItems - How many points to distribute. The first sits at angle 0 (3 o'clock).\n * @returns An array of `totalItems` points spread evenly around the circle.\n * @example\n * distributeAroundCircle({ x: 0, y: 0 }, 10, 4); // => 4 points at 3, 6, 9, 12 o'clock\n */\nfunction distributeAroundCircle(circleCenter: Point, radius: number, totalItems: number): Point[] {\n    const totalCircleRadians = Math.PI * 2;\n    const returnArray: Point[] = [];\n    for (let i = 0; i < totalItems; i++) {\n        const percent = i / totalItems;\n        returnArray.push({\n            x: circleCenter.x + radius * Math.cos(totalCircleRadians * percent),\n            y: circleCenter.y + radius * Math.sin(totalCircleRadians * percent),\n        });\n    }\n    return returnArray;\n}","jsSource":"/**\n * Evenly space N points around a circle (e.g. for a radial menu or clock face).\n *\n * @param circleCenter - Center of the circle.\n * @param radius - Radius of the circle.\n * @param totalItems - How many points to distribute. The first sits at angle 0 (3 o'clock).\n * @returns An array of `totalItems` points spread evenly around the circle.\n * @example\n * distributeAroundCircle({ x: 0, y: 0 }, 10, 4); // => 4 points at 3, 6, 9, 12 o'clock\n */\nfunction distributeAroundCircle(circleCenter, radius, totalItems) {\n    const totalCircleRadians = Math.PI * 2;\n    const returnArray = [];\n    for (let i = 0; i < totalItems; i++) {\n        const percent = i / totalItems;\n        returnArray.push({\n            x: circleCenter.x + radius * Math.cos(totalCircleRadians * percent),\n            y: circleCenter.y + radius * Math.sin(totalCircleRadians * percent),\n        });\n    }\n    return returnArray;\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"easeIn","module":"Easing","tsSource":"/**\n * Quadratic ease-in — starts slow, accelerates toward the end.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value (`t²`), 0 at `t=0` and 1 at `t=1`.\n * @example\n * easeIn(0.5); // => 0.25\n */\nfunction easeIn(t: number): number {\n    return t * t;\n}","jsSource":"/**\n * Quadratic ease-in — starts slow, accelerates toward the end.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value (`t²`), 0 at `t=0` and 1 at `t=1`.\n * @example\n * easeIn(0.5); // => 0.25\n */\nfunction easeIn(t) {\n    return t * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInCubic","module":"Easing","tsSource":"/**\n * Cubic ease-in.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInCubic(0.5); // => 0.125\n */\nfunction easeInCubic(t: number): number {\n    return t * t * t;\n}","jsSource":"/**\n * Cubic ease-in.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInCubic(0.5); // => 0.125\n */\nfunction easeInCubic(t) {\n    return t * t * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInOut","module":"Easing","tsSource":"/**\n * Quadratic ease-in-out — accelerates then decelerates, symmetric about `t=0.5`.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value, 0 at `t=0` and 1 at `t=1`.\n * @example\n * easeInOut(0.5); // => 0.5\n */\nfunction easeInOut(t: number): number {\n    return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;\n}","jsSource":"/**\n * Quadratic ease-in-out — accelerates then decelerates, symmetric about `t=0.5`.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value, 0 at `t=0` and 1 at `t=1`.\n * @example\n * easeInOut(0.5); // => 0.5\n */\nfunction easeInOut(t) {\n    return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInOutCubic","module":"Easing","tsSource":"/**\n * Cubic ease-in-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutCubic(0.5); // => 0.5\n */\nfunction easeInOutCubic(t: number): number {\n    return t < 0.5 ? 4 * t * t * t : (t - 1) * (2 * t - 2) * (2 * t - 2) + 1;\n}","jsSource":"/**\n * Cubic ease-in-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutCubic(0.5); // => 0.5\n */\nfunction easeInOutCubic(t) {\n    return t < 0.5 ? 4 * t * t * t : (t - 1) * (2 * t - 2) * (2 * t - 2) + 1;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInOutQuad","module":"Easing","tsSource":"/**\n * Quadratic ease-in-out. Legacy alias from the vanilla JS utilities.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutQuad(0.5); // => 0.5\n */\nfunction easeInOutQuad(t: number): number {\n    return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;\n}","jsSource":"/**\n * Quadratic ease-in-out. Legacy alias from the vanilla JS utilities.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutQuad(0.5); // => 0.5\n */\nfunction easeInOutQuad(t) {\n    return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInOutQuart","module":"Easing","tsSource":"/**\n * Quartic ease-in-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutQuart(0.5); // => 0.5\n */\nfunction easeInOutQuart(t: number): number {\n    if (t < 0.5)\n        return 8 * t * t * t * t;\n    const u = t - 1;\n    return 1 - 8 * u * u * u * u;\n}","jsSource":"/**\n * Quartic ease-in-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutQuart(0.5); // => 0.5\n */\nfunction easeInOutQuart(t) {\n    if (t < 0.5)\n        return 8 * t * t * t * t;\n    const u = t - 1;\n    return 1 - 8 * u * u * u * u;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInOutQuint","module":"Easing","tsSource":"/**\n * Quintic ease-in-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutQuint(0.5); // => 0.5\n */\nfunction easeInOutQuint(t: number): number {\n    if (t < 0.5)\n        return 16 * t * t * t * t * t;\n    const u = t - 1;\n    return 1 + 16 * u * u * u * u * u;\n}","jsSource":"/**\n * Quintic ease-in-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInOutQuint(0.5); // => 0.5\n */\nfunction easeInOutQuint(t) {\n    if (t < 0.5)\n        return 16 * t * t * t * t * t;\n    const u = t - 1;\n    return 1 + 16 * u * u * u * u * u;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInQuad","module":"Easing","tsSource":"/**\n * Quadratic ease-in. Legacy alias from the vanilla JS utilities.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInQuad(0.5); // => 0.25\n */\nfunction easeInQuad(t: number): number {\n    return t * t;\n}","jsSource":"/**\n * Quadratic ease-in. Legacy alias from the vanilla JS utilities.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInQuad(0.5); // => 0.25\n */\nfunction easeInQuad(t) {\n    return t * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInQuart","module":"Easing","tsSource":"/**\n * Quartic ease-in.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInQuart(0.5); // => 0.0625\n */\nfunction easeInQuart(t: number): number {\n    return t * t * t * t;\n}","jsSource":"/**\n * Quartic ease-in.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInQuart(0.5); // => 0.0625\n */\nfunction easeInQuart(t) {\n    return t * t * t * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeInQuint","module":"Easing","tsSource":"/**\n * Quintic ease-in.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInQuint(0.5); // => 0.03125\n */\nfunction easeInQuint(t: number): number {\n    return t * t * t * t * t;\n}","jsSource":"/**\n * Quintic ease-in.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeInQuint(0.5); // => 0.03125\n */\nfunction easeInQuint(t) {\n    return t * t * t * t * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOut","module":"Easing","tsSource":"/**\n * Quadratic ease-out — starts fast, decelerates toward the end.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value, 0 at `t=0` and 1 at `t=1`.\n * @example\n * easeOut(0.5); // => 0.75\n */\nfunction easeOut(t: number): number {\n    return t * (2 - t);\n}","jsSource":"/**\n * Quadratic ease-out — starts fast, decelerates toward the end.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value, 0 at `t=0` and 1 at `t=1`.\n * @example\n * easeOut(0.5); // => 0.75\n */\nfunction easeOut(t) {\n    return t * (2 - t);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOutBounce","module":"Easing","tsSource":"/**\n * Bounce ease-out using Robert Penner's common piecewise curve.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutBounce(1); // => 1 (lands exactly on the target)\n */\nfunction easeOutBounce(t: number): number {\n    if (t < 1 / 2.75) {\n        return 7.5625 * t * t;\n    }\n    if (t < 2 / 2.75) {\n        const u = t - 1.5 / 2.75;\n        return 7.5625 * u * u + 0.75;\n    }\n    if (t < 2.5 / 2.75) {\n        const u = t - 2.25 / 2.75;\n        return 7.5625 * u * u + 0.9375;\n    }\n    const u = t - 2.625 / 2.75;\n    return 7.5625 * u * u + 0.984375;\n}","jsSource":"/**\n * Bounce ease-out using Robert Penner's common piecewise curve.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutBounce(1); // => 1 (lands exactly on the target)\n */\nfunction easeOutBounce(t) {\n    if (t < 1 / 2.75) {\n        return 7.5625 * t * t;\n    }\n    if (t < 2 / 2.75) {\n        const u = t - 1.5 / 2.75;\n        return 7.5625 * u * u + 0.75;\n    }\n    if (t < 2.5 / 2.75) {\n        const u = t - 2.25 / 2.75;\n        return 7.5625 * u * u + 0.9375;\n    }\n    const u = t - 2.625 / 2.75;\n    return 7.5625 * u * u + 0.984375;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOutCubic","module":"Easing","tsSource":"/**\n * Cubic ease-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutCubic(0.5); // => 0.875\n */\nfunction easeOutCubic(t: number): number {\n    const u = t - 1;\n    return u * u * u + 1;\n}","jsSource":"/**\n * Cubic ease-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutCubic(0.5); // => 0.875\n */\nfunction easeOutCubic(t) {\n    const u = t - 1;\n    return u * u * u + 1;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOutElastic","module":"Easing","tsSource":"/**\n * Elastic ease-out with a short overshoot.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutElastic(1); // => 1 (settles on the target after overshooting)\n */\nfunction easeOutElastic(t: number): number {\n    const p = 0.3;\n    return Math.pow(2, -10 * t) * Math.sin(((t - p / 4) * (2 * Math.PI)) / p) + 1;\n}","jsSource":"/**\n * Elastic ease-out with a short overshoot.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutElastic(1); // => 1 (settles on the target after overshooting)\n */\nfunction easeOutElastic(t) {\n    const p = 0.3;\n    return Math.pow(2, -10 * t) * Math.sin(((t - p / 4) * (2 * Math.PI)) / p) + 1;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOutQuad","module":"Easing","tsSource":"/**\n * Quadratic ease-out. Legacy alias from the vanilla JS utilities.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutQuad(0.5); // => 0.75\n */\nfunction easeOutQuad(t: number): number {\n    return t * (2 - t);\n}","jsSource":"/**\n * Quadratic ease-out. Legacy alias from the vanilla JS utilities.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutQuad(0.5); // => 0.75\n */\nfunction easeOutQuad(t) {\n    return t * (2 - t);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOutQuart","module":"Easing","tsSource":"/**\n * Quartic ease-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutQuart(0.5); // => 0.9375\n */\nfunction easeOutQuart(t: number): number {\n    const u = t - 1;\n    return 1 - u * u * u * u;\n}","jsSource":"/**\n * Quartic ease-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutQuart(0.5); // => 0.9375\n */\nfunction easeOutQuart(t) {\n    const u = t - 1;\n    return 1 - u * u * u * u;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"easeOutQuint","module":"Easing","tsSource":"/**\n * Quintic ease-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutQuint(0.5); // => 0.96875\n */\nfunction easeOutQuint(t: number): number {\n    const u = t - 1;\n    return 1 + u * u * u * u * u;\n}","jsSource":"/**\n * Quintic ease-out.\n *\n * @param t - Normalized time, 0-1.\n * @returns The eased value.\n * @example\n * easeOutQuint(0.5); // => 0.96875\n */\nfunction easeOutQuint(t) {\n    const u = t - 1;\n    return 1 + u * u * u * u * u;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"equilateralTriangle","module":"EquilateralTriangle","tsSource":"/**\n * Compute the three vertices of an equilateral triangle inscribed in a circle.\n *\n * @param radius - Distance from the center to each vertex (the circumradius).\n * @param centerPoint - Center of the triangle.\n * @param angle - Rotation in radians; `point1` sits at this angle, the others 120° apart.\n * @returns The three corners as `{ point1, point2, point3 }`.\n * @example\n * equilateralTriangle(5, { x: 0, y: 0 }, 0); // point1 at { x: 5, y: 0 }\n */\nfunction equilateralTriangle(radius: number, centerPoint: Point, angle: number): {\n    point1: Point;\n    point2: Point;\n    point3: Point;\n} {\n    let allRadiansInACircle = 2 * Math.PI;\n    let point1 = {\n        x: radius * Math.cos(angle) + centerPoint.x,\n        y: radius * Math.sin(angle) + centerPoint.y,\n    };\n    let point2 = {\n        x: radius * Math.cos(angle + (1 / 3) * allRadiansInACircle) + centerPoint.x,\n        y: radius * Math.sin(angle + (1 / 3) * allRadiansInACircle) + centerPoint.y,\n    };\n    let point3 = {\n        x: radius * Math.cos(angle + (2 / 3) * allRadiansInACircle) + centerPoint.x,\n        y: radius * Math.sin(angle + (2 / 3) * allRadiansInACircle) + centerPoint.y,\n    };\n    return { point1, point2, point3 };\n}","jsSource":"/**\n * Compute the three vertices of an equilateral triangle inscribed in a circle.\n *\n * @param radius - Distance from the center to each vertex (the circumradius).\n * @param centerPoint - Center of the triangle.\n * @param angle - Rotation in radians; `point1` sits at this angle, the others 120° apart.\n * @returns The three corners as `{ point1, point2, point3 }`.\n * @example\n * equilateralTriangle(5, { x: 0, y: 0 }, 0); // point1 at { x: 5, y: 0 }\n */\nfunction equilateralTriangle(radius, centerPoint, angle) {\n    let allRadiansInACircle = 2 * Math.PI;\n    let point1 = {\n        x: radius * Math.cos(angle) + centerPoint.x,\n        y: radius * Math.sin(angle) + centerPoint.y,\n    };\n    let point2 = {\n        x: radius * Math.cos(angle + (1 / 3) * allRadiansInACircle) + centerPoint.x,\n        y: radius * Math.sin(angle + (1 / 3) * allRadiansInACircle) + centerPoint.y,\n    };\n    let point3 = {\n        x: radius * Math.cos(angle + (2 / 3) * allRadiansInACircle) + centerPoint.x,\n        y: radius * Math.sin(angle + (2 / 3) * allRadiansInACircle) + centerPoint.y,\n    };\n    return { point1, point2, point3 };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"findPointAroundCircle","module":"FindPointAroundCircle","tsSource":"/**\n * Find the point a given percentage of the way around a circle.\n *\n * @param circleCenter - Center of the circle.\n * @param radius - Radius of the circle.\n * @param percentageAroundCircle - Position around the circle, `0`–`100`. 0 is at 3 o'clock; 25 is at 6 o'clock.\n * @returns The `{ x, y }` point on the circle at that percentage.\n * @example\n * findPointAroundCircle({ x: 0, y: 0 }, 10, 25); // => { x: 0, y: 10 }\n */\nfunction findPointAroundCircle(circleCenter: Point, radius: number, percentageAroundCircle: number): Point {\n    let totalCircleRadians = Math.PI * 2;\n    let percent = percentageAroundCircle / 100;\n    const x = circleCenter.x + radius * Math.cos(totalCircleRadians * percent);\n    const y = circleCenter.y + radius * Math.sin(totalCircleRadians * percent);\n    return { x, y };\n}","jsSource":"/**\n * Find the point a given percentage of the way around a circle.\n *\n * @param circleCenter - Center of the circle.\n * @param radius - Radius of the circle.\n * @param percentageAroundCircle - Position around the circle, `0`–`100`. 0 is at 3 o'clock; 25 is at 6 o'clock.\n * @returns The `{ x, y }` point on the circle at that percentage.\n * @example\n * findPointAroundCircle({ x: 0, y: 0 }, 10, 25); // => { x: 0, y: 10 }\n */\nfunction findPointAroundCircle(circleCenter, radius, percentageAroundCircle) {\n    let totalCircleRadians = Math.PI * 2;\n    let percent = percentageAroundCircle / 100;\n    const x = circleCenter.x + radius * Math.cos(totalCircleRadians * percent);\n    const y = circleCenter.y + radius * Math.sin(totalCircleRadians * percent);\n    return { x, y };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"gameOfLifeStep","module":"GameOfLife","tsSource":"/**\n * Compute the next generation of Conway's Game of Life.\n *\n * Applies the classic rules to every cell — a live cell survives with 2 or 3 live\n * neighbours, a dead cell is born with exactly 3 — using a toroidal (wrap-around)\n * grid so the edges connect. Returns a new grid rather than mutating the input.\n *\n * @param grid - The current generation as a flat row-major array of `0`/`1` (length `cols * rows`).\n * @param cols - Number of columns.\n * @param rows - Number of rows.\n * @returns A new `Uint8Array` holding the next generation.\n * @example\n * grid = gameOfLifeStep(grid, cols, rows); // advance one tick\n */\nfunction gameOfLifeStep(grid: ArrayLike<number>, cols: number, rows: number): Uint8Array {\n    let next = new Uint8Array(cols * rows);\n    for (let y = 0; y < rows; y++) {\n        for (let x = 0; x < cols; x++) {\n            let i = y * cols + x;\n            let n = countNeighbors(grid, x, y, cols, rows);\n            if (grid[i] && (n === 2 || n === 3))\n                next[i] = 1;\n            else if (!grid[i] && n === 3)\n                next[i] = 1;\n        }\n    }\n    return next;\n}","jsSource":"/**\n * Compute the next generation of Conway's Game of Life.\n *\n * Applies the classic rules to every cell — a live cell survives with 2 or 3 live\n * neighbours, a dead cell is born with exactly 3 — using a toroidal (wrap-around)\n * grid so the edges connect. Returns a new grid rather than mutating the input.\n *\n * @param grid - The current generation as a flat row-major array of `0`/`1` (length `cols * rows`).\n * @param cols - Number of columns.\n * @param rows - Number of rows.\n * @returns A new `Uint8Array` holding the next generation.\n * @example\n * grid = gameOfLifeStep(grid, cols, rows); // advance one tick\n */\nfunction gameOfLifeStep(grid, cols, rows) {\n    let next = new Uint8Array(cols * rows);\n    for (let y = 0; y < rows; y++) {\n        for (let x = 0; x < cols; x++) {\n            let i = y * cols + x;\n            let n = countNeighbors(grid, x, y, cols, rows);\n            if (grid[i] && (n === 2 || n === 3))\n                next[i] = 1;\n            else if (!grid[i] && n === 3)\n                next[i] = 1;\n        }\n    }\n    return next;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"getPointOnLine","module":"GetPointOnLine","tsSource":"/**\n * Find a point partway along the straight line between two points (lerp in 2D).\n *\n * @param p1 - Start point, returned when `t` is 0.\n * @param p2 - End point, returned when `t` is 1.\n * @param t - Fraction along the line; `0.5` is the midpoint.\n * @returns The interpolated `{ x, y }` point.\n * @example\n * getPointOnLine({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction getPointOnLine(p1: Point, p2: Point, t: number): Point {\n    const lerp = (a: number, b: number, t: number) => a + (b - a) * t;\n    return { x: lerp(p1.x, p2.x, t), y: lerp(p1.y, p2.y, t) };\n}","jsSource":"/**\n * Find a point partway along the straight line between two points (lerp in 2D).\n *\n * @param p1 - Start point, returned when `t` is 0.\n * @param p2 - End point, returned when `t` is 1.\n * @param t - Fraction along the line; `0.5` is the midpoint.\n * @returns The interpolated `{ x, y }` point.\n * @example\n * getPointOnLine({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction getPointOnLine(p1, p2, t) {\n    const lerp = (a, b, t) => a + (b - a) * t;\n    return { x: lerp(p1.x, p2.x, t), y: lerp(p1.y, p2.y, t) };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"getRandomColors","module":"GetRandomColors","tsSource":"/**\n * Pick a random HSL color, optionally biased toward a named hue family.\n *\n * Returns hue/saturation/lightness components (not a CSS string) so the caller can\n * format them. Picking the hue from a restricted range keeps random colors within\n * a recognizable family (e.g. all blues).\n *\n * @param str - A color family to stay within: `\"red\"`, `\"orange\"`, `\"yellow\"`,\n *   `\"green\"`, or `\"blue\"`. Anything else (default `\"all\"`) spans the full wheel.\n * @returns `{ H, S, L }` — hue (0–360), saturation (50–100), lightness (25–75).\n * @example\n * const { H, S, L } = getRandomColors(\"blue\");\n * const css = `hsl(${H}, ${S}%, ${L}%)`;\n */\nfunction getRandomColors(str: string = \"all\"): {\n    H: number;\n    S: number;\n    L: number;\n} {\n    let range = [0, 360];\n    if (str.includes(\"blue\")) {\n        range = [200, 250];\n    }\n    else if (str.includes(\"green\")) {\n        range = [80, 150];\n    }\n    else if (str.includes(\"orange\")) {\n        range = [40, 80];\n    }\n    else if (str.includes(\"red\")) {\n        range = [0, 40];\n    }\n    else if (str.includes(\"yellow\")) {\n        range = [51, 60];\n    }\n    else {\n        range = [0, 360];\n    }\n    let H = randomIntegerBetween(range[0], range[1]);\n    let S = randomIntegerBetween(50, 100);\n    let L = randomIntegerBetween(25, 75);\n    return { H, S, L };\n}","jsSource":"/**\n * Pick a random HSL color, optionally biased toward a named hue family.\n *\n * Returns hue/saturation/lightness components (not a CSS string) so the caller can\n * format them. Picking the hue from a restricted range keeps random colors within\n * a recognizable family (e.g. all blues).\n *\n * @param str - A color family to stay within: `\"red\"`, `\"orange\"`, `\"yellow\"`,\n *   `\"green\"`, or `\"blue\"`. Anything else (default `\"all\"`) spans the full wheel.\n * @returns `{ H, S, L }` — hue (0–360), saturation (50–100), lightness (25–75).\n * @example\n * const { H, S, L } = getRandomColors(\"blue\");\n * const css = `hsl(${H}, ${S}%, ${L}%)`;\n */\nfunction getRandomColors(str = \"all\") {\n    let range = [0, 360];\n    if (str.includes(\"blue\")) {\n        range = [200, 250];\n    }\n    else if (str.includes(\"green\")) {\n        range = [80, 150];\n    }\n    else if (str.includes(\"orange\")) {\n        range = [40, 80];\n    }\n    else if (str.includes(\"red\")) {\n        range = [0, 40];\n    }\n    else if (str.includes(\"yellow\")) {\n        range = [51, 60];\n    }\n    else {\n        range = [0, 360];\n    }\n    let H = randomIntegerBetween(range[0], range[1]);\n    let S = randomIntegerBetween(50, 100);\n    let L = randomIntegerBetween(25, 75);\n    return { H, S, L };\n}","dependencyKeys":["randomIntegerBetween"],"interfaceNames":[]},
  {"key":"getRotation","module":"GetRotation","tsSource":"/**\n * Get the angle (in radians) pointing from one point toward another.\n *\n * Handy for rotating a sprite so it \"faces\" a target. Uses `Math.atan2`, so the\n * result is in the range `-π`…`π`, measured clockwise from the positive x-axis.\n *\n * @param currentPoint - The point doing the looking.\n * @param destinationPoint - The point being looked at.\n * @returns The heading angle in radians.\n * @example\n * getRotation({ x: 0, y: 0 }, { x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction getRotation(currentPoint: Point, destinationPoint: Point): number {\n    return Math.atan2(destinationPoint.y - currentPoint.y, destinationPoint.x - currentPoint.x);\n}","jsSource":"/**\n * Get the angle (in radians) pointing from one point toward another.\n *\n * Handy for rotating a sprite so it \"faces\" a target. Uses `Math.atan2`, so the\n * result is in the range `-π`…`π`, measured clockwise from the positive x-axis.\n *\n * @param currentPoint - The point doing the looking.\n * @param destinationPoint - The point being looked at.\n * @returns The heading angle in radians.\n * @example\n * getRotation({ x: 0, y: 0 }, { x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction getRotation(currentPoint, destinationPoint) {\n    return Math.atan2(destinationPoint.y - currentPoint.y, destinationPoint.x - currentPoint.x);\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"getTriangleData","module":"GetTriangleData","tsSource":"/**\n * Get the horizontal/vertical deltas and distance between two points.\n *\n * @param p1 - First point.\n * @param p2 - Second point.\n * @returns `{ dx, dy, distance }` — the run, the rise, and the hypotenuse.\n * @example\n * getTriangleData({ x: 0, y: 0 }, { x: 3, y: 4 }); // => { dx: 3, dy: 4, distance: 5 }\n */\nfunction getTriangleData(p1: Point, p2: Point): {\n    dx: number;\n    dy: number;\n    distance: number;\n} {\n    let dx = p2.x - p1.x, dy = p2.y - p1.y;\n    return { dx, dy, distance: Math.sqrt(dx * dx + dy * dy) };\n}","jsSource":"/**\n * Get the horizontal/vertical deltas and distance between two points.\n *\n * @param p1 - First point.\n * @param p2 - Second point.\n * @returns `{ dx, dy, distance }` — the run, the rise, and the hypotenuse.\n * @example\n * getTriangleData({ x: 0, y: 0 }, { x: 3, y: 4 }); // => { dx: 3, dy: 4, distance: 5 }\n */\nfunction getTriangleData(p1, p2) {\n    let dx = p2.x - p1.x, dy = p2.y - p1.y;\n    return { dx, dy, distance: Math.sqrt(dx * dx + dy * dy) };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"grStep","module":"GRStep","tsSource":"/**\n * Advance an orbiting body one frame under gravity with a general-relativity correction.\n *\n * Like Newtonian gravity but with an extra `1 + grStrength/r²` term that strengthens\n * the pull at close range — this is what makes orbits *precess* (the famous anomaly\n * in Mercury's orbit). **Mutates `orbiter`** in place.\n *\n * @param orbiter - The orbiting body (its `x`, `y`, `vx`, `vy` are mutated).\n * @param sun - The central mass being orbited.\n * @param grStrength - Strength of the relativistic correction; `0` reduces to Newtonian gravity.\n * @returns Nothing — `orbiter` is updated in place.\n * @example\n * grStep(planet, sun, 800); // call once per frame to see the orbit precess\n */\nfunction grStep(orbiter: {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n}, sun: {\n    x: number;\n    y: number;\n    mass: number;\n}, grStrength: number): void {\n    let dx = sun.x - orbiter.x, dy = sun.y - orbiter.y;\n    let r = Math.sqrt(dx * dx + dy * dy);\n    let force = (0.5 * sun.mass / (r * r)) * (1 + grStrength / (r * r));\n    orbiter.vx += (dx / r) * force;\n    orbiter.vy += (dy / r) * force;\n    orbiter.x += orbiter.vx;\n    orbiter.y += orbiter.vy;\n}","jsSource":"/**\n * Advance an orbiting body one frame under gravity with a general-relativity correction.\n *\n * Like Newtonian gravity but with an extra `1 + grStrength/r²` term that strengthens\n * the pull at close range — this is what makes orbits *precess* (the famous anomaly\n * in Mercury's orbit). **Mutates `orbiter`** in place.\n *\n * @param orbiter - The orbiting body (its `x`, `y`, `vx`, `vy` are mutated).\n * @param sun - The central mass being orbited.\n * @param grStrength - Strength of the relativistic correction; `0` reduces to Newtonian gravity.\n * @returns Nothing — `orbiter` is updated in place.\n * @example\n * grStep(planet, sun, 800); // call once per frame to see the orbit precess\n */\nfunction grStep(orbiter, sun, grStrength) {\n    let dx = sun.x - orbiter.x, dy = sun.y - orbiter.y;\n    let r = Math.sqrt(dx * dx + dy * dy);\n    let force = (0.5 * sun.mass / (r * r)) * (1 + grStrength / (r * r));\n    orbiter.vx += (dx / r) * force;\n    orbiter.vy += (dy / r) * force;\n    orbiter.x += orbiter.vx;\n    orbiter.y += orbiter.vy;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"gravitationalStep","module":"OrbitalMotion","tsSource":"/**\n * Advance an orbiting body one frame under Newtonian gravity (inverse-square attraction).\n *\n * Each step pulls the orbiter toward the central body with a force proportional to\n * `mass / r²`, then integrates velocity and position — enough to produce stable\n * orbits. **Mutates `orbiter`** in place.\n *\n * @param orbiter - The orbiting body (its `x`, `y`, `vx`, `vy` are mutated).\n * @param body - The central mass being orbited.\n * @returns Nothing — `orbiter` is updated in place.\n * @example\n * gravitationalStep(planet, sun); // call once per frame\n */\nfunction gravitationalStep(orbiter: {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n}, body: {\n    x: number;\n    y: number;\n    mass: number;\n}): void {\n    let dx = body.x - orbiter.x, dy = body.y - orbiter.y;\n    let dist = Math.sqrt(dx * dx + dy * dy);\n    let force = (0.5 * body.mass) / (dist * dist);\n    orbiter.vx += (dx / dist) * force;\n    orbiter.vy += (dy / dist) * force;\n    orbiter.x += orbiter.vx;\n    orbiter.y += orbiter.vy;\n}","jsSource":"/**\n * Advance an orbiting body one frame under Newtonian gravity (inverse-square attraction).\n *\n * Each step pulls the orbiter toward the central body with a force proportional to\n * `mass / r²`, then integrates velocity and position — enough to produce stable\n * orbits. **Mutates `orbiter`** in place.\n *\n * @param orbiter - The orbiting body (its `x`, `y`, `vx`, `vy` are mutated).\n * @param body - The central mass being orbited.\n * @returns Nothing — `orbiter` is updated in place.\n * @example\n * gravitationalStep(planet, sun); // call once per frame\n */\nfunction gravitationalStep(orbiter, body) {\n    let dx = body.x - orbiter.x, dy = body.y - orbiter.y;\n    let dist = Math.sqrt(dx * dx + dy * dy);\n    let force = (0.5 * body.mass) / (dist * dist);\n    orbiter.vx += (dx / dist) * force;\n    orbiter.vy += (dy / dist) * force;\n    orbiter.x += orbiter.vx;\n    orbiter.y += orbiter.vy;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"hslToRgb","module":"Color","tsSource":"/**\n * Convert an HSL color back to RGB.\n *\n * Inverse of {@link rgbToHsl}. Hue wraps, so values outside 0–360 are accepted.\n *\n * @param hsl - `h` in degrees (wraps), `s`/`l` 0–100.\n * @returns Channels rounded to integers 0–255.\n * @example\n * hslToRgb({ h: 120, s: 100, l: 50 }); // => { r: 0, g: 255, b: 0 }\n */\nfunction hslToRgb({ h, s, l }: HSL): RGB {\n    const sn = s / 100;\n    const ln = l / 100;\n    const c = (1 - Math.abs(2 * ln - 1)) * sn;\n    const hp = ((((h % 360) + 360) % 360)) / 60;\n    const x = c * (1 - Math.abs((hp % 2) - 1));\n    let r1 = 0;\n    let g1 = 0;\n    let b1 = 0;\n    if (hp < 1)\n        [r1, g1, b1] = [c, x, 0];\n    else if (hp < 2)\n        [r1, g1, b1] = [x, c, 0];\n    else if (hp < 3)\n        [r1, g1, b1] = [0, c, x];\n    else if (hp < 4)\n        [r1, g1, b1] = [0, x, c];\n    else if (hp < 5)\n        [r1, g1, b1] = [x, 0, c];\n    else\n        [r1, g1, b1] = [c, 0, x];\n    const m = ln - c / 2;\n    return {\n        r: Math.round((r1 + m) * 255),\n        g: Math.round((g1 + m) * 255),\n        b: Math.round((b1 + m) * 255),\n    };\n}","jsSource":"/**\n * Convert an HSL color back to RGB.\n *\n * Inverse of {@link rgbToHsl}. Hue wraps, so values outside 0–360 are accepted.\n *\n * @param hsl - `h` in degrees (wraps), `s`/`l` 0–100.\n * @returns Channels rounded to integers 0–255.\n * @example\n * hslToRgb({ h: 120, s: 100, l: 50 }); // => { r: 0, g: 255, b: 0 }\n */\nfunction hslToRgb({ h, s, l }) {\n    const sn = s / 100;\n    const ln = l / 100;\n    const c = (1 - Math.abs(2 * ln - 1)) * sn;\n    const hp = ((((h % 360) + 360) % 360)) / 60;\n    const x = c * (1 - Math.abs((hp % 2) - 1));\n    let r1 = 0;\n    let g1 = 0;\n    let b1 = 0;\n    if (hp < 1)\n        [r1, g1, b1] = [c, x, 0];\n    else if (hp < 2)\n        [r1, g1, b1] = [x, c, 0];\n    else if (hp < 3)\n        [r1, g1, b1] = [0, c, x];\n    else if (hp < 4)\n        [r1, g1, b1] = [0, x, c];\n    else if (hp < 5)\n        [r1, g1, b1] = [x, 0, c];\n    else\n        [r1, g1, b1] = [c, 0, x];\n    const m = ln - c / 2;\n    return {\n        r: Math.round((r1 + m) * 255),\n        g: Math.round((g1 + m) * 255),\n        b: Math.round((b1 + m) * 255),\n    };\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"inverseLerp","module":"InverseLerp","tsSource":"/**\n * The inverse of {@link lerp}: find the fraction `t` that produces `value` between `a` and `b`.\n *\n * Answers \"how far along am I?\" — e.g. turning a current value into a 0–1 progress.\n *\n * @param a - Range start (where the result is 0).\n * @param b - Range end (where the result is 1).\n * @param value - The value to locate within the range.\n * @returns The fraction `t` such that `lerp(a, b, t) === value`. Returns `0` if `a === b`.\n * @example\n * inverseLerp(0, 100, 25); // => 0.25\n * inverseLerp(20, 40, 30); // => 0.5\n */\nfunction inverseLerp(a: number, b: number, value: number): number {\n    return a === b ? 0 : (value - a) / (b - a);\n}","jsSource":"/**\n * The inverse of {@link lerp}: find the fraction `t` that produces `value` between `a` and `b`.\n *\n * Answers \"how far along am I?\" — e.g. turning a current value into a 0–1 progress.\n *\n * @param a - Range start (where the result is 0).\n * @param b - Range end (where the result is 1).\n * @param value - The value to locate within the range.\n * @returns The fraction `t` such that `lerp(a, b, t) === value`. Returns `0` if `a === b`.\n * @example\n * inverseLerp(0, 100, 25); // => 0.25\n * inverseLerp(20, 40, 30); // => 0.5\n */\nfunction inverseLerp(a, b, value) {\n    return a === b ? 0 : (value - a) / (b - a);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"lensDeflection","module":"LensDeflection","tsSource":"/**\n * Approximate how far a light ray is bent as it passes a massive object (gravitational lensing).\n *\n * A simplified model of Einstein's light-bending: the deflection grows with the\n * lens `mass` and falls off with distance from it (the impact parameter `b`),\n * which is why objects behind a galaxy appear warped into arcs.\n *\n * @param x - The ray's current horizontal position.\n * @param y0 - The ray's undeflected vertical position.\n * @param lx - Lens (mass) center x.\n * @param ly - Lens (mass) center y.\n * @param mass - Mass of the lensing object; larger bends light more.\n * @returns The vertical deflection to apply at this `x` (0 when far from the lens).\n * @example\n * const dy = lensDeflection(rayX, rayY, lensX, lensY, 2000);\n */\nfunction lensDeflection(x: number, y0: number, lx: number, ly: number, mass: number): number {\n    let b = Math.abs(y0 - ly);\n    if (b === 0 || x <= lx - 100 || x >= lx + 100)\n        return 0;\n    let r = Math.sqrt((x - lx) * (x - lx) + b * b);\n    return r > 5 ? mass / (r * b) * 0.5 : 0;\n}","jsSource":"/**\n * Approximate how far a light ray is bent as it passes a massive object (gravitational lensing).\n *\n * A simplified model of Einstein's light-bending: the deflection grows with the\n * lens `mass` and falls off with distance from it (the impact parameter `b`),\n * which is why objects behind a galaxy appear warped into arcs.\n *\n * @param x - The ray's current horizontal position.\n * @param y0 - The ray's undeflected vertical position.\n * @param lx - Lens (mass) center x.\n * @param ly - Lens (mass) center y.\n * @param mass - Mass of the lensing object; larger bends light more.\n * @returns The vertical deflection to apply at this `x` (0 when far from the lens).\n * @example\n * const dy = lensDeflection(rayX, rayY, lensX, lensY, 2000);\n */\nfunction lensDeflection(x, y0, lx, ly, mass) {\n    let b = Math.abs(y0 - ly);\n    if (b === 0 || x <= lx - 100 || x >= lx + 100)\n        return 0;\n    let r = Math.sqrt((x - lx) * (x - lx) + b * b);\n    return r > 5 ? mass / (r * b) * 0.5 : 0;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"lerp","module":"Lerp","tsSource":"/**\n * Linearly interpolate between two numbers.\n *\n * @param a - Start value, returned when `t` is 0.\n * @param b - End value, returned when `t` is 1.\n * @param t - Interpolation factor. 0–1 stays within the range; values outside extrapolate.\n * @returns The interpolated value `a + (b - a) * t`.\n * @example\n * lerp(0, 100, 0.5); // => 50\n */\nfunction lerp(a: number, b: number, t: number): number {\n    return a + (b - a) * t;\n}","jsSource":"/**\n * Linearly interpolate between two numbers.\n *\n * @param a - Start value, returned when `t` is 0.\n * @param b - End value, returned when `t` is 1.\n * @param t - Interpolation factor. 0–1 stays within the range; values outside extrapolate.\n * @returns The interpolated value `a + (b - a) * t`.\n * @example\n * lerp(0, 100, 0.5); // => 50\n */\nfunction lerp(a, b, t) {\n    return a + (b - a) * t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"lerpAngle","module":"AngleInterpolation","tsSource":"/**\n * Interpolate between two angles along the shortest arc (no spinning the long way around).\n *\n * The angle-aware cousin of {@link lerp} — use it to smoothly rotate something\n * toward a target heading without sudden flips at the 0/360° boundary.\n *\n * @param a - Start angle in radians, returned when `t` is 0.\n * @param b - Target angle in radians, returned when `t` is 1.\n * @param t - Interpolation factor, 0–1.\n * @returns The interpolated angle in radians.\n * @example\n * lerpAngle(0, Math.PI / 2, 0.5); // => Math.PI / 4\n */\nfunction lerpAngle(a: number, b: number, t: number): number {\n    return a + shortestAngleBetween(a, b) * t;\n}","jsSource":"/**\n * Interpolate between two angles along the shortest arc (no spinning the long way around).\n *\n * The angle-aware cousin of {@link lerp} — use it to smoothly rotate something\n * toward a target heading without sudden flips at the 0/360° boundary.\n *\n * @param a - Start angle in radians, returned when `t` is 0.\n * @param b - Target angle in radians, returned when `t` is 1.\n * @param t - Interpolation factor, 0–1.\n * @returns The interpolated angle in radians.\n * @example\n * lerpAngle(0, Math.PI / 2, 0.5); // => Math.PI / 4\n */\nfunction lerpAngle(a, b, t) {\n    return a + shortestAngleBetween(a, b) * t;\n}","dependencyKeys":["shortestAngleBetween"],"interfaceNames":[]},
  {"key":"lerpColor","module":"Color","tsSource":"/**\n * Interpolate two colors in RGB space — a straight per-channel {@link lerp}.\n *\n * This is the obvious approach, and it has a famous pitfall: blending two vivid\n * complementary colors (e.g. blue↔yellow) passes through a muddy gray at the\n * midpoint, because the midpoint of opposite channels is ~middle-gray. Compare\n * with {@link lerpColorHsl}.\n *\n * @param a - Start color, returned at `t = 0`.\n * @param b - End color, returned at `t = 1`.\n * @param t - Interpolation factor, 0–1.\n * @returns The blended color (channels may be fractional).\n * @example\n * lerpColor({ r: 0, g: 0, b: 255 }, { r: 255, g: 255, b: 0 }, 0.5); // => { r: 127.5, g: 127.5, b: 127.5 } (gray!)\n */\nfunction lerpColor(a: RGB, b: RGB, t: number): RGB {\n    return {\n        r: lerp(a.r, b.r, t),\n        g: lerp(a.g, b.g, t),\n        b: lerp(a.b, b.b, t),\n    };\n}","jsSource":"/**\n * Interpolate two colors in RGB space — a straight per-channel {@link lerp}.\n *\n * This is the obvious approach, and it has a famous pitfall: blending two vivid\n * complementary colors (e.g. blue↔yellow) passes through a muddy gray at the\n * midpoint, because the midpoint of opposite channels is ~middle-gray. Compare\n * with {@link lerpColorHsl}.\n *\n * @param a - Start color, returned at `t = 0`.\n * @param b - End color, returned at `t = 1`.\n * @param t - Interpolation factor, 0–1.\n * @returns The blended color (channels may be fractional).\n * @example\n * lerpColor({ r: 0, g: 0, b: 255 }, { r: 255, g: 255, b: 0 }, 0.5); // => { r: 127.5, g: 127.5, b: 127.5 } (gray!)\n */\nfunction lerpColor(a, b, t) {\n    return {\n        r: lerp(a.r, b.r, t),\n        g: lerp(a.g, b.g, t),\n        b: lerp(a.b, b.b, t),\n    };\n}","dependencyKeys":["lerp"],"interfaceNames":[]},
  {"key":"lerpColorHsl","module":"Color","tsSource":"/**\n * Interpolate two colors through HSL space, rotating hue the short way around.\n *\n * Because hue, saturation, and lightness move independently, the blend stays\n * vivid instead of collapsing to gray — the colors travel *around* the wheel\n * rather than *through* its desaturated center. This is the perceptually nicer\n * default for gradients. Contrast with {@link lerpColor}.\n *\n * @param a - Start color, returned at `t = 0`.\n * @param b - End color, returned at `t = 1`.\n * @param t - Interpolation factor, 0–1.\n * @returns The blended color, channels rounded to 0–255.\n * @example\n * lerpColorHsl({ r: 0, g: 0, b: 255 }, { r: 255, g: 255, b: 0 }, 0.5); // a vivid mid-hue, not gray\n */\nfunction lerpColorHsl(a: RGB, b: RGB, t: number): RGB {\n    const ha = rgbToHsl(a);\n    const hb = rgbToHsl(b);\n    // Shortest signed hue delta in (-180, 180], so we never spin the long way.\n    const dh = ((((hb.h - ha.h) % 360) + 540) % 360) - 180;\n    return hslToRgb({\n        h: ha.h + dh * t,\n        s: lerp(ha.s, hb.s, t),\n        l: lerp(ha.l, hb.l, t),\n    });\n}","jsSource":"/**\n * Interpolate two colors through HSL space, rotating hue the short way around.\n *\n * Because hue, saturation, and lightness move independently, the blend stays\n * vivid instead of collapsing to gray — the colors travel *around* the wheel\n * rather than *through* its desaturated center. This is the perceptually nicer\n * default for gradients. Contrast with {@link lerpColor}.\n *\n * @param a - Start color, returned at `t = 0`.\n * @param b - End color, returned at `t = 1`.\n * @param t - Interpolation factor, 0–1.\n * @returns The blended color, channels rounded to 0–255.\n * @example\n * lerpColorHsl({ r: 0, g: 0, b: 255 }, { r: 255, g: 255, b: 0 }, 0.5); // a vivid mid-hue, not gray\n */\nfunction lerpColorHsl(a, b, t) {\n    const ha = rgbToHsl(a);\n    const hb = rgbToHsl(b);\n    // Shortest signed hue delta in (-180, 180], so we never spin the long way.\n    const dh = ((((hb.h - ha.h) % 360) + 540) % 360) - 180;\n    return hslToRgb({\n        h: ha.h + dh * t,\n        s: lerp(ha.s, hb.s, t),\n        l: lerp(ha.l, hb.l, t),\n    });\n}","dependencyKeys":["hslToRgb","lerp","rgbToHsl"],"interfaceNames":[]},
  {"key":"lineCircle","module":"CollisionObjectAPI/LineCircle","tsSource":"/**\n * Test whether a line segment intersects a circle (object-argument form of `lineToCircle`).\n *\n * Projects the circle's center onto the segment, clamps that projection to the\n * segment bounds, and checks whether the closest point lies within the radius.\n * This also handles degenerate zero-length segments safely.\n *\n * @param line - The line segment (`startPoint`, `endPoint`).\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the segment touches or crosses the circle.\n * @remarks\n * This object-argument form is the **recommended** one; {@link lineToCircle} is the flat,\n * seven-number equivalent running the identical closest-point-on-segment test.\n * @example\n * lineCircle({ startPoint: { x: 0, y: 0 }, endPoint: { x: 10, y: 0 } }, { x: 5, y: 0, radius: 2 }); // => true\n */\nfunction lineCircle(line: Line, circle: Circle): boolean {\n    const dx = line.endPoint.x - line.startPoint.x;\n    const dy = line.endPoint.y - line.startPoint.y;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = circle.x - line.startPoint.x;\n        const pointDy = circle.y - line.startPoint.y;\n        return pointDx * pointDx + pointDy * pointDy <= circle.radius * circle.radius;\n    }\n    const projection = ((circle.x - line.startPoint.x) * dx + (circle.y - line.startPoint.y) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const closestX = line.startPoint.x + t * dx;\n    const closestY = line.startPoint.y + t * dy;\n    const distanceX = circle.x - closestX;\n    const distanceY = circle.y - closestY;\n    return distanceX * distanceX + distanceY * distanceY <= circle.radius * circle.radius;\n}","jsSource":"/**\n * Test whether a line segment intersects a circle (object-argument form of `lineToCircle`).\n *\n * Projects the circle's center onto the segment, clamps that projection to the\n * segment bounds, and checks whether the closest point lies within the radius.\n * This also handles degenerate zero-length segments safely.\n *\n * @param line - The line segment (`startPoint`, `endPoint`).\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the segment touches or crosses the circle.\n * @remarks\n * This object-argument form is the **recommended** one; {@link lineToCircle} is the flat,\n * seven-number equivalent running the identical closest-point-on-segment test.\n * @example\n * lineCircle({ startPoint: { x: 0, y: 0 }, endPoint: { x: 10, y: 0 } }, { x: 5, y: 0, radius: 2 }); // => true\n */\nfunction lineCircle(line, circle) {\n    const dx = line.endPoint.x - line.startPoint.x;\n    const dy = line.endPoint.y - line.startPoint.y;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = circle.x - line.startPoint.x;\n        const pointDy = circle.y - line.startPoint.y;\n        return pointDx * pointDx + pointDy * pointDy <= circle.radius * circle.radius;\n    }\n    const projection = ((circle.x - line.startPoint.x) * dx + (circle.y - line.startPoint.y) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const closestX = line.startPoint.x + t * dx;\n    const closestY = line.startPoint.y + t * dy;\n    const distanceX = circle.x - closestX;\n    const distanceY = circle.y - closestY;\n    return distanceX * distanceX + distanceY * distanceY <= circle.radius * circle.radius;\n}","dependencyKeys":[],"interfaceNames":["Circle","Line"]},
  {"key":"lineLength","module":"LineLength","tsSource":"/**\n * Get the length of a line segment.\n *\n * @param line - A line with `startPoint` and `endPoint`.\n * @returns The straight-line distance between the two endpoints.\n * @remarks\n * This is the segment-shaped spelling of Euclidean distance — the same measurement as\n * {@link distance} on two loose points, computed with `Math.hypot`.\n * @example\n * lineLength({ startPoint: { x: 0, y: 0 }, endPoint: { x: 3, y: 4 } }); // => 5\n */\nfunction lineLength(line: Line): number {\n    const { startPoint, endPoint } = line;\n    return Math.hypot(startPoint.x - endPoint.x, startPoint.y - endPoint.y);\n}","jsSource":"/**\n * Get the length of a line segment.\n *\n * @param line - A line with `startPoint` and `endPoint`.\n * @returns The straight-line distance between the two endpoints.\n * @remarks\n * This is the segment-shaped spelling of Euclidean distance — the same measurement as\n * {@link distance} on two loose points, computed with `Math.hypot`.\n * @example\n * lineLength({ startPoint: { x: 0, y: 0 }, endPoint: { x: 3, y: 4 } }); // => 5\n */\nfunction lineLength(line) {\n    const { startPoint, endPoint } = line;\n    return Math.hypot(startPoint.x - endPoint.x, startPoint.y - endPoint.y);\n}","dependencyKeys":[],"interfaceNames":["Line"]},
  {"key":"lineLine","module":"CollisionObjectAPI/LineLine","tsSource":"/**\n * Test whether two line segments cross, and report where (object-argument form).\n *\n * Unlike the boolean `lineToLine`, this returns the intersection coordinates when\n * the segments meet — useful for drawing a marker at the crossing.\n *\n * Parallel or collinear segments return `{ hit: false }` in this implementation.\n *\n * @param line1 - First segment (`startPoint`, `endPoint`).\n * @param line2 - Second segment (`startPoint`, `endPoint`).\n * @returns `{ hit: true, intersectionX, intersectionY }` if they cross, else `{ hit: false }`.\n * @remarks\n * Use this when you need the intersection *point*. If a plain yes/no is enough, the flat\n * {@link lineToLine} returns a `boolean`.\n * @example\n * const r = lineLine(a, b);\n * if (r.hit) drawDot(r.intersectionX, r.intersectionY);\n */\nfunction lineLine(line1: Line, line2: Line) {\n    const denominator = (line2.endPoint.y - line2.startPoint.y) * (line1.endPoint.x - line1.startPoint.x) -\n        (line2.endPoint.x - line2.startPoint.x) * (line1.endPoint.y - line1.startPoint.y);\n    if (Math.abs(denominator) < 0.0001) {\n        return { hit: false } as const;\n    }\n    const uA = ((line2.endPoint.x - line2.startPoint.x) * (line1.startPoint.y - line2.startPoint.y) -\n        (line2.endPoint.y - line2.startPoint.y) * (line1.startPoint.x - line2.startPoint.x)) /\n        denominator;\n    const uB = ((line1.endPoint.x - line1.startPoint.x) * (line1.startPoint.y - line2.startPoint.y) -\n        (line1.endPoint.y - line1.startPoint.y) * (line1.startPoint.x - line2.startPoint.x)) /\n        denominator;\n    if (uA >= 0 && uA <= 1 && uB >= 0 && uB <= 1) {\n        const intersectionX = line1.startPoint.x + uA * (line1.endPoint.x - line1.startPoint.x);\n        const intersectionY = line1.startPoint.y + uA * (line1.endPoint.y - line1.startPoint.y);\n        return {\n            hit: true,\n            intersectionX,\n            intersectionY,\n        };\n    }\n    return { hit: false };\n}","jsSource":"/**\n * Test whether two line segments cross, and report where (object-argument form).\n *\n * Unlike the boolean `lineToLine`, this returns the intersection coordinates when\n * the segments meet — useful for drawing a marker at the crossing.\n *\n * Parallel or collinear segments return `{ hit: false }` in this implementation.\n *\n * @param line1 - First segment (`startPoint`, `endPoint`).\n * @param line2 - Second segment (`startPoint`, `endPoint`).\n * @returns `{ hit: true, intersectionX, intersectionY }` if they cross, else `{ hit: false }`.\n * @remarks\n * Use this when you need the intersection *point*. If a plain yes/no is enough, the flat\n * {@link lineToLine} returns a `boolean`.\n * @example\n * const r = lineLine(a, b);\n * if (r.hit) drawDot(r.intersectionX, r.intersectionY);\n */\nfunction lineLine(line1, line2) {\n    const denominator = (line2.endPoint.y - line2.startPoint.y) * (line1.endPoint.x - line1.startPoint.x) -\n        (line2.endPoint.x - line2.startPoint.x) * (line1.endPoint.y - line1.startPoint.y);\n    if (Math.abs(denominator) < 0.0001) {\n        return { hit: false };\n    }\n    const uA = ((line2.endPoint.x - line2.startPoint.x) * (line1.startPoint.y - line2.startPoint.y) -\n        (line2.endPoint.y - line2.startPoint.y) * (line1.startPoint.x - line2.startPoint.x)) /\n        denominator;\n    const uB = ((line1.endPoint.x - line1.startPoint.x) * (line1.startPoint.y - line2.startPoint.y) -\n        (line1.endPoint.y - line1.startPoint.y) * (line1.startPoint.x - line2.startPoint.x)) /\n        denominator;\n    if (uA >= 0 && uA <= 1 && uB >= 0 && uB <= 1) {\n        const intersectionX = line1.startPoint.x + uA * (line1.endPoint.x - line1.startPoint.x);\n        const intersectionY = line1.startPoint.y + uA * (line1.endPoint.y - line1.startPoint.y);\n        return {\n            hit: true,\n            intersectionX,\n            intersectionY,\n        };\n    }\n    return { hit: false };\n}","dependencyKeys":[],"interfaceNames":["Line"]},
  {"key":"linePoint","module":"CollisionObjectAPI/LinePoint","tsSource":"/**\n * Test whether a point lies on a line segment (object-argument form).\n *\n * Projects the point onto the segment, clamps that projection to the segment\n * bounds, and checks whether the nearest point lands within a built-in `0.1`\n * unit tolerance.\n *\n * @param line - The line segment (`startPoint`, `endPoint`).\n * @param point - The point to test.\n * @returns `true` if the point sits on the segment within the built-in `0.1` tolerance.\n * @remarks\n * The tolerance here is fixed at `0.1`. If you need to choose it, use the flat\n * {@link lineToPoint}, which takes an explicit `threshold` argument.\n * @example\n * linePoint({ startPoint: { x: 0, y: 0 }, endPoint: { x: 10, y: 0 } }, { x: 5, y: 0 }); // => true\n */\nfunction linePoint(line: Line, point: Point): boolean {\n    const threshold = 0.1;\n    const dx = line.endPoint.x - line.startPoint.x;\n    const dy = line.endPoint.y - line.startPoint.y;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = point.x - line.startPoint.x;\n        const pointDy = point.y - line.startPoint.y;\n        return pointDx * pointDx + pointDy * pointDy <= threshold * threshold;\n    }\n    const projection = ((point.x - line.startPoint.x) * dx + (point.y - line.startPoint.y) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const closestX = line.startPoint.x + t * dx;\n    const closestY = line.startPoint.y + t * dy;\n    const distanceX = point.x - closestX;\n    const distanceY = point.y - closestY;\n    return distanceX * distanceX + distanceY * distanceY <= threshold * threshold;\n}","jsSource":"/**\n * Test whether a point lies on a line segment (object-argument form).\n *\n * Projects the point onto the segment, clamps that projection to the segment\n * bounds, and checks whether the nearest point lands within a built-in `0.1`\n * unit tolerance.\n *\n * @param line - The line segment (`startPoint`, `endPoint`).\n * @param point - The point to test.\n * @returns `true` if the point sits on the segment within the built-in `0.1` tolerance.\n * @remarks\n * The tolerance here is fixed at `0.1`. If you need to choose it, use the flat\n * {@link lineToPoint}, which takes an explicit `threshold` argument.\n * @example\n * linePoint({ startPoint: { x: 0, y: 0 }, endPoint: { x: 10, y: 0 } }, { x: 5, y: 0 }); // => true\n */\nfunction linePoint(line, point) {\n    const threshold = 0.1;\n    const dx = line.endPoint.x - line.startPoint.x;\n    const dy = line.endPoint.y - line.startPoint.y;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = point.x - line.startPoint.x;\n        const pointDy = point.y - line.startPoint.y;\n        return pointDx * pointDx + pointDy * pointDy <= threshold * threshold;\n    }\n    const projection = ((point.x - line.startPoint.x) * dx + (point.y - line.startPoint.y) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const closestX = line.startPoint.x + t * dx;\n    const closestY = line.startPoint.y + t * dy;\n    const distanceX = point.x - closestX;\n    const distanceY = point.y - closestY;\n    return distanceX * distanceX + distanceY * distanceY <= threshold * threshold;\n}","dependencyKeys":[],"interfaceNames":["Line","Point"]},
  {"key":"lineToCircle","module":"LineToCircle","tsSource":"/**\n * Test whether a line segment intersects a circle.\n *\n * Finds the closest point on the segment to the circle's center and checks\n * whether it lies within the radius. This also handles degenerate zero-length\n * segments safely.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param cr - Circle radius.\n * @returns `true` if the segment touches or crosses the circle.\n * @remarks\n * Flat, seven-number form. For new code the **recommended** shape is the object-argument\n * {@link lineCircle} (`lineCircle({ startPoint, endPoint }, { x, y, radius })`); both run\n * the identical closest-point-on-segment test.\n * @example\n * lineToCircle(0, 0, 10, 0, 5, 0, 2); // => true\n */\nfunction lineToCircle(x1: number, y1: number, x2: number, y2: number, cx: number, cy: number, cr: number): boolean {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = cx - x1;\n        const pointDy = cy - y1;\n        return pointDx * pointDx + pointDy * pointDy <= cr * cr;\n    }\n    const projection = ((cx - x1) * dx + (cy - y1) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const px = x1 + t * dx;\n    const py = y1 + t * dy;\n    const ddx = cx - px;\n    const ddy = cy - py;\n    return ddx * ddx + ddy * ddy <= cr * cr;\n}","jsSource":"/**\n * Test whether a line segment intersects a circle.\n *\n * Finds the closest point on the segment to the circle's center and checks\n * whether it lies within the radius. This also handles degenerate zero-length\n * segments safely.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param cr - Circle radius.\n * @returns `true` if the segment touches or crosses the circle.\n * @remarks\n * Flat, seven-number form. For new code the **recommended** shape is the object-argument\n * {@link lineCircle} (`lineCircle({ startPoint, endPoint }, { x, y, radius })`); both run\n * the identical closest-point-on-segment test.\n * @example\n * lineToCircle(0, 0, 10, 0, 5, 0, 2); // => true\n */\nfunction lineToCircle(x1, y1, x2, y2, cx, cy, cr) {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = cx - x1;\n        const pointDy = cy - y1;\n        return pointDx * pointDx + pointDy * pointDy <= cr * cr;\n    }\n    const projection = ((cx - x1) * dx + (cy - y1) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const px = x1 + t * dx;\n    const py = y1 + t * dy;\n    const ddx = cx - px;\n    const ddy = cy - py;\n    return ddx * ddx + ddy * ddy <= cr * cr;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"lineToLine","module":"LineToLine","tsSource":"/**\n * Test whether two line segments cross.\n *\n * Parallel or collinear segments return `false` in this implementation.\n *\n * @param x1 - First segment start x.\n * @param y1 - First segment start y.\n * @param x2 - First segment end x.\n * @param y2 - First segment end y.\n * @param x3 - Second segment start x.\n * @param y3 - Second segment start y.\n * @param x4 - Second segment end x.\n * @param y4 - Second segment end y.\n * @returns `true` if the two segments intersect (parallel segments return `false`).\n * @remarks\n * Flat, eight-number form that answers only \"do they cross?\" If you also need *where*\n * they cross, use the object-argument {@link lineLine}, which returns\n * `{ hit, intersectionX, intersectionY }`.\n * @example\n * lineToLine(0, 0, 10, 10, 0, 10, 10, 0); // => true (an X)\n */\nfunction lineToLine(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number, x4: number, y4: number): boolean {\n    const denominator = (x1 - x2) * (y3 - y4) - (y1 - y2) * (x3 - x4);\n    if (Math.abs(denominator) < 0.0001)\n        return false;\n    const t = ((x1 - x3) * (y3 - y4) - (y1 - y3) * (x3 - x4)) / denominator;\n    const u = -((x1 - x2) * (y1 - y3) - (y1 - y2) * (x1 - x3)) / denominator;\n    return t >= 0 && t <= 1 && u >= 0 && u <= 1;\n}","jsSource":"/**\n * Test whether two line segments cross.\n *\n * Parallel or collinear segments return `false` in this implementation.\n *\n * @param x1 - First segment start x.\n * @param y1 - First segment start y.\n * @param x2 - First segment end x.\n * @param y2 - First segment end y.\n * @param x3 - Second segment start x.\n * @param y3 - Second segment start y.\n * @param x4 - Second segment end x.\n * @param y4 - Second segment end y.\n * @returns `true` if the two segments intersect (parallel segments return `false`).\n * @remarks\n * Flat, eight-number form that answers only \"do they cross?\" If you also need *where*\n * they cross, use the object-argument {@link lineLine}, which returns\n * `{ hit, intersectionX, intersectionY }`.\n * @example\n * lineToLine(0, 0, 10, 10, 0, 10, 10, 0); // => true (an X)\n */\nfunction lineToLine(x1, y1, x2, y2, x3, y3, x4, y4) {\n    const denominator = (x1 - x2) * (y3 - y4) - (y1 - y2) * (x3 - x4);\n    if (Math.abs(denominator) < 0.0001)\n        return false;\n    const t = ((x1 - x3) * (y3 - y4) - (y1 - y3) * (x3 - x4)) / denominator;\n    const u = -((x1 - x2) * (y1 - y3) - (y1 - y2) * (x1 - x3)) / denominator;\n    return t >= 0 && t <= 1 && u >= 0 && u <= 1;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"lineToPoint","module":"LineToPoint","tsSource":"/**\n * Test whether a point lies on a line segment, within a tolerance.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param px - Point x.\n * @param py - Point y.\n * @param threshold - How close (in units) the point must be to count as \"on the line\".\n * @returns `true` if the point is within `threshold` of the segment.\n * @remarks\n * This flat form lets you choose the `threshold`. The object-argument {@link linePoint}\n * (`linePoint({ startPoint, endPoint }, { x, y })`) is the same closest-point test with a\n * fixed `0.1` tolerance baked in — reach for it when the default is fine and you want the\n * tidier call site.\n * @example\n * lineToPoint(0, 0, 10, 0, 5, 0, 0.5); // => true\n */\nfunction lineToPoint(x1: number, y1: number, x2: number, y2: number, px: number, py: number, threshold: number): boolean {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const t = Math.max(0, Math.min(1, ((px - x1) * dx + (py - y1) * dy) / (dx * dx + dy * dy)));\n    const lx = x1 + t * dx;\n    const ly = y1 + t * dy;\n    const ddx = px - lx;\n    const ddy = py - ly;\n    return ddx * ddx + ddy * ddy <= threshold * threshold;\n}","jsSource":"/**\n * Test whether a point lies on a line segment, within a tolerance.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param px - Point x.\n * @param py - Point y.\n * @param threshold - How close (in units) the point must be to count as \"on the line\".\n * @returns `true` if the point is within `threshold` of the segment.\n * @remarks\n * This flat form lets you choose the `threshold`. The object-argument {@link linePoint}\n * (`linePoint({ startPoint, endPoint }, { x, y })`) is the same closest-point test with a\n * fixed `0.1` tolerance baked in — reach for it when the default is fine and you want the\n * tidier call site.\n * @example\n * lineToPoint(0, 0, 10, 0, 5, 0, 0.5); // => true\n */\nfunction lineToPoint(x1, y1, x2, y2, px, py, threshold) {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const t = Math.max(0, Math.min(1, ((px - x1) * dx + (py - y1) * dy) / (dx * dx + dy * dy)));\n    const lx = x1 + t * dx;\n    const ly = y1 + t * dy;\n    const ddx = px - lx;\n    const ddy = py - ly;\n    return ddx * ddx + ddy * ddy <= threshold * threshold;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"lineToRect","module":"LineToRect","tsSource":"/**\n * Test whether a line segment crosses any edge of an axis-aligned rectangle.\n *\n * Note: this checks the four edges only — a segment lying entirely *inside* the\n * rectangle is not reported as a hit.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @returns `true` if the segment crosses a rectangle edge.\n * @example\n * lineToRect(-5, 5, 15, 5, 0, 0, 10, 10); // => true\n */\nfunction lineToRect(x1: number, y1: number, x2: number, y2: number, rx: number, ry: number, rw: number, rh: number): boolean {\n    const rectEdges: [\n        number,\n        number,\n        number,\n        number\n    ][] = [\n        [rx, ry, rx + rw, ry],\n        [rx + rw, ry, rx + rw, ry + rh],\n        [rx + rw, ry + rh, rx, ry + rh],\n        [rx, ry + rh, rx, ry],\n    ];\n    for (const [x3, y3, x4, y4] of rectEdges) {\n        if (lineToLine(x1, y1, x2, y2, x3, y3, x4, y4)) {\n            return true;\n        }\n    }\n    return false;\n}","jsSource":"/**\n * Test whether a line segment crosses any edge of an axis-aligned rectangle.\n *\n * Note: this checks the four edges only — a segment lying entirely *inside* the\n * rectangle is not reported as a hit.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @returns `true` if the segment crosses a rectangle edge.\n * @example\n * lineToRect(-5, 5, 15, 5, 0, 0, 10, 10); // => true\n */\nfunction lineToRect(x1, y1, x2, y2, rx, ry, rw, rh) {\n    const rectEdges = [\n        [rx, ry, rx + rw, ry],\n        [rx + rw, ry, rx + rw, ry + rh],\n        [rx + rw, ry + rh, rx, ry + rh],\n        [rx, ry + rh, rx, ry],\n    ];\n    for (const [x3, y3, x4, y4] of rectEdges) {\n        if (lineToLine(x1, y1, x2, y2, x3, y3, x4, y4)) {\n            return true;\n        }\n    }\n    return false;\n}","dependencyKeys":["lineToLine"],"interfaceNames":[]},
  {"key":"linear","module":"Easing","tsSource":"/**\n * Linear easing — constant speed; the identity curve.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value, equal to `t`.\n * @example\n * linear(0.5); // => 0.5\n */\nfunction linear(t: number): number {\n    return t;\n}","jsSource":"/**\n * Linear easing — constant speed; the identity curve.\n *\n * @param t - Normalized time, 0–1.\n * @returns The eased value, equal to `t`.\n * @example\n * linear(0.5); // => 0.5\n */\nfunction linear(t) {\n    return t;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"loop","module":"Animate","tsSource":"/**\n * Map elapsed time into a repeating 0..1 progress value.\n *\n * @example\n * loop(750, 500); // => 0.5 (250ms into the second cycle)\n */\nfunction loop(elapsedMs: number, durationMs: number): number {\n    if (durationMs <= 0)\n        return 1;\n    return ((elapsedMs % durationMs) + durationMs) % durationMs / durationMs;\n}","jsSource":"/**\n * Map elapsed time into a repeating 0..1 progress value.\n *\n * @example\n * loop(750, 500); // => 0.5 (250ms into the second cycle)\n */\nfunction loop(elapsedMs, durationMs) {\n    if (durationMs <= 0)\n        return 1;\n    return ((elapsedMs % durationMs) + durationMs) % durationMs / durationMs;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"mapRange","module":"MapRange","tsSource":"/**\n * Re-map a number from one range into another (also known as \"remap\").\n *\n * Linear and unclamped: a value outside the input range maps proportionally\n * outside the output range. Pair with {@link clamp} if you need it bounded.\n *\n * @param value - The input value.\n * @param inMin - Start of the input range.\n * @param inMax - End of the input range.\n * @param outMin - Start of the output range.\n * @param outMax - End of the output range.\n * @returns `value` rescaled from the input range to the output range.\n * @example\n * mapRange(5, 0, 10, 0, 100); // => 50\n * mapRange(0.5, 0, 1, -180, 180); // => 0\n */\nfunction mapRange(value: number, inMin: number, inMax: number, outMin: number, outMax: number): number {\n    return outMin + ((value - inMin) * (outMax - outMin)) / (inMax - inMin);\n}","jsSource":"/**\n * Re-map a number from one range into another (also known as \"remap\").\n *\n * Linear and unclamped: a value outside the input range maps proportionally\n * outside the output range. Pair with {@link clamp} if you need it bounded.\n *\n * @param value - The input value.\n * @param inMin - Start of the input range.\n * @param inMax - End of the input range.\n * @param outMin - Start of the output range.\n * @param outMax - End of the output range.\n * @returns `value` rescaled from the input range to the output range.\n * @example\n * mapRange(5, 0, 10, 0, 100); // => 50\n * mapRange(0.5, 0, 1, -180, 180); // => 0\n */\nfunction mapRange(value, inMin, inMax, outMin, outMax) {\n    return outMin + ((value - inMin) * (outMax - outMin)) / (inMax - inMin);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"moveAlongLine","module":"MoveAlongLine","tsSource":"/**\n * Find the point a given fraction of the way from one point to another.\n *\n * @param origin - Start point, returned when `ratio` is 0.\n * @param destination - End point, returned when `ratio` is 1.\n * @param ratio - Fraction of the way along; `0.5` is the midpoint.\n * @returns The interpolated `{ x, y }` point.\n * @example\n * moveAlongLine({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction moveAlongLine(origin: Point, destination: Point, ratio: number): Point {\n    let x = origin.x + ratio * (destination.x - origin.x);\n    let y = origin.y + ratio * (destination.y - origin.y);\n    return { x, y };\n}","jsSource":"/**\n * Find the point a given fraction of the way from one point to another.\n *\n * @param origin - Start point, returned when `ratio` is 0.\n * @param destination - End point, returned when `ratio` is 1.\n * @param ratio - Fraction of the way along; `0.5` is the midpoint.\n * @returns The interpolated `{ x, y }` point.\n * @example\n * moveAlongLine({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction moveAlongLine(origin, destination, ratio) {\n    let x = origin.x + ratio * (destination.x - origin.x);\n    let y = origin.y + ratio * (destination.y - origin.y);\n    return { x, y };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"moveToward","module":"MoveToward","tsSource":"/**\n * Step a moving object toward a destination at a fixed speed, snapping on arrival.\n *\n * **Mutates `obj`** — its `x`/`y` are advanced by up to `speed` units toward `dest`.\n * If it's within one step, it lands exactly on `dest`.\n *\n * @param obj - The point to move (mutated in place).\n * @param dest - The target point.\n * @param speed - Maximum distance to travel this call.\n * @returns The heading angle (radians) from `obj` toward `dest`, for orienting the sprite.\n * @example\n * const ball = { x: 0, y: 0 };\n * moveToward(ball, { x: 10, y: 0 }, 2); // ball is now { x: 2, y: 0 }; returns 0\n */\nfunction moveToward(obj: Point, dest: Point, speed: number): number {\n    let dx = dest.x - obj.x, dy = dest.y - obj.y;\n    let dist = Math.sqrt(dx * dx + dy * dy);\n    if (dist > speed) {\n        obj.x += (dx / dist) * speed;\n        obj.y += (dy / dist) * speed;\n    }\n    else {\n        obj.x = dest.x;\n        obj.y = dest.y;\n    }\n    return Math.atan2(dy, dx);\n}","jsSource":"/**\n * Step a moving object toward a destination at a fixed speed, snapping on arrival.\n *\n * **Mutates `obj`** — its `x`/`y` are advanced by up to `speed` units toward `dest`.\n * If it's within one step, it lands exactly on `dest`.\n *\n * @param obj - The point to move (mutated in place).\n * @param dest - The target point.\n * @param speed - Maximum distance to travel this call.\n * @returns The heading angle (radians) from `obj` toward `dest`, for orienting the sprite.\n * @example\n * const ball = { x: 0, y: 0 };\n * moveToward(ball, { x: 10, y: 0 }, 2); // ball is now { x: 2, y: 0 }; returns 0\n */\nfunction moveToward(obj, dest, speed) {\n    let dx = dest.x - obj.x, dy = dest.y - obj.y;\n    let dist = Math.sqrt(dx * dx + dy * dy);\n    if (dist > speed) {\n        obj.x += (dx / dist) * speed;\n        obj.y += (dy / dist) * speed;\n    }\n    else {\n        obj.x = dest.x;\n        obj.y = dest.y;\n    }\n    return Math.atan2(dy, dx);\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"numberWithCommas","module":"NumberWithCommas","tsSource":"/**\n * Format a number with comma thousands-separators.\n *\n * @param x - The number to format.\n * @returns The number as a string with a comma every three digits.\n * @example\n * numberWithCommas(1234567); // => \"1,234,567\"\n */\nfunction numberWithCommas(x: number): string {\n    return x.toString().replace(/\\B(?=(\\d{3})+(?!\\d))/g, \",\");\n}","jsSource":"/**\n * Format a number with comma thousands-separators.\n *\n * @param x - The number to format.\n * @returns The number as a string with a comma every three digits.\n * @example\n * numberWithCommas(1234567); // => \"1,234,567\"\n */\nfunction numberWithCommas(x) {\n    return x.toString().replace(/\\B(?=(\\d{3})+(?!\\d))/g, \",\");\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"pingPong","module":"PingPong","tsSource":"/**\n * Bounce a value back and forth between 0 and `length` as `t` increases.\n *\n * Produces a triangle wave: `0 → length → 0 → length …`. Perfect for seamless\n * looping motion (a sprite sliding to the edge and back without a visible jump).\n *\n * @param t - The input value, usually an ever-increasing time or counter.\n * @param length - The peak of the bounce; output ranges over `[0, length]`.\n * @returns The ping-ponged value in `[0, length]`. Returns `0` if `length <= 0`.\n * @example\n * pingPong(15, 10); // => 5  (past the peak, heading back down)\n * pingPong(20, 10); // => 0  (full cycle, back to start)\n */\nfunction pingPong(t: number, length: number): number {\n    if (length <= 0)\n        return 0;\n    const span = length * 2;\n    const x = ((t % span) + span) % span;\n    return length - Math.abs(x - length);\n}","jsSource":"/**\n * Bounce a value back and forth between 0 and `length` as `t` increases.\n *\n * Produces a triangle wave: `0 → length → 0 → length …`. Perfect for seamless\n * looping motion (a sprite sliding to the edge and back without a visible jump).\n *\n * @param t - The input value, usually an ever-increasing time or counter.\n * @param length - The peak of the bounce; output ranges over `[0, length]`.\n * @returns The ping-ponged value in `[0, length]`. Returns `0` if `length <= 0`.\n * @example\n * pingPong(15, 10); // => 5  (past the peak, heading back down)\n * pingPong(20, 10); // => 0  (full cycle, back to start)\n */\nfunction pingPong(t, length) {\n    if (length <= 0)\n        return 0;\n    const span = length * 2;\n    const x = ((t % span) + span) % span;\n    return length - Math.abs(x - length);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"pointCircle","module":"CollisionObjectAPI/PointCircle","tsSource":"/**\n * Test whether a point falls inside or on a circle (object-argument form of `pointToCircle`).\n *\n * @param point - The point to test.\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the point is within `radius` of the circle center.\n * @remarks\n * This object-argument form is the **recommended** one; {@link pointToCircle} is the\n * flat, five-number equivalent running the same squared-distance comparison.\n * @example\n * pointCircle({ x: 1, y: 1 }, { x: 0, y: 0, radius: 5 }); // => true\n */\nfunction pointCircle(point: Point, circle: Circle): boolean {\n    const dx = point.x - circle.x;\n    const dy = point.y - circle.y;\n    return dx * dx + dy * dy <= circle.radius * circle.radius;\n}","jsSource":"/**\n * Test whether a point falls inside or on a circle (object-argument form of `pointToCircle`).\n *\n * @param point - The point to test.\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the point is within `radius` of the circle center.\n * @remarks\n * This object-argument form is the **recommended** one; {@link pointToCircle} is the\n * flat, five-number equivalent running the same squared-distance comparison.\n * @example\n * pointCircle({ x: 1, y: 1 }, { x: 0, y: 0, radius: 5 }); // => true\n */\nfunction pointCircle(point, circle) {\n    const dx = point.x - circle.x;\n    const dy = point.y - circle.y;\n    return dx * dx + dy * dy <= circle.radius * circle.radius;\n}","dependencyKeys":[],"interfaceNames":["Circle","Point"]},
  {"key":"pointToCircle","module":"PointToCircle","tsSource":"/**\n * Test whether a point falls inside (or on the edge of) a circle.\n *\n * @param px - Point x.\n * @param py - Point y.\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param radius - Circle radius.\n * @returns `true` if the point is within `radius` of the center.\n * @remarks\n * Flat, five-number form. For new code the **recommended** shape is the object-argument\n * {@link pointCircle} (`pointCircle({ x, y }, { x, y, radius })`). Both use the same\n * squared-distance comparison — comparing `dx² + dy²` against `radius²` avoids a `sqrt`\n * and gives the identical result for a non-negative radius.\n * @example\n * pointToCircle(1, 1, 0, 0, 5); // => true\n */\nfunction pointToCircle(px: number, py: number, cx: number, cy: number, radius: number): boolean {\n    const dx = px - cx;\n    const dy = py - cy;\n    return dx * dx + dy * dy <= radius * radius;\n}","jsSource":"/**\n * Test whether a point falls inside (or on the edge of) a circle.\n *\n * @param px - Point x.\n * @param py - Point y.\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param radius - Circle radius.\n * @returns `true` if the point is within `radius` of the center.\n * @remarks\n * Flat, five-number form. For new code the **recommended** shape is the object-argument\n * {@link pointCircle} (`pointCircle({ x, y }, { x, y, radius })`). Both use the same\n * squared-distance comparison — comparing `dx² + dy²` against `radius²` avoids a `sqrt`\n * and gives the identical result for a non-negative radius.\n * @example\n * pointToCircle(1, 1, 0, 0, 5); // => true\n */\nfunction pointToCircle(px, py, cx, cy, radius) {\n    const dx = px - cx;\n    const dy = py - cy;\n    return dx * dx + dy * dy <= radius * radius;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"pointToPolygon","module":"PointToPolygon","tsSource":"/**\n * Test whether a point falls inside a polygon (ray-casting / even-odd rule).\n *\n * Casts a horizontal ray from the point and counts how many polygon edges it\n * crosses — an odd count means the point is inside. Works for both convex and\n * concave polygons.\n *\n * @param px - Point x.\n * @param py - Point y.\n * @param vertices - The polygon vertices, in order.\n * @returns `true` if the point lies inside the polygon.\n * @remarks\n * Takes loose `px, py` plus a raw vertex array. For new code the **recommended** shape is\n * the object-argument {@link polygonPoint} (`polygonPoint({ vertices }, { x, y })`), which\n * runs the identical ray-cast.\n * @example\n * const square = [{ x: 0, y: 0 }, { x: 10, y: 0 }, { x: 10, y: 10 }, { x: 0, y: 10 }];\n * pointToPolygon(5, 5, square); // => true\n * pointToPolygon(20, 20, square); // => false\n */\nfunction pointToPolygon(px: number, py: number, vertices: Point[]): boolean {\n    let inside = false;\n    for (let i = 0, j = vertices.length - 1; i < vertices.length; j = i++) {\n        const xi = vertices[i].x, yi = vertices[i].y;\n        const xj = vertices[j].x, yj = vertices[j].y;\n        if ((yi > py) !== (yj > py) && px < (xj - xi) * (py - yi) / (yj - yi) + xi) {\n            inside = !inside;\n        }\n    }\n    return inside;\n}","jsSource":"/**\n * Test whether a point falls inside a polygon (ray-casting / even-odd rule).\n *\n * Casts a horizontal ray from the point and counts how many polygon edges it\n * crosses — an odd count means the point is inside. Works for both convex and\n * concave polygons.\n *\n * @param px - Point x.\n * @param py - Point y.\n * @param vertices - The polygon vertices, in order.\n * @returns `true` if the point lies inside the polygon.\n * @remarks\n * Takes loose `px, py` plus a raw vertex array. For new code the **recommended** shape is\n * the object-argument {@link polygonPoint} (`polygonPoint({ vertices }, { x, y })`), which\n * runs the identical ray-cast.\n * @example\n * const square = [{ x: 0, y: 0 }, { x: 10, y: 0 }, { x: 10, y: 10 }, { x: 0, y: 10 }];\n * pointToPolygon(5, 5, square); // => true\n * pointToPolygon(20, 20, square); // => false\n */\nfunction pointToPolygon(px, py, vertices) {\n    let inside = false;\n    for (let i = 0, j = vertices.length - 1; i < vertices.length; j = i++) {\n        const xi = vertices[i].x, yi = vertices[i].y;\n        const xj = vertices[j].x, yj = vertices[j].y;\n        if ((yi > py) !== (yj > py) && px < (xj - xi) * (py - yi) / (yj - yi) + xi) {\n            inside = !inside;\n        }\n    }\n    return inside;\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"pointToRect","module":"PointToRect","tsSource":"/**\n * Test whether a point falls inside (or on the edge of) an axis-aligned rectangle.\n *\n * @param px - Point x.\n * @param py - Point y.\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @returns `true` if the point lies within the rectangle bounds, including edges.\n * @example\n * pointToRect(5, 5, 0, 0, 10, 10); // => true\n */\nfunction pointToRect(px: number, py: number, rx: number, ry: number, rw: number, rh: number): boolean {\n    return px >= rx && px <= rx + rw && py >= ry && py <= ry + rh;\n}","jsSource":"/**\n * Test whether a point falls inside (or on the edge of) an axis-aligned rectangle.\n *\n * @param px - Point x.\n * @param py - Point y.\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @returns `true` if the point lies within the rectangle bounds, including edges.\n * @example\n * pointToRect(5, 5, 0, 0, 10, 10); // => true\n */\nfunction pointToRect(px, py, rx, ry, rw, rh) {\n    return px >= rx && px <= rx + rw && py >= ry && py <= ry + rh;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"polygonCircle","module":"CollisionObjectAPI/PolygonCircle","tsSource":"/**\n * Test whether a circle overlaps a polygon.\n *\n * Walks each polygon edge and tests it against the circle. If no edge touches\n * the circle, it then checks whether the circle's center sits inside the polygon,\n * which catches the fully-contained case.\n *\n * @param polygon - The polygon (`vertices`).\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the circle touches an edge or sits inside the polygon.\n * @example\n * polygonCircle({ vertices: square }, { x: 5, y: 5, radius: 2 }); // => true\n */\nfunction polygonCircle(polygon: Polygon, circle: Circle): boolean {\n    const { vertices } = polygon;\n    for (let current = 0; current < vertices.length; current++) {\n        const startPoint = vertices[current];\n        const endPoint = vertices[(current + 1) % vertices.length];\n        const edge: Line = { startPoint, endPoint };\n        if (lineCircle(edge, circle)) {\n            return true;\n        }\n    }\n    return polygonPoint(polygon, {\n        x: circle.x,\n        y: circle.y,\n    });\n}","jsSource":"/**\n * Test whether a circle overlaps a polygon.\n *\n * Walks each polygon edge and tests it against the circle. If no edge touches\n * the circle, it then checks whether the circle's center sits inside the polygon,\n * which catches the fully-contained case.\n *\n * @param polygon - The polygon (`vertices`).\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the circle touches an edge or sits inside the polygon.\n * @example\n * polygonCircle({ vertices: square }, { x: 5, y: 5, radius: 2 }); // => true\n */\nfunction polygonCircle(polygon, circle) {\n    const { vertices } = polygon;\n    for (let current = 0; current < vertices.length; current++) {\n        const startPoint = vertices[current];\n        const endPoint = vertices[(current + 1) % vertices.length];\n        const edge = { startPoint, endPoint };\n        if (lineCircle(edge, circle)) {\n            return true;\n        }\n    }\n    return polygonPoint(polygon, {\n        x: circle.x,\n        y: circle.y,\n    });\n}","dependencyKeys":["lineCircle","polygonPoint"],"interfaceNames":["Circle","Line","Polygon"]},
  {"key":"polygonLine","module":"CollisionObjectAPI/PolygonLine","tsSource":"/**\n * Test whether a line segment crosses any edge of a polygon.\n *\n * Walks each polygon edge and runs a segment-vs-segment intersection test\n * against `line`.\n *\n * This checks edge crossings only. A line segment fully contained inside the\n * polygon, with no boundary intersection, returns `false`.\n *\n * @param polygon - The polygon (`vertices`).\n * @param line - The line segment (`startPoint`, `endPoint`).\n * @returns `true` if the line crosses a polygon edge.\n * @example\n * polygonLine({ vertices: square }, { startPoint: { x: -5, y: 5 }, endPoint: { x: 15, y: 5 } }); // => true\n */\nfunction polygonLine(polygon: Polygon, line: Line): boolean {\n    const { vertices } = polygon;\n    for (let current = 0; current < vertices.length; current++) {\n        const startPoint = vertices[current];\n        const endPoint = vertices[(current + 1) % vertices.length];\n        const edge: Line = { startPoint, endPoint };\n        if (lineLine(line, edge).hit) {\n            return true;\n        }\n    }\n    return false;\n}","jsSource":"/**\n * Test whether a line segment crosses any edge of a polygon.\n *\n * Walks each polygon edge and runs a segment-vs-segment intersection test\n * against `line`.\n *\n * This checks edge crossings only. A line segment fully contained inside the\n * polygon, with no boundary intersection, returns `false`.\n *\n * @param polygon - The polygon (`vertices`).\n * @param line - The line segment (`startPoint`, `endPoint`).\n * @returns `true` if the line crosses a polygon edge.\n * @example\n * polygonLine({ vertices: square }, { startPoint: { x: -5, y: 5 }, endPoint: { x: 15, y: 5 } }); // => true\n */\nfunction polygonLine(polygon, line) {\n    const { vertices } = polygon;\n    for (let current = 0; current < vertices.length; current++) {\n        const startPoint = vertices[current];\n        const endPoint = vertices[(current + 1) % vertices.length];\n        const edge = { startPoint, endPoint };\n        if (lineLine(line, edge).hit) {\n            return true;\n        }\n    }\n    return false;\n}","dependencyKeys":["lineLine"],"interfaceNames":["Line","Polygon"]},
  {"key":"polygonPoint","module":"CollisionObjectAPI/PolygonPoint","tsSource":"/**\n * Test whether a point is inside a polygon (ray-casting / even-odd rule).\n *\n * Casts a horizontal ray from the point and counts how many polygon edges it\n * crosses — an odd count means the point is inside. Works for both convex and\n * concave polygons.\n *\n * Boundary points are not special-cased. A point on an edge or vertex follows\n * the raw parity result from the ray test, so edge cases may differ depending\n * on which boundary segment is hit.\n *\n * @param polygon - The polygon (`vertices`).\n * @param point - The point to test.\n * @returns `true` if the point is inside the polygon.\n * @remarks\n * This object-argument form is the **recommended** one; {@link pointToPolygon} is the flat\n * `(px, py, vertices[])` equivalent running the same ray-cast.\n * @example\n * polygonPoint({ vertices: square }, { x: 5, y: 5 }); // => true\n */\nfunction polygonPoint(polygon: Polygon, point: Point): boolean {\n    const { vertices } = polygon;\n    let inside = false;\n    for (let current = 0, previous = vertices.length - 1; current < vertices.length; previous = current++) {\n        const { x: currentX, y: currentY } = vertices[current];\n        const { x: previousX, y: previousY } = vertices[previous];\n        if ((currentY > point.y) !== (previousY > point.y) &&\n            point.x < ((previousX - currentX) * (point.y - currentY)) / (previousY - currentY) + currentX) {\n            inside = !inside;\n        }\n    }\n    return inside;\n}","jsSource":"/**\n * Test whether a point is inside a polygon (ray-casting / even-odd rule).\n *\n * Casts a horizontal ray from the point and counts how many polygon edges it\n * crosses — an odd count means the point is inside. Works for both convex and\n * concave polygons.\n *\n * Boundary points are not special-cased. A point on an edge or vertex follows\n * the raw parity result from the ray test, so edge cases may differ depending\n * on which boundary segment is hit.\n *\n * @param polygon - The polygon (`vertices`).\n * @param point - The point to test.\n * @returns `true` if the point is inside the polygon.\n * @remarks\n * This object-argument form is the **recommended** one; {@link pointToPolygon} is the flat\n * `(px, py, vertices[])` equivalent running the same ray-cast.\n * @example\n * polygonPoint({ vertices: square }, { x: 5, y: 5 }); // => true\n */\nfunction polygonPoint(polygon, point) {\n    const { vertices } = polygon;\n    let inside = false;\n    for (let current = 0, previous = vertices.length - 1; current < vertices.length; previous = current++) {\n        const { x: currentX, y: currentY } = vertices[current];\n        const { x: previousX, y: previousY } = vertices[previous];\n        if ((currentY > point.y) !== (previousY > point.y) &&\n            point.x < ((previousX - currentX) * (point.y - currentY)) / (previousY - currentY) + currentX) {\n            inside = !inside;\n        }\n    }\n    return inside;\n}","dependencyKeys":[],"interfaceNames":["Point","Polygon"]},
  {"key":"polygonPolygon","module":"CollisionObjectAPI/PolygonPolygon","tsSource":"/**\n * Test whether two polygons overlap (object-argument form).\n *\n * Walks each edge of the first polygon and tests it against the second. If no\n * edges cross, it then checks both containment directions by testing a sample\n * vertex from each polygon against the other polygon.\n *\n * @param polygon1 - First polygon (`vertices`).\n * @param polygon2 - Second polygon (`vertices`).\n * @returns `true` if the polygons intersect or one contains the other.\n * @remarks\n * This is the **canonical, complete** polygon-overlap test. The flat {@link polygonToPolygon}\n * (bare vertex arrays) delegates here, so both catch edge-only crossings, not just\n * contained vertices.\n * @example\n * polygonPolygon({ vertices: squareA }, { vertices: squareB }); // => true when overlapping\n */\nfunction polygonPolygon(polygon1: Polygon, polygon2: Polygon): boolean {\n    const { vertices } = polygon1;\n    for (let current = 0; current < vertices.length; current++) {\n        const startPoint = vertices[current];\n        const endPoint = vertices[(current + 1) % vertices.length];\n        const edge: Line = { startPoint, endPoint };\n        if (polygonLine(polygon2, edge)) {\n            return true;\n        }\n    }\n    return polygonPoint(polygon1, polygon2.vertices[0]) || polygonPoint(polygon2, polygon1.vertices[0]);\n}","jsSource":"/**\n * Test whether two polygons overlap (object-argument form).\n *\n * Walks each edge of the first polygon and tests it against the second. If no\n * edges cross, it then checks both containment directions by testing a sample\n * vertex from each polygon against the other polygon.\n *\n * @param polygon1 - First polygon (`vertices`).\n * @param polygon2 - Second polygon (`vertices`).\n * @returns `true` if the polygons intersect or one contains the other.\n * @remarks\n * This is the **canonical, complete** polygon-overlap test. The flat {@link polygonToPolygon}\n * (bare vertex arrays) delegates here, so both catch edge-only crossings, not just\n * contained vertices.\n * @example\n * polygonPolygon({ vertices: squareA }, { vertices: squareB }); // => true when overlapping\n */\nfunction polygonPolygon(polygon1, polygon2) {\n    const { vertices } = polygon1;\n    for (let current = 0; current < vertices.length; current++) {\n        const startPoint = vertices[current];\n        const endPoint = vertices[(current + 1) % vertices.length];\n        const edge = { startPoint, endPoint };\n        if (polygonLine(polygon2, edge)) {\n            return true;\n        }\n    }\n    return polygonPoint(polygon1, polygon2.vertices[0]) || polygonPoint(polygon2, polygon1.vertices[0]);\n}","dependencyKeys":["polygonLine","polygonPoint"],"interfaceNames":["Line","Polygon"]},
  {"key":"polygonToPolygon","module":"PolygonToPolygon","tsSource":"/**\n * Test whether two polygons overlap.\n *\n * This flat form takes two raw vertex arrays and delegates to the canonical\n * {@link polygonPolygon}, which is complete: it checks edge crossings *and* both\n * containment directions. (An earlier version tested vertices only and silently\n * missed edge-only overlaps — two rotated squares crossing in an \"X\" with no\n * contained vertex. That gap is gone.)\n *\n * For new code the **recommended** shape is {@link polygonPolygon} itself\n * (`polygonPolygon({ vertices }, { vertices })`); this wrapper exists for callers\n * that already hold bare point arrays.\n *\n * @param poly1 - First polygon as an array of points.\n * @param poly2 - Second polygon as an array of points.\n * @returns `true` if the polygons intersect or one contains the other.\n * @example\n * polygonToPolygon(squareA, squareB); // => true when they overlap\n */\nfunction polygonToPolygon(poly1: Point[], poly2: Point[]): boolean {\n    return polygonPolygon({ vertices: poly1 }, { vertices: poly2 });\n}","jsSource":"/**\n * Test whether two polygons overlap.\n *\n * This flat form takes two raw vertex arrays and delegates to the canonical\n * {@link polygonPolygon}, which is complete: it checks edge crossings *and* both\n * containment directions. (An earlier version tested vertices only and silently\n * missed edge-only overlaps — two rotated squares crossing in an \"X\" with no\n * contained vertex. That gap is gone.)\n *\n * For new code the **recommended** shape is {@link polygonPolygon} itself\n * (`polygonPolygon({ vertices }, { vertices })`); this wrapper exists for callers\n * that already hold bare point arrays.\n *\n * @param poly1 - First polygon as an array of points.\n * @param poly2 - Second polygon as an array of points.\n * @returns `true` if the polygons intersect or one contains the other.\n * @example\n * polygonToPolygon(squareA, squareB); // => true when they overlap\n */\nfunction polygonToPolygon(poly1, poly2) {\n    return polygonPolygon({ vertices: poly1 }, { vertices: poly2 });\n}","dependencyKeys":["polygonPolygon"],"interfaceNames":["Point"]},
  {"key":"quadraticBezier","module":"QuadraticBezier","tsSource":"/**\n * Find the point at parameter `t` along a quadratic Bézier curve (one control point).\n *\n * Uses the closed-form quadratic Bézier formula\n * `(1-t)²·p0 + 2(1-t)t·p1 + t²·p2`.\n *\n * @param t - Position along the curve, 0–1.\n * @param p0 - Start anchor point (returned at `t = 0`).\n * @param p1 - The single control handle that bends the curve.\n * @param p2 - End anchor point (returned at `t = 1`).\n * @returns The `{ x, y }` point on the curve at `t`.\n * @example\n * quadraticBezier(0.5, {x:0,y:0}, {x:50,y:100}, {x:100,y:0}); // => { x: 50, y: 50 }\n */\nfunction quadraticBezier(t: number, p0: Point, p1: Point, p2: Point): Point {\n    const mt = 1 - t;\n    return {\n        x: mt * mt * p0.x + 2 * mt * t * p1.x + t * t * p2.x,\n        y: mt * mt * p0.y + 2 * mt * t * p1.y + t * t * p2.y,\n    };\n}","jsSource":"/**\n * Find the point at parameter `t` along a quadratic Bézier curve (one control point).\n *\n * Uses the closed-form quadratic Bézier formula\n * `(1-t)²·p0 + 2(1-t)t·p1 + t²·p2`.\n *\n * @param t - Position along the curve, 0–1.\n * @param p0 - Start anchor point (returned at `t = 0`).\n * @param p1 - The single control handle that bends the curve.\n * @param p2 - End anchor point (returned at `t = 1`).\n * @returns The `{ x, y }` point on the curve at `t`.\n * @example\n * quadraticBezier(0.5, {x:0,y:0}, {x:50,y:100}, {x:100,y:0}); // => { x: 50, y: 50 }\n */\nfunction quadraticBezier(t, p0, p1, p2) {\n    const mt = 1 - t;\n    return {\n        x: mt * mt * p0.x + 2 * mt * t * p1.x + t * t * p2.x,\n        y: mt * mt * p0.y + 2 * mt * t * p1.y + t * t * p2.y,\n    };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"radToDeg","module":"RadToDeg","tsSource":"/**\n * Convert an angle from radians to degrees.\n *\n * @param radians - Angle in radians.\n * @returns The equivalent angle in degrees.\n * @example\n * radToDeg(Math.PI); // => 180\n */\nfunction radToDeg(radians: number): number {\n    return (radians * 180) / Math.PI;\n}","jsSource":"/**\n * Convert an angle from radians to degrees.\n *\n * @param radians - Angle in radians.\n * @returns The equivalent angle in degrees.\n * @example\n * radToDeg(Math.PI); // => 180\n */\nfunction radToDeg(radians) {\n    return (radians * 180) / Math.PI;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"randomIntegerBetween","module":"RandomIntegerBetween","tsSource":"/**\n * Pick a random integer in an inclusive range.\n *\n * @param min - Lowest possible value (inclusive).\n * @param max - Highest possible value (inclusive).\n * @returns A random integer `n` where `min <= n <= max`.\n * @example\n * randomIntegerBetween(1, 6); // => e.g. 4 (a six-sided die)\n */\nfunction randomIntegerBetween(min: number, max: number): number {\n    max++;\n    return Math.floor(Math.random() * (max - min) + min);\n}","jsSource":"/**\n * Pick a random integer in an inclusive range.\n *\n * @param min - Lowest possible value (inclusive).\n * @param max - Highest possible value (inclusive).\n * @returns A random integer `n` where `min <= n <= max`.\n * @example\n * randomIntegerBetween(1, 6); // => e.g. 4 (a six-sided die)\n */\nfunction randomIntegerBetween(min, max) {\n    max++;\n    return Math.floor(Math.random() * (max - min) + min);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"randomNumberBetween","module":"RandomNumberBetween","tsSource":"/**\n * Pick a random floating-point number in a half-open range.\n *\n * @param min - Lower bound (inclusive).\n * @param max - Upper bound (exclusive).\n * @returns A random number `n` where `min <= n < max`.\n * @example\n * randomNumberBetween(0, 1); // => e.g. 0.2738\n */\nfunction randomNumberBetween(min: number, max: number): number {\n    return Math.random() * (max - min) + min;\n}","jsSource":"/**\n * Pick a random floating-point number in a half-open range.\n *\n * @param min - Lower bound (inclusive).\n * @param max - Upper bound (exclusive).\n * @returns A random number `n` where `min <= n < max`.\n * @example\n * randomNumberBetween(0, 1); // => e.g. 0.2738\n */\nfunction randomNumberBetween(min, max) {\n    return Math.random() * (max - min) + min;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"rectToPolygon","module":"RectToPolygon","tsSource":"/**\n * Test whether an axis-aligned rectangle overlaps a polygon.\n *\n * Combines three checks so no overlap is missed:\n * 1. Any polygon vertex inside the rectangle.\n * 2. Any rectangle corner inside the polygon.\n * 3. Any polygon edge crossing any rectangle edge.\n *\n * The vertex-only approach used by most simple implementations fails when one\n * shape is entirely inside the other, or when edges cross without either shape's\n * vertices being contained. All three checks together are complete.\n *\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @param vertices - The polygon vertices, in order.\n * @returns `true` if the rectangle and polygon overlap.\n * @example\n * const tri = [{ x: 5, y: 0 }, { x: 10, y: 10 }, { x: 0, y: 10 }];\n * rectToPolygon(3, 3, 8, 8, tri); // => true\n * rectToPolygon(50, 50, 10, 10, tri); // => false\n */\nfunction rectToPolygon(rx: number, ry: number, rw: number, rh: number, vertices: Point[]): boolean {\n    const rx2 = rx + rw, ry2 = ry + rh;\n    // 1. Any polygon vertex inside the rect?\n    for (const { x, y } of vertices) {\n        if (x >= rx && x <= rx2 && y >= ry && y <= ry2)\n            return true;\n    }\n    // 2. Any rect corner inside the polygon?\n    const corners: Point[] = [\n        { x: rx, y: ry }, { x: rx2, y: ry },\n        { x: rx2, y: ry2 }, { x: rx, y: ry2 },\n    ];\n    for (const { x, y } of corners) {\n        if (pointInPolygon(x, y, vertices))\n            return true;\n    }\n    // 3. Any polygon edge crossing any rect edge?\n    const rectEdges: [\n        number,\n        number,\n        number,\n        number\n    ][] = [\n        [rx, ry, rx2, ry], [rx2, ry, rx2, ry2],\n        [rx2, ry2, rx, ry2], [rx, ry2, rx, ry],\n    ];\n    const n = vertices.length;\n    for (let i = 0; i < n; i++) {\n        const a = vertices[i], b = vertices[(i + 1) % n];\n        for (const [x3, y3, x4, y4] of rectEdges) {\n            if (segmentsIntersect(a.x, a.y, b.x, b.y, x3, y3, x4, y4))\n                return true;\n        }\n    }\n    return false;\n}","jsSource":"/**\n * Test whether an axis-aligned rectangle overlaps a polygon.\n *\n * Combines three checks so no overlap is missed:\n * 1. Any polygon vertex inside the rectangle.\n * 2. Any rectangle corner inside the polygon.\n * 3. Any polygon edge crossing any rectangle edge.\n *\n * The vertex-only approach used by most simple implementations fails when one\n * shape is entirely inside the other, or when edges cross without either shape's\n * vertices being contained. All three checks together are complete.\n *\n * @param rx - Rectangle left edge.\n * @param ry - Rectangle top edge.\n * @param rw - Rectangle width.\n * @param rh - Rectangle height.\n * @param vertices - The polygon vertices, in order.\n * @returns `true` if the rectangle and polygon overlap.\n * @example\n * const tri = [{ x: 5, y: 0 }, { x: 10, y: 10 }, { x: 0, y: 10 }];\n * rectToPolygon(3, 3, 8, 8, tri); // => true\n * rectToPolygon(50, 50, 10, 10, tri); // => false\n */\nfunction rectToPolygon(rx, ry, rw, rh, vertices) {\n    const rx2 = rx + rw, ry2 = ry + rh;\n    // 1. Any polygon vertex inside the rect?\n    for (const { x, y } of vertices) {\n        if (x >= rx && x <= rx2 && y >= ry && y <= ry2)\n            return true;\n    }\n    // 2. Any rect corner inside the polygon?\n    const corners = [\n        { x: rx, y: ry }, { x: rx2, y: ry },\n        { x: rx2, y: ry2 }, { x: rx, y: ry2 },\n    ];\n    for (const { x, y } of corners) {\n        if (pointInPolygon(x, y, vertices))\n            return true;\n    }\n    // 3. Any polygon edge crossing any rect edge?\n    const rectEdges = [\n        [rx, ry, rx2, ry], [rx2, ry, rx2, ry2],\n        [rx2, ry2, rx, ry2], [rx, ry2, rx, ry],\n    ];\n    const n = vertices.length;\n    for (let i = 0; i < n; i++) {\n        const a = vertices[i], b = vertices[(i + 1) % n];\n        for (const [x3, y3, x4, y4] of rectEdges) {\n            if (segmentsIntersect(a.x, a.y, b.x, b.y, x3, y3, x4, y4))\n                return true;\n        }\n    }\n    return false;\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"rectToRect","module":"RectToRect","tsSource":"/**\n * Test whether two axis-aligned rectangles overlap (AABB collision).\n *\n * @param x1 - First rect left edge.\n * @param y1 - First rect top edge.\n * @param w1 - First rect width.\n * @param h1 - First rect height.\n * @param x2 - Second rect left edge.\n * @param y2 - Second rect top edge.\n * @param w2 - Second rect width.\n * @param h2 - Second rect height.\n * @returns `true` if the two rectangles have positive overlapping area.\n * @example\n * rectToRect(0, 0, 10, 10, 5, 5, 10, 10); // => true\n */\nfunction rectToRect(x1: number, y1: number, w1: number, h1: number, x2: number, y2: number, w2: number, h2: number): boolean {\n    return x1 < x2 + w2 && x1 + w1 > x2 && y1 < y2 + h2 && y1 + h1 > y2;\n}","jsSource":"/**\n * Test whether two axis-aligned rectangles overlap (AABB collision).\n *\n * @param x1 - First rect left edge.\n * @param y1 - First rect top edge.\n * @param w1 - First rect width.\n * @param h1 - First rect height.\n * @param x2 - Second rect left edge.\n * @param y2 - Second rect top edge.\n * @param w2 - Second rect width.\n * @param h2 - Second rect height.\n * @returns `true` if the two rectangles have positive overlapping area.\n * @example\n * rectToRect(0, 0, 10, 10, 5, 5, 10, 10); // => true\n */\nfunction rectToRect(x1, y1, w1, h1, x2, y2, w2, h2) {\n    return x1 < x2 + w2 && x1 + w1 > x2 && y1 < y2 + h2 && y1 + h1 > y2;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"rgbToCss","module":"Color","tsSource":"/**\n * Format an RGB color as a CSS `rgb()` string, rounding channels to integers.\n *\n * @param rgb - Channels 0–255 (fractional values are rounded).\n * @returns e.g. `\"rgb(255, 128, 0)\"`.\n * @example\n * rgbToCss({ r: 255, g: 127.5, b: 0 }); // => \"rgb(255, 128, 0)\"\n */\nfunction rgbToCss({ r, g, b }: RGB): string {\n    return `rgb(${Math.round(r)}, ${Math.round(g)}, ${Math.round(b)})`;\n}","jsSource":"/**\n * Format an RGB color as a CSS `rgb()` string, rounding channels to integers.\n *\n * @param rgb - Channels 0–255 (fractional values are rounded).\n * @returns e.g. `\"rgb(255, 128, 0)\"`.\n * @example\n * rgbToCss({ r: 255, g: 127.5, b: 0 }); // => \"rgb(255, 128, 0)\"\n */\nfunction rgbToCss({ r, g, b }) {\n    return `rgb(${Math.round(r)}, ${Math.round(g)}, ${Math.round(b)})`;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"rgbToHsl","module":"Color","tsSource":"/**\n * Convert an RGB color to HSL.\n *\n * Hue is the angle on the color wheel; saturation is how vivid (vs. gray) the\n * color is; lightness is how close to black/white. Separating these is what lets\n * you interpolate colors without dragging them through gray — see {@link lerpColorHsl}.\n *\n * @param rgb - Channels 0–255.\n * @returns `{ h, s, l }` with `h` 0–360 and `s`/`l` 0–100.\n * @example\n * rgbToHsl({ r: 255, g: 0, b: 0 }); // => { h: 0, s: 100, l: 50 }\n */\nfunction rgbToHsl({ r, g, b }: RGB): HSL {\n    const rn = r / 255;\n    const gn = g / 255;\n    const bn = b / 255;\n    const max = Math.max(rn, gn, bn);\n    const min = Math.min(rn, gn, bn);\n    const d = max - min;\n    let h = 0;\n    if (d !== 0) {\n        if (max === rn)\n            h = ((gn - bn) / d) % 6;\n        else if (max === gn)\n            h = (bn - rn) / d + 2;\n        else\n            h = (rn - gn) / d + 4;\n        h *= 60;\n        if (h < 0)\n            h += 360;\n    }\n    const l = (max + min) / 2;\n    const s = d === 0 ? 0 : d / (1 - Math.abs(2 * l - 1));\n    return { h, s: s * 100, l: l * 100 };\n}","jsSource":"/**\n * Convert an RGB color to HSL.\n *\n * Hue is the angle on the color wheel; saturation is how vivid (vs. gray) the\n * color is; lightness is how close to black/white. Separating these is what lets\n * you interpolate colors without dragging them through gray — see {@link lerpColorHsl}.\n *\n * @param rgb - Channels 0–255.\n * @returns `{ h, s, l }` with `h` 0–360 and `s`/`l` 0–100.\n * @example\n * rgbToHsl({ r: 255, g: 0, b: 0 }); // => { h: 0, s: 100, l: 50 }\n */\nfunction rgbToHsl({ r, g, b }) {\n    const rn = r / 255;\n    const gn = g / 255;\n    const bn = b / 255;\n    const max = Math.max(rn, gn, bn);\n    const min = Math.min(rn, gn, bn);\n    const d = max - min;\n    let h = 0;\n    if (d !== 0) {\n        if (max === rn)\n            h = ((gn - bn) / d) % 6;\n        else if (max === gn)\n            h = (bn - rn) / d + 2;\n        else\n            h = (rn - gn) / d + 4;\n        h *= 60;\n        if (h < 0)\n            h += 360;\n    }\n    const l = (max + min) / 2;\n    const s = d === 0 ? 0 : d / (1 - Math.abs(2 * l - 1));\n    return { h, s: s * 100, l: l * 100 };\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"shortestAngleBetween","module":"AngleInterpolation","tsSource":"/**\n * The smallest signed angle to rotate from `a` to `b`, taking the short way around.\n *\n * Solves the classic \"360° seam\" problem: rotating from 350° to 10° should be\n * `+20°`, not `-340°`. Positive results turn counter-clockwise.\n *\n * @param a - Start angle in radians.\n * @param b - Target angle in radians.\n * @returns The shortest signed delta in `(-π, π]`.\n * @example\n * shortestAngleBetween(0, Math.PI / 2); // => Math.PI / 2\n */\nfunction shortestAngleBetween(a: number, b: number): number {\n    return wrapAngle(b - a);\n}","jsSource":"/**\n * The smallest signed angle to rotate from `a` to `b`, taking the short way around.\n *\n * Solves the classic \"360° seam\" problem: rotating from 350° to 10° should be\n * `+20°`, not `-340°`. Positive results turn counter-clockwise.\n *\n * @param a - Start angle in radians.\n * @param b - Target angle in radians.\n * @returns The shortest signed delta in `(-π, π]`.\n * @example\n * shortestAngleBetween(0, Math.PI / 2); // => Math.PI / 2\n */\nfunction shortestAngleBetween(a, b) {\n    return wrapAngle(b - a);\n}","dependencyKeys":["wrapAngle"],"interfaceNames":[]},
  {"key":"sineCurve","module":"SineCurve","tsSource":"/**\n * Oscillate a value smoothly around a baseline using a sine wave.\n *\n * Pure and deterministic: pass the current `time` (e.g. `performance.now()`) and the\n * same inputs always produce the same output — so it's easy to test and reproduce.\n *\n * @param startingValue - The baseline the result oscillates around.\n * @param differential - The amplitude (how far above/below the baseline it swings).\n * @param speed - Oscillation speed multiplier applied to `time`.\n * @param time - The current time in milliseconds (e.g. `performance.now()`).\n * @returns The oscillating value at the given moment.\n * @example\n * const y = sineCurve(200, 50, 0.002, performance.now()); // bob around y=200 by ±50\n */\nfunction sineCurve(startingValue: number, differential: number, speed: number, time: number): number {\n    return startingValue + Math.sin(time * speed) * differential;\n}","jsSource":"/**\n * Oscillate a value smoothly around a baseline using a sine wave.\n *\n * Pure and deterministic: pass the current `time` (e.g. `performance.now()`) and the\n * same inputs always produce the same output — so it's easy to test and reproduce.\n *\n * @param startingValue - The baseline the result oscillates around.\n * @param differential - The amplitude (how far above/below the baseline it swings).\n * @param speed - Oscillation speed multiplier applied to `time`.\n * @param time - The current time in milliseconds (e.g. `performance.now()`).\n * @returns The oscillating value at the given moment.\n * @example\n * const y = sineCurve(200, 50, 0.002, performance.now()); // bob around y=200 by ±50\n */\nfunction sineCurve(startingValue, differential, speed, time) {\n    return startingValue + Math.sin(time * speed) * differential;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"sineWave","module":"SineWave","tsSource":"/**\n * Sample the height of a sine wave at a horizontal position.\n *\n * The standard wave equation: `centerY + amplitude · sin(2π·(x/wavelength + phase))`.\n * Animate by advancing `phase` over time to make the wave travel.\n *\n * @param x - Horizontal position to sample.\n * @param centerY - The wave's rest (center) height.\n * @param amplitude - Peak height above/below center.\n * @param wavelength - Horizontal distance between successive peaks.\n * @param phase - Phase offset in wavelengths; advance over time to animate.\n * @returns The wave's y value at `x`.\n * @example\n * const y = sineWave(x, 200, 50, 120, time * 0.01);\n */\nfunction sineWave(x: number, centerY: number, amplitude: number, wavelength: number, phase: number): number {\n    return centerY + Math.sin((x / wavelength + phase) * Math.PI * 2) * amplitude;\n}","jsSource":"/**\n * Sample the height of a sine wave at a horizontal position.\n *\n * The standard wave equation: `centerY + amplitude · sin(2π·(x/wavelength + phase))`.\n * Animate by advancing `phase` over time to make the wave travel.\n *\n * @param x - Horizontal position to sample.\n * @param centerY - The wave's rest (center) height.\n * @param amplitude - Peak height above/below center.\n * @param wavelength - Horizontal distance between successive peaks.\n * @param phase - Phase offset in wavelengths; advance over time to animate.\n * @returns The wave's y value at `x`.\n * @example\n * const y = sineWave(x, 200, 50, 120, time * 0.01);\n */\nfunction sineWave(x, centerY, amplitude, wavelength, phase) {\n    return centerY + Math.sin((x / wavelength + phase) * Math.PI * 2) * amplitude;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"smootherstep","module":"Smoothstep","tsSource":"/**\n * Ken Perlin's smoother variant of {@link smoothstep}.\n *\n * Uses a 5th-order curve so both the 1st *and* 2nd derivatives are zero at the\n * edges — even less perceptible \"popping\" at the start and end of the transition.\n *\n * @param edge0 - Lower edge; output is 0 at or below this.\n * @param edge1 - Upper edge; output is 1 at or above this.\n * @param x - The value to interpolate.\n * @returns A very smoothly eased value in `[0, 1]`.\n * @example\n * smootherstep(0, 10, 5); // => 0.5\n */\nfunction smootherstep(edge0: number, edge1: number, x: number): number {\n    if (edge0 === edge1)\n        return x < edge0 ? 0 : 1;\n    const t = Math.min(1, Math.max(0, (x - edge0) / (edge1 - edge0)));\n    return t * t * t * (t * (t * 6 - 15) + 10);\n}","jsSource":"/**\n * Ken Perlin's smoother variant of {@link smoothstep}.\n *\n * Uses a 5th-order curve so both the 1st *and* 2nd derivatives are zero at the\n * edges — even less perceptible \"popping\" at the start and end of the transition.\n *\n * @param edge0 - Lower edge; output is 0 at or below this.\n * @param edge1 - Upper edge; output is 1 at or above this.\n * @param x - The value to interpolate.\n * @returns A very smoothly eased value in `[0, 1]`.\n * @example\n * smootherstep(0, 10, 5); // => 0.5\n */\nfunction smootherstep(edge0, edge1, x) {\n    if (edge0 === edge1)\n        return x < edge0 ? 0 : 1;\n    const t = Math.min(1, Math.max(0, (x - edge0) / (edge1 - edge0)));\n    return t * t * t * (t * (t * 6 - 15) + 10);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"smoothstep","module":"Smoothstep","tsSource":"/**\n * Smooth Hermite interpolation between two edges — the classic GLSL `smoothstep`.\n *\n * Returns 0 below `edge0`, 1 above `edge1`, and an eased S-curve in between (flat\n * at both ends), giving gentler transitions than a raw linear ramp.\n *\n * @param edge0 - Lower edge; output is 0 at or below this.\n * @param edge1 - Upper edge; output is 1 at or above this.\n * @param x - The value to interpolate.\n * @returns A smoothly eased value in `[0, 1]`.\n * @example\n * smoothstep(0, 10, 5); // => 0.5\n * smoothstep(0, 10, -2); // => 0\n */\nfunction smoothstep(edge0: number, edge1: number, x: number): number {\n    if (edge0 === edge1)\n        return x < edge0 ? 0 : 1;\n    const t = Math.min(1, Math.max(0, (x - edge0) / (edge1 - edge0)));\n    return t * t * (3 - 2 * t);\n}","jsSource":"/**\n * Smooth Hermite interpolation between two edges — the classic GLSL `smoothstep`.\n *\n * Returns 0 below `edge0`, 1 above `edge1`, and an eased S-curve in between (flat\n * at both ends), giving gentler transitions than a raw linear ramp.\n *\n * @param edge0 - Lower edge; output is 0 at or below this.\n * @param edge1 - Upper edge; output is 1 at or above this.\n * @param x - The value to interpolate.\n * @returns A smoothly eased value in `[0, 1]`.\n * @example\n * smoothstep(0, 10, 5); // => 0.5\n * smoothstep(0, 10, -2); // => 0\n */\nfunction smoothstep(edge0, edge1, x) {\n    if (edge0 === edge1)\n        return x < edge0 ? 0 : 1;\n    const t = Math.min(1, Math.max(0, (x - edge0) / (edge1 - edge0)));\n    return t * t * (3 - 2 * t);\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"sphereLighting","module":"SphereLighting","tsSource":"/**\n * Find where the specular highlight sits on a 2D sphere, given a light source.\n *\n * Points from the sphere's center toward the light and walks part of the way out\n * along the radius — a cheap way to fake a 3D-lit highlight on a flat circle.\n *\n * @param sphere - The sphere's center and radius.\n * @param lightSource - Position of the light.\n * @param highlightReach - How far toward the edge the highlight sits, 0–1. Defaults to `0.4`.\n * @returns The `{ x, y }` position to draw the highlight.\n * @example\n * const hi = sphereLighting({ x: 100, y: 100, radius: 40 }, { x: 0, y: 0 });\n */\nfunction sphereLighting(sphere: {\n    x: number;\n    y: number;\n    radius: number;\n}, lightSource: Point, highlightReach: number = 0.4): Point {\n    const angleToLight = Math.atan2(lightSource.y - sphere.y, lightSource.x - sphere.x);\n    return {\n        x: sphere.x + Math.cos(angleToLight) * sphere.radius * highlightReach,\n        y: sphere.y + Math.sin(angleToLight) * sphere.radius * highlightReach,\n    };\n}","jsSource":"/**\n * Find where the specular highlight sits on a 2D sphere, given a light source.\n *\n * Points from the sphere's center toward the light and walks part of the way out\n * along the radius — a cheap way to fake a 3D-lit highlight on a flat circle.\n *\n * @param sphere - The sphere's center and radius.\n * @param lightSource - Position of the light.\n * @param highlightReach - How far toward the edge the highlight sits, 0–1. Defaults to `0.4`.\n * @returns The `{ x, y }` position to draw the highlight.\n * @example\n * const hi = sphereLighting({ x: 100, y: 100, radius: 40 }, { x: 0, y: 0 });\n */\nfunction sphereLighting(sphere, lightSource, highlightReach = 0.4) {\n    const angleToLight = Math.atan2(lightSource.y - sphere.y, lightSource.x - sphere.x);\n    return {\n        x: sphere.x + Math.cos(angleToLight) * sphere.radius * highlightReach,\n        y: sphere.y + Math.sin(angleToLight) * sphere.radius * highlightReach,\n    };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"springValue","module":"Animate","tsSource":"/**\n * Advance a damped spring one fixed simulation step.\n *\n * @example\n * let s = { value: 0, velocity: 0 };\n * s = springValue(s, 100); // => state nudged toward 100; call each frame\n */\nfunction springValue(state: SpringState, target: number, { stiffness = 170, damping = 26, mass = 1, deltaSeconds = 1 / 60, }: SpringOptions = {}): SpringState {\n    const force = -stiffness * (state.value - target);\n    const damper = -damping * state.velocity;\n    const acceleration = (force + damper) / mass;\n    const velocity = state.velocity + acceleration * deltaSeconds;\n    const value = state.value + velocity * deltaSeconds;\n    return { value, velocity };\n}","jsSource":"/**\n * Advance a damped spring one fixed simulation step.\n *\n * @example\n * let s = { value: 0, velocity: 0 };\n * s = springValue(s, 100); // => state nudged toward 100; call each frame\n */\nfunction springValue(state, target, { stiffness = 170, damping = 26, mass = 1, deltaSeconds = 1 / 60, } = {}) {\n    const force = -stiffness * (state.value - target);\n    const damper = -damping * state.velocity;\n    const acceleration = (force + damper) / mass;\n    const velocity = state.velocity + acceleration * deltaSeconds;\n    const value = state.value + velocity * deltaSeconds;\n    return { value, velocity };\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"stagger","module":"Animate","tsSource":"/**\n * Return the delayed progress for an item in a staggered list.\n *\n * @example\n * stagger(2, 600, 500, 200); // => 0.4 (item 2 starts 400ms in)\n */\nfunction stagger(index: number, elapsedMs: number, durationMs: number, staggerMs: number): number {\n    return delay(elapsedMs, Math.max(0, index) * staggerMs, durationMs);\n}","jsSource":"/**\n * Return the delayed progress for an item in a staggered list.\n *\n * @example\n * stagger(2, 600, 500, 200); // => 0.4 (item 2 starts 400ms in)\n */\nfunction stagger(index, elapsedMs, durationMs, staggerMs) {\n    return delay(elapsedMs, Math.max(0, index) * staggerMs, durationMs);\n}","dependencyKeys":["delay"],"interfaceNames":[]},
  {"key":"starVertices","module":"Star","tsSource":"/**\n * Compute the vertices of a star polygon, alternating between an outer and inner radius.\n *\n * @param spikes - Number of points on the star.\n * @param innerRadius - Radius of the inner (valley) vertices.\n * @param outerRadius - Radius of the outer (tip) vertices.\n * @param angle - Starting rotation in radians. Defaults to `0`.\n * @param options - Optional spin animation: `{ rotate, rotateSpeed, clockwise, time }`. When\n *   `rotate` is true the angle advances with `options.time` (ms, e.g. `performance.now()`),\n *   keeping the function pure — pass the time in rather than reading the clock here.\n * @returns `{ vertices }` — `spikes * 2` points, alternating outer then inner.\n * @example\n * starVertices(5, 5, 10).vertices.length; // => 10\n */\nfunction starVertices(spikes: number, innerRadius: number, outerRadius: number, angle: number = 0, options: {\n    rotate?: boolean;\n    rotateSpeed?: number;\n    clockwise?: boolean;\n    time?: number;\n} = { rotate: false, rotateSpeed: 1000, clockwise: true }): {\n    vertices: Point[];\n} {\n    let vertices: Point[] = [];\n    let rot = 0;\n    let step = Math.PI / spikes;\n    const rotate = options.rotate ?? false;\n    const rotateSpeed = options.rotateSpeed ?? 1000;\n    const clockwise = options.clockwise ?? true;\n    const time = options.time ?? 0;\n    let rotateQ = rotate\n        ? time / rotateSpeed\n        : 0;\n    if (!clockwise)\n        rotateQ *= -1;\n    for (let i = 0; i < spikes; i++) {\n        let x = Math.cos(angle + rot + rotateQ) * outerRadius;\n        let y = Math.sin(angle + rot + rotateQ) * outerRadius;\n        vertices.push({ x, y });\n        rot += step;\n        x = Math.cos(angle + rot + rotateQ) * innerRadius;\n        y = Math.sin(angle + rot + rotateQ) * innerRadius;\n        vertices.push({ x, y });\n        rot += step;\n    }\n    return { vertices };\n}","jsSource":"/**\n * Compute the vertices of a star polygon, alternating between an outer and inner radius.\n *\n * @param spikes - Number of points on the star.\n * @param innerRadius - Radius of the inner (valley) vertices.\n * @param outerRadius - Radius of the outer (tip) vertices.\n * @param angle - Starting rotation in radians. Defaults to `0`.\n * @param options - Optional spin animation: `{ rotate, rotateSpeed, clockwise, time }`. When\n *   `rotate` is true the angle advances with `options.time` (ms, e.g. `performance.now()`),\n *   keeping the function pure — pass the time in rather than reading the clock here.\n * @returns `{ vertices }` — `spikes * 2` points, alternating outer then inner.\n * @example\n * starVertices(5, 5, 10).vertices.length; // => 10\n */\nfunction starVertices(spikes, innerRadius, outerRadius, angle = 0, options = { rotate: false, rotateSpeed: 1000, clockwise: true }) {\n    let vertices = [];\n    let rot = 0;\n    let step = Math.PI / spikes;\n    const rotate = options.rotate ?? false;\n    const rotateSpeed = options.rotateSpeed ?? 1000;\n    const clockwise = options.clockwise ?? true;\n    const time = options.time ?? 0;\n    let rotateQ = rotate\n        ? time / rotateSpeed\n        : 0;\n    if (!clockwise)\n        rotateQ *= -1;\n    for (let i = 0; i < spikes; i++) {\n        let x = Math.cos(angle + rot + rotateQ) * outerRadius;\n        let y = Math.sin(angle + rot + rotateQ) * outerRadius;\n        vertices.push({ x, y });\n        rot += step;\n        x = Math.cos(angle + rot + rotateQ) * innerRadius;\n        y = Math.sin(angle + rot + rotateQ) * innerRadius;\n        vertices.push({ x, y });\n        rot += step;\n    }\n    return { vertices };\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"triangleDataFromLine","module":"GetTriangleData","tsSource":"/**\n * Solve the right triangle formed by a line and its horizontal/vertical legs.\n *\n * Treats the line as the hypotenuse and derives the side lengths and angles via\n * basic SOH-CAH-TOA trigonometry.\n *\n * @param startPoint - One end of the line.\n * @param endPoint - The other end of the line.\n * @returns `{ angleInDegrees, remainingAngle, hypotenuse, adjacent, opposite }`.\n * @example\n * triangleDataFromLine({ x: 0, y: 0 }, { x: 4, y: 3 });\n * // => { angleInDegrees: 36, remainingAngle: 54, hypotenuse: 5, adjacent: 4, opposite: 3 }\n */\nfunction triangleDataFromLine(startPoint: Point, endPoint: Point): {\n    angleInDegrees: number;\n    remainingAngle: number;\n    hypotenuse: number;\n    adjacent: number;\n    opposite: number;\n} {\n    let hypotenuse = lineLength({ startPoint, endPoint });\n    let adjacent = lineLength({\n        startPoint,\n        endPoint: {\n            x: endPoint.x,\n            y: startPoint.y,\n        },\n    });\n    let opposite = lineLength({\n        startPoint: {\n            x: endPoint.x,\n            y: startPoint.y,\n        },\n        endPoint,\n    });\n    let oh = opposite / hypotenuse;\n    let angle1 = Math.asin(oh);\n    let angleInDegrees = Math.floor(angle1 * (180 / Math.PI));\n    let remainingAngle = 180 - angleInDegrees - 90;\n    return { angleInDegrees, remainingAngle, hypotenuse, adjacent, opposite };\n}","jsSource":"/**\n * Solve the right triangle formed by a line and its horizontal/vertical legs.\n *\n * Treats the line as the hypotenuse and derives the side lengths and angles via\n * basic SOH-CAH-TOA trigonometry.\n *\n * @param startPoint - One end of the line.\n * @param endPoint - The other end of the line.\n * @returns `{ angleInDegrees, remainingAngle, hypotenuse, adjacent, opposite }`.\n * @example\n * triangleDataFromLine({ x: 0, y: 0 }, { x: 4, y: 3 });\n * // => { angleInDegrees: 36, remainingAngle: 54, hypotenuse: 5, adjacent: 4, opposite: 3 }\n */\nfunction triangleDataFromLine(startPoint, endPoint) {\n    let hypotenuse = lineLength({ startPoint, endPoint });\n    let adjacent = lineLength({\n        startPoint,\n        endPoint: {\n            x: endPoint.x,\n            y: startPoint.y,\n        },\n    });\n    let opposite = lineLength({\n        startPoint: {\n            x: endPoint.x,\n            y: startPoint.y,\n        },\n        endPoint,\n    });\n    let oh = opposite / hypotenuse;\n    let angle1 = Math.asin(oh);\n    let angleInDegrees = Math.floor(angle1 * (180 / Math.PI));\n    let remainingAngle = 180 - angleInDegrees - 90;\n    return { angleInDegrees, remainingAngle, hypotenuse, adjacent, opposite };\n}","dependencyKeys":["lineLength"],"interfaceNames":["Point"]},
  {"key":"tweenFrame","module":"Animate","tsSource":"/**\n * Alias for tweenObject when you want to sample animation state one frame at a time.\n *\n * @example\n * tweenFrame({ x: { from: 0, to: 10 } }, 500, 500); // => { x: 10 }\n */\nconst tweenFrame = tweenObject;","jsSource":"/**\n * Alias for tweenObject when you want to sample animation state one frame at a time.\n *\n * @example\n * tweenFrame({ x: { from: 0, to: 10 } }, 500, 500); // => { x: 10 }\n */\nconst tweenFrame = tweenObject;","dependencyKeys":["tweenObject"],"interfaceNames":[]},
  {"key":"tweenObject","module":"Animate","tsSource":"/**\n * Return every numeric property of an object tween at a specific elapsed time.\n *\n * @example\n * tweenObject({ x: { from: 0, to: 10 } }, 250, 500); // => { x: 5 }\n */\nfunction tweenObject<T extends Record<string, number>>(spec: TweenObjectSpec<T>, elapsedMs: number, durationMs: number, easingOrOptions: EasingFunction | TweenValueOptions = linear): T {\n    const out = {} as T;\n    for (const key of Object.keys(spec) as Array<keyof T>) {\n        const range = spec[key];\n        out[key] = tweenValue(range.from, range.to, elapsedMs, durationMs, easingOrOptions) as T[keyof T];\n    }\n    return out;\n}","jsSource":"/**\n * Return every numeric property of an object tween at a specific elapsed time.\n *\n * @example\n * tweenObject({ x: { from: 0, to: 10 } }, 250, 500); // => { x: 5 }\n */\nfunction tweenObject(spec, elapsedMs, durationMs, easingOrOptions = linear) {\n    const out = {};\n    for (const key of Object.keys(spec)) {\n        const range = spec[key];\n        out[key] = tweenValue(range.from, range.to, elapsedMs, durationMs, easingOrOptions);\n    }\n    return out;\n}","dependencyKeys":["linear","tweenValue"],"interfaceNames":[]},
  {"key":"tweenValue","module":"Animate","tsSource":"/**\n * Return the value of a scalar tween at a specific elapsed time.\n *\n * @example\n * tweenValue(0, 100, 250, 500); // => 50 (halfway through, linear)\n */\nfunction tweenValue(from: number, to: number, elapsedMs: number, durationMs: number, easingOrOptions: EasingFunction | TweenValueOptions = linear): number {\n    const options = typeof easingOrOptions === 'function' ? { easing: easingOrOptions } : easingOrOptions;\n    const easing = options.easing ?? linear;\n    const shouldClamp = options.clamp ?? true;\n    const t = durationMs <= 0 ? 1 : elapsedMs / durationMs;\n    const progress = shouldClamp ? clamp(t, 0, 1) : t;\n    return lerp(from, to, easing(progress));\n}","jsSource":"/**\n * Return the value of a scalar tween at a specific elapsed time.\n *\n * @example\n * tweenValue(0, 100, 250, 500); // => 50 (halfway through, linear)\n */\nfunction tweenValue(from, to, elapsedMs, durationMs, easingOrOptions = linear) {\n    const options = typeof easingOrOptions === 'function' ? { easing: easingOrOptions } : easingOrOptions;\n    const easing = options.easing ?? linear;\n    const shouldClamp = options.clamp ?? true;\n    const t = durationMs <= 0 ? 1 : elapsedMs / durationMs;\n    const progress = shouldClamp ? clamp(t, 0, 1) : t;\n    return lerp(from, to, easing(progress));\n}","dependencyKeys":["clamp","lerp","linear"],"interfaceNames":[]},
  {"key":"unitCirclePoint","module":"UnitCirclePoint","tsSource":"/**\n * Get the point on a circle at a given angle, along with the raw cosine/sine.\n *\n * Returning `cos`/`sin` too saves the caller recomputing them (e.g. to orient\n * something tangent to the circle).\n *\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param radius - Circle radius.\n * @param time - Angle in radians (named `time` since it's usually driven by a clock).\n * @returns `{ x, y, cos, sin }` — the point and the trig components used to find it.\n * @example\n * unitCirclePoint(100, 100, 50, 0); // => { x: 150, y: 100, cos: 1, sin: 0 }\n */\nfunction unitCirclePoint(cx: number, cy: number, radius: number, time: number): {\n    x: number;\n    y: number;\n    cos: number;\n    sin: number;\n} {\n    let cos = Math.cos(time), sin = Math.sin(time);\n    return { x: cx + radius * cos, y: cy + radius * sin, cos, sin };\n}","jsSource":"/**\n * Get the point on a circle at a given angle, along with the raw cosine/sine.\n *\n * Returning `cos`/`sin` too saves the caller recomputing them (e.g. to orient\n * something tangent to the circle).\n *\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param radius - Circle radius.\n * @param time - Angle in radians (named `time` since it's usually driven by a clock).\n * @returns `{ x, y, cos, sin }` — the point and the trig components used to find it.\n * @example\n * unitCirclePoint(100, 100, 50, 0); // => { x: 150, y: 100, cos: 1, sin: 0 }\n */\nfunction unitCirclePoint(cx, cy, radius, time) {\n    let cos = Math.cos(time), sin = Math.sin(time);\n    return { x: cx + radius * cos, y: cy + radius * sin, cos, sin };\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"vecAdd","module":"Vec2","tsSource":"// 2D vector operations. All functions are pure and return a brand-new vector\n// (the inputs are never mutated), so they compose freely. Names are `vec`-prefixed\n// to stay unambiguous in the flat `@utilspalooza/core` namespace.\n/**\n * Add two vectors component-wise.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns A new vector `{ x: a.x + b.x, y: a.y + b.y }`.\n * @example\n * vecAdd({ x: 1, y: 2 }, { x: 3, y: 4 }); // => { x: 4, y: 6 }\n */\nfunction vecAdd(a: Vector, b: Vector): Vector {\n    return { x: a.x + b.x, y: a.y + b.y };\n}","jsSource":"// 2D vector operations. All functions are pure and return a brand-new vector\n// (the inputs are never mutated), so they compose freely. Names are `vec`-prefixed\n// to stay unambiguous in the flat `@utilspalooza/core` namespace.\n/**\n * Add two vectors component-wise.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns A new vector `{ x: a.x + b.x, y: a.y + b.y }`.\n * @example\n * vecAdd({ x: 1, y: 2 }, { x: 3, y: 4 }); // => { x: 4, y: 6 }\n */\nfunction vecAdd(a, b) {\n    return { x: a.x + b.x, y: a.y + b.y };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecAngle","module":"Vec2","tsSource":"/**\n * The angle of a vector in radians, measured counter-clockwise from the positive x-axis.\n *\n * @param v - The vector.\n * @returns The angle in radians, in the range `-π`…`π` (via `Math.atan2`).\n * @example\n * vecAngle({ x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction vecAngle(v: Vector): number {\n    return Math.atan2(v.y, v.x);\n}","jsSource":"/**\n * The angle of a vector in radians, measured counter-clockwise from the positive x-axis.\n *\n * @param v - The vector.\n * @returns The angle in radians, in the range `-π`…`π` (via `Math.atan2`).\n * @example\n * vecAngle({ x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction vecAngle(v) {\n    return Math.atan2(v.y, v.x);\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecAngleBetween","module":"Vec2","tsSource":"/**\n * The (unsigned) angle between two vectors.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns The angle in radians, in the range `0`…`π`. Returns `0` if either vector is zero-length.\n * @example\n * vecAngleBetween({ x: 1, y: 0 }, { x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction vecAngleBetween(a: Vector, b: Vector): number {\n    const mags = Math.hypot(a.x, a.y) * Math.hypot(b.x, b.y);\n    if (mags === 0)\n        return 0;\n    // clamp guards against tiny floating-point overshoot beyond [-1, 1]\n    const cos = Math.max(-1, Math.min(1, (a.x * b.x + a.y * b.y) / mags));\n    return Math.acos(cos);\n}","jsSource":"/**\n * The (unsigned) angle between two vectors.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns The angle in radians, in the range `0`…`π`. Returns `0` if either vector is zero-length.\n * @example\n * vecAngleBetween({ x: 1, y: 0 }, { x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction vecAngleBetween(a, b) {\n    const mags = Math.hypot(a.x, a.y) * Math.hypot(b.x, b.y);\n    if (mags === 0)\n        return 0;\n    // clamp guards against tiny floating-point overshoot beyond [-1, 1]\n    const cos = Math.max(-1, Math.min(1, (a.x * b.x + a.y * b.y) / mags));\n    return Math.acos(cos);\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecCross","module":"Vec2","tsSource":"/**\n * 2D cross product — the z-component of the 3D cross, a single scalar.\n *\n * Its sign tells you the turn direction from `a` to `b` (positive = counter-clockwise),\n * and its magnitude is the area of the parallelogram they span.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns The scalar `a.x * b.y - a.y * b.x`.\n * @example\n * vecCross({ x: 1, y: 0 }, { x: 0, y: 1 }); // => 1\n */\nfunction vecCross(a: Vector, b: Vector): number {\n    return a.x * b.y - a.y * b.x;\n}","jsSource":"/**\n * 2D cross product — the z-component of the 3D cross, a single scalar.\n *\n * Its sign tells you the turn direction from `a` to `b` (positive = counter-clockwise),\n * and its magnitude is the area of the parallelogram they span.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns The scalar `a.x * b.y - a.y * b.x`.\n * @example\n * vecCross({ x: 1, y: 0 }, { x: 0, y: 1 }); // => 1\n */\nfunction vecCross(a, b) {\n    return a.x * b.y - a.y * b.x;\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecDot","module":"Vec2","tsSource":"/**\n * Dot product of two vectors.\n *\n * A measure of how much the vectors point the same way: positive when roughly\n * aligned, zero when perpendicular, negative when opposed. The basis for projection.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns The scalar `a.x * b.x + a.y * b.y`.\n * @example\n * vecDot({ x: 1, y: 0 }, { x: 0, y: 1 }); // => 0 (perpendicular)\n */\nfunction vecDot(a: Vector, b: Vector): number {\n    return a.x * b.x + a.y * b.y;\n}","jsSource":"/**\n * Dot product of two vectors.\n *\n * A measure of how much the vectors point the same way: positive when roughly\n * aligned, zero when perpendicular, negative when opposed. The basis for projection.\n *\n * @param a - First vector.\n * @param b - Second vector.\n * @returns The scalar `a.x * b.x + a.y * b.y`.\n * @example\n * vecDot({ x: 1, y: 0 }, { x: 0, y: 1 }); // => 0 (perpendicular)\n */\nfunction vecDot(a, b) {\n    return a.x * b.x + a.y * b.y;\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecLerp","module":"Vec2","tsSource":"/**\n * Linearly interpolate between two vectors (lerp in 2D).\n *\n * @param a - Start vector, returned when `t` is 0.\n * @param b - End vector, returned when `t` is 1.\n * @param t - Interpolation factor; `0.5` is the midpoint.\n * @returns A new vector partway between `a` and `b`.\n * @example\n * vecLerp({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction vecLerp(a: Vector, b: Vector, t: number): Vector {\n    return { x: a.x + (b.x - a.x) * t, y: a.y + (b.y - a.y) * t };\n}","jsSource":"/**\n * Linearly interpolate between two vectors (lerp in 2D).\n *\n * @param a - Start vector, returned when `t` is 0.\n * @param b - End vector, returned when `t` is 1.\n * @param t - Interpolation factor; `0.5` is the midpoint.\n * @returns A new vector partway between `a` and `b`.\n * @example\n * vecLerp({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction vecLerp(a, b, t) {\n    return { x: a.x + (b.x - a.x) * t, y: a.y + (b.y - a.y) * t };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecLimit","module":"Vec2","tsSource":"/**\n * Clamp a vector's magnitude to at most `max`, preserving its direction.\n *\n * Common for capping a velocity to a maximum speed.\n *\n * @param v - The vector to limit.\n * @param max - The maximum allowed length.\n * @returns A new vector no longer than `max` (a copy of `v` if it was already shorter).\n * @example\n * vecLimit({ x: 30, y: 40 }, 10); // => { x: 6, y: 8 } (was length 50, now length 10)\n */\nfunction vecLimit(v: Vector, max: number): Vector {\n    const m = Math.hypot(v.x, v.y);\n    if (m > max && m > 0) {\n        return { x: (v.x / m) * max, y: (v.y / m) * max };\n    }\n    return { x: v.x, y: v.y };\n}","jsSource":"/**\n * Clamp a vector's magnitude to at most `max`, preserving its direction.\n *\n * Common for capping a velocity to a maximum speed.\n *\n * @param v - The vector to limit.\n * @param max - The maximum allowed length.\n * @returns A new vector no longer than `max` (a copy of `v` if it was already shorter).\n * @example\n * vecLimit({ x: 30, y: 40 }, 10); // => { x: 6, y: 8 } (was length 50, now length 10)\n */\nfunction vecLimit(v, max) {\n    const m = Math.hypot(v.x, v.y);\n    if (m > max && m > 0) {\n        return { x: (v.x / m) * max, y: (v.y / m) * max };\n    }\n    return { x: v.x, y: v.y };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecMagnitude","module":"Vec2","tsSource":"/**\n * Length (magnitude) of a vector.\n *\n * @param v - The vector.\n * @returns Its length, `√(x² + y²)`.\n * @example\n * vecMagnitude({ x: 3, y: 4 }); // => 5\n */\nfunction vecMagnitude(v: Vector): number {\n    return Math.hypot(v.x, v.y);\n}","jsSource":"/**\n * Length (magnitude) of a vector.\n *\n * @param v - The vector.\n * @returns Its length, `√(x² + y²)`.\n * @example\n * vecMagnitude({ x: 3, y: 4 }); // => 5\n */\nfunction vecMagnitude(v) {\n    return Math.hypot(v.x, v.y);\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecMagnitudeSquared","module":"Vec2","tsSource":"/**\n * Squared length of a vector — cheaper than {@link vecMagnitude} (skips the square root).\n *\n * Use this when you only need to *compare* lengths (e.g. \"which is closer?\"), since\n * comparing squared lengths gives the same answer without the costly `sqrt`.\n *\n * @param v - The vector.\n * @returns `x² + y²`.\n * @example\n * vecMagnitudeSquared({ x: 3, y: 4 }); // => 25\n */\nfunction vecMagnitudeSquared(v: Vector): number {\n    return v.x * v.x + v.y * v.y;\n}","jsSource":"/**\n * Squared length of a vector — cheaper than {@link vecMagnitude} (skips the square root).\n *\n * Use this when you only need to *compare* lengths (e.g. \"which is closer?\"), since\n * comparing squared lengths gives the same answer without the costly `sqrt`.\n *\n * @param v - The vector.\n * @returns `x² + y²`.\n * @example\n * vecMagnitudeSquared({ x: 3, y: 4 }); // => 25\n */\nfunction vecMagnitudeSquared(v) {\n    return v.x * v.x + v.y * v.y;\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecNormalize","module":"Vec2","tsSource":"/**\n * Normalize a vector to length 1, preserving its direction (a \"unit vector\").\n *\n * @param v - The vector to normalize.\n * @returns A new unit vector, or `{ x: 0, y: 0 }` if `v` has zero length.\n * @example\n * vecNormalize({ x: 0, y: 5 }); // => { x: 0, y: 1 }\n */\nfunction vecNormalize(v: Vector): Vector {\n    const m = Math.hypot(v.x, v.y);\n    return m === 0 ? { x: 0, y: 0 } : { x: v.x / m, y: v.y / m };\n}","jsSource":"/**\n * Normalize a vector to length 1, preserving its direction (a \"unit vector\").\n *\n * @param v - The vector to normalize.\n * @returns A new unit vector, or `{ x: 0, y: 0 }` if `v` has zero length.\n * @example\n * vecNormalize({ x: 0, y: 5 }); // => { x: 0, y: 1 }\n */\nfunction vecNormalize(v) {\n    const m = Math.hypot(v.x, v.y);\n    return m === 0 ? { x: 0, y: 0 } : { x: v.x / m, y: v.y / m };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecPerpendicular","module":"Vec2","tsSource":"/**\n * A vector perpendicular to the given one (rotated 90° counter-clockwise).\n *\n * Handy for finding a surface normal from an edge.\n *\n * @param v - The vector.\n * @returns A new vector `{ x: -v.y, y: v.x }`.\n * @example\n * vecPerpendicular({ x: 1, y: 0 }); // => { x: 0, y: 1 }\n */\nfunction vecPerpendicular(v: Vector): Vector {\n    return { x: -v.y, y: v.x };\n}","jsSource":"/**\n * A vector perpendicular to the given one (rotated 90° counter-clockwise).\n *\n * Handy for finding a surface normal from an edge.\n *\n * @param v - The vector.\n * @returns A new vector `{ x: -v.y, y: v.x }`.\n * @example\n * vecPerpendicular({ x: 1, y: 0 }); // => { x: 0, y: 1 }\n */\nfunction vecPerpendicular(v) {\n    return { x: -v.y, y: v.x };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecReflect","module":"Vec2","tsSource":"/**\n * Reflect a vector off a surface, like a ball bouncing off a wall.\n *\n * @param v - The incoming vector (e.g. a velocity).\n * @param normal - The surface normal. Should be a unit vector; normalize it first if unsure.\n * @returns The reflected vector, `v - 2 (v · normal) normal`.\n * @example\n * vecReflect({ x: 1, y: -1 }, { x: 0, y: 1 }); // => { x: 1, y: 1 } (bounce off the floor)\n */\nfunction vecReflect(v: Vector, normal: Vector): Vector {\n    const d = 2 * (v.x * normal.x + v.y * normal.y);\n    return { x: v.x - d * normal.x, y: v.y - d * normal.y };\n}","jsSource":"/**\n * Reflect a vector off a surface, like a ball bouncing off a wall.\n *\n * @param v - The incoming vector (e.g. a velocity).\n * @param normal - The surface normal. Should be a unit vector; normalize it first if unsure.\n * @returns The reflected vector, `v - 2 (v · normal) normal`.\n * @example\n * vecReflect({ x: 1, y: -1 }, { x: 0, y: 1 }); // => { x: 1, y: 1 } (bounce off the floor)\n */\nfunction vecReflect(v, normal) {\n    const d = 2 * (v.x * normal.x + v.y * normal.y);\n    return { x: v.x - d * normal.x, y: v.y - d * normal.y };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecRotate","module":"Vec2","tsSource":"/**\n * Rotate a vector by an angle (counter-clockwise in standard math coordinates).\n *\n * @param v - The vector to rotate.\n * @param radians - The rotation angle in radians.\n * @returns A new, rotated vector of the same length.\n * @example\n * vecRotate({ x: 1, y: 0 }, Math.PI / 2); // => { x: ~0, y: 1 }\n */\nfunction vecRotate(v: Vector, radians: number): Vector {\n    const c = Math.cos(radians);\n    const s = Math.sin(radians);\n    return { x: v.x * c - v.y * s, y: v.x * s + v.y * c };\n}","jsSource":"/**\n * Rotate a vector by an angle (counter-clockwise in standard math coordinates).\n *\n * @param v - The vector to rotate.\n * @param radians - The rotation angle in radians.\n * @returns A new, rotated vector of the same length.\n * @example\n * vecRotate({ x: 1, y: 0 }, Math.PI / 2); // => { x: ~0, y: 1 }\n */\nfunction vecRotate(v, radians) {\n    const c = Math.cos(radians);\n    const s = Math.sin(radians);\n    return { x: v.x * c - v.y * s, y: v.x * s + v.y * c };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecScale","module":"Vec2","tsSource":"/**\n * Scale a vector by a scalar (stretch or shrink it, keeping direction unless negative).\n *\n * @param v - The vector to scale.\n * @param s - The scalar multiplier.\n * @returns A new vector `{ x: v.x * s, y: v.y * s }`.\n * @example\n * vecScale({ x: 3, y: 4 }, 2); // => { x: 6, y: 8 }\n */\nfunction vecScale(v: Vector, s: number): Vector {\n    return { x: v.x * s, y: v.y * s };\n}","jsSource":"/**\n * Scale a vector by a scalar (stretch or shrink it, keeping direction unless negative).\n *\n * @param v - The vector to scale.\n * @param s - The scalar multiplier.\n * @returns A new vector `{ x: v.x * s, y: v.y * s }`.\n * @example\n * vecScale({ x: 3, y: 4 }, 2); // => { x: 6, y: 8 }\n */\nfunction vecScale(v, s) {\n    return { x: v.x * s, y: v.y * s };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"vecSubtract","module":"Vec2","tsSource":"/**\n * Subtract vector `b` from `a` — the displacement pointing from `b` to `a`.\n *\n * @param a - The vector to subtract from.\n * @param b - The vector being subtracted.\n * @returns A new vector `{ x: a.x - b.x, y: a.y - b.y }`.\n * @example\n * vecSubtract({ x: 5, y: 5 }, { x: 1, y: 2 }); // => { x: 4, y: 3 }\n */\nfunction vecSubtract(a: Vector, b: Vector): Vector {\n    return { x: a.x - b.x, y: a.y - b.y };\n}","jsSource":"/**\n * Subtract vector `b` from `a` — the displacement pointing from `b` to `a`.\n *\n * @param a - The vector to subtract from.\n * @param b - The vector being subtracted.\n * @returns A new vector `{ x: a.x - b.x, y: a.y - b.y }`.\n * @example\n * vecSubtract({ x: 5, y: 5 }, { x: 1, y: 2 }); // => { x: 4, y: 3 }\n */\nfunction vecSubtract(a, b) {\n    return { x: a.x - b.x, y: a.y - b.y };\n}","dependencyKeys":[],"interfaceNames":["Vector"]},
  {"key":"waveAmplitude","module":"WaveAmplitude","tsSource":"/**\n * Compute the combined wave height at a point from multiple ripple sources (interference).\n *\n * Sums a traveling cosine wave radiating from each source and averages them, so\n * overlapping waves reinforce (bright) or cancel (dark) — the two-slit interference\n * pattern. `k` is the spatial frequency (2π/wavelength), `omega` the temporal frequency.\n *\n * @param x - Sample point x.\n * @param y - Sample point y.\n * @param sources - The wave-emitting source points.\n * @param time - Current time, advanced each frame to animate the waves.\n * @param k - Wave number (spatial frequency).\n * @param omega - Angular frequency (temporal).\n * @returns The averaged wave amplitude at the point, roughly `-1`…`1`.\n * @example\n * const a = waveAmplitude(x, y, [s1, s2], time, 0.05, 0.1);\n */\nfunction waveAmplitude(x: number, y: number, sources: Point[], time: number, k: number, omega: number): number {\n    let amp = 0;\n    for (let s of sources) {\n        let dx = x - s.x, dy = y - s.y;\n        amp += Math.cos(k * Math.sqrt(dx * dx + dy * dy) - omega * time);\n    }\n    return amp / sources.length;\n}","jsSource":"/**\n * Compute the combined wave height at a point from multiple ripple sources (interference).\n *\n * Sums a traveling cosine wave radiating from each source and averages them, so\n * overlapping waves reinforce (bright) or cancel (dark) — the two-slit interference\n * pattern. `k` is the spatial frequency (2π/wavelength), `omega` the temporal frequency.\n *\n * @param x - Sample point x.\n * @param y - Sample point y.\n * @param sources - The wave-emitting source points.\n * @param time - Current time, advanced each frame to animate the waves.\n * @param k - Wave number (spatial frequency).\n * @param omega - Angular frequency (temporal).\n * @returns The averaged wave amplitude at the point, roughly `-1`…`1`.\n * @example\n * const a = waveAmplitude(x, y, [s1, s2], time, 0.05, 0.1);\n */\nfunction waveAmplitude(x, y, sources, time, k, omega) {\n    let amp = 0;\n    for (let s of sources) {\n        let dx = x - s.x, dy = y - s.y;\n        amp += Math.cos(k * Math.sqrt(dx * dx + dy * dy) - omega * time);\n    }\n    return amp / sources.length;\n}","dependencyKeys":[],"interfaceNames":["Point"]},
  {"key":"wrap","module":"Wrap","tsSource":"/**\n * Wrap a value into the half-open range `[min, max)`, looping around at the ends.\n *\n * Like a modulo that respects an arbitrary lower bound — values that run off one\n * end reappear at the other (think Asteroids-style screen wrap, or cyclic indices).\n *\n * @param value - The value to wrap.\n * @param min - Lower bound (inclusive).\n * @param max - Upper bound (exclusive).\n * @returns `value` mapped into `[min, max)`. Returns `min` if the range is empty.\n * @example\n * wrap(11, 0, 10); // => 1\n * wrap(-1, 0, 10); // => 9\n */\nfunction wrap(value: number, min: number, max: number): number {\n    const range = max - min;\n    if (range <= 0)\n        return min;\n    return ((((value - min) % range) + range) % range) + min;\n}","jsSource":"/**\n * Wrap a value into the half-open range `[min, max)`, looping around at the ends.\n *\n * Like a modulo that respects an arbitrary lower bound — values that run off one\n * end reappear at the other (think Asteroids-style screen wrap, or cyclic indices).\n *\n * @param value - The value to wrap.\n * @param min - Lower bound (inclusive).\n * @param max - Upper bound (exclusive).\n * @returns `value` mapped into `[min, max)`. Returns `min` if the range is empty.\n * @example\n * wrap(11, 0, 10); // => 1\n * wrap(-1, 0, 10); // => 9\n */\nfunction wrap(value, min, max) {\n    const range = max - min;\n    if (range <= 0)\n        return min;\n    return ((((value - min) % range) + range) % range) + min;\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"wrapAngle","module":"AngleInterpolation","tsSource":"/**\n * Normalize an angle to the range `(-π, π]`.\n *\n * Collapses any angle (including ones that have wound past a full turn) to its\n * canonical equivalent, so `3π` and `π` are treated as the same direction.\n *\n * @param radians - The angle to normalize, in radians.\n * @returns The equivalent angle in `(-π, π]`.\n * @example\n * wrapAngle(3 * Math.PI); // => ~3.14159 (π)\n */\nfunction wrapAngle(radians: number): number {\n    return Math.atan2(Math.sin(radians), Math.cos(radians));\n}","jsSource":"/**\n * Normalize an angle to the range `(-π, π]`.\n *\n * Collapses any angle (including ones that have wound past a full turn) to its\n * canonical equivalent, so `3π` and `π` are treated as the same direction.\n *\n * @param radians - The angle to normalize, in radians.\n * @returns The equivalent angle in `(-π, π]`.\n * @example\n * wrapAngle(3 * Math.PI); // => ~3.14159 (π)\n */\nfunction wrapAngle(radians) {\n    return Math.atan2(Math.sin(radians), Math.cos(radians));\n}","dependencyKeys":[],"interfaceNames":[]},
  {"key":"yoyo","module":"Animate","tsSource":"/**\n * Map elapsed time into a reversing 0..1..0 progress value.\n *\n * @example\n * yoyo(500, 500); // => 1 (at the turnaround peak)\n */\nfunction yoyo(elapsedMs: number, durationMs: number): number {\n    if (durationMs <= 0)\n        return 1;\n    const cycle = loop(elapsedMs, durationMs * 2);\n    return cycle <= 0.5 ? cycle * 2 : (1 - cycle) * 2;\n}","jsSource":"/**\n * Map elapsed time into a reversing 0..1..0 progress value.\n *\n * @example\n * yoyo(500, 500); // => 1 (at the turnaround peak)\n */\nfunction yoyo(elapsedMs, durationMs) {\n    if (durationMs <= 0)\n        return 1;\n    const cycle = loop(elapsedMs, durationMs * 2);\n    return cycle <= 0.5 ? cycle * 2 : (1 - cycle) * 2;\n}","dependencyKeys":["loop"],"interfaceNames":[]},
];

// Core exports that are deliberately NOT standalone snippets, with the
// reason. The catalog test asserts entries + exclusions === core-api.json.
export const EXPORT_CATALOG_EXCLUDED: Record<string, string> = {
  "Ball": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Boid": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "BoidsOptions": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Circle": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Container": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "EasingFunction": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Flock": "Stateful convenience class wrapping boidsStep with its own RNG-seeded flock state — install @utilspalooza/core to use it; the copyable math is boidsStep itself.",
  "FlockBounds": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "HSL": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "HueFamily": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Line": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Point": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Polygon": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "RGB": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "ShapeInMotion": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "SpringOptions": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "SpringState": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "TickerFrame": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "TickerHandle": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "TweenObjectSpec": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "TweenValueOptions": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "Vector": "Type-only export — no runtime snippet; shared shapes ship in the TypeScript output's interface block instead.",
  "ticker": "requestAnimationFrame scheduler with environment fallbacks (the ONE deliberate clock exception in core) — a lifecycle convenience for package consumers, not a standalone math snippet."
};
