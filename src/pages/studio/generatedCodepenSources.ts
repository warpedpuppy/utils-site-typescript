// AUTO-GENERATED by scripts/generate-codepen-sources.mjs — DO NOT EDIT.
// Regenerate with:  node scripts/generate-codepen-sources.mjs
//
// Plain-JavaScript source text for every function embedded in a Studio
// CodePen payload, extracted at build time from the canonical TypeScript
// declarations listed in scripts/codepen-source-manifest.mjs. Because these
// are string literals, production minification cannot rename the
// declarations out from under the handwritten pen glue.

export const CODEPEN_FUNCTION_SOURCES: Record<string, string> = {
  ballBounce: "/**\n * Advance a ball one frame under gravity, bouncing it off the container walls.\n *\n * A simple Euler-integration physics step: applies gravity to vertical velocity,\n * moves the ball, then reflects and damps its velocity (restitution + friction)\n * whenever it hits a wall. **Mutates `ball`** in place.\n *\n * @param ball - The ball to advance (its `x`, `y`, `vx`, `vy` are mutated).\n * @param stage - The bounding container (`width`/`height`) the ball bounces inside.\n * @param restitution - Bounciness: fraction of speed kept on each bounce (0–1).\n *   `1` is a perfectly elastic, never-tiring bounce; lower values lose energy faster.\n *   Defaults to `0.72`.\n * @returns Nothing — `ball` is updated in place.\n * @example\n * ballBounce(ball, { x: 0, y: 0, width: 800, height: 600 });      // default bounce\n * ballBounce(ball, stage, 0.95);                                   // extra bouncy\n */\nfunction ballBounce(ball, stage, restitution = 0.72) {\n    const gravity = 0.4;\n    const friction = 0.985;\n    ball.vy += gravity;\n    ball.x += ball.vx;\n    ball.y += ball.vy;\n    if (ball.y >= stage.height - ball.radius) {\n        ball.y = stage.height - ball.radius;\n        ball.vy = -Math.abs(ball.vy) * restitution;\n        ball.vx *= friction;\n    }\n    if (ball.y <= ball.radius) {\n        ball.y = ball.radius;\n        ball.vy = Math.abs(ball.vy);\n    }\n    if (ball.x >= stage.width - ball.radius) {\n        ball.x = stage.width - ball.radius;\n        ball.vx = -Math.abs(ball.vx) * restitution;\n    }\n    if (ball.x <= ball.radius) {\n        ball.x = ball.radius;\n        ball.vx = Math.abs(ball.vx) * restitution;\n    }\n}",
  circleFromThreePoints: "/**\n * Find the unique circle (the circumcircle) passing through three points.\n *\n * @param point1 - First point on the circle.\n * @param point2 - Second point on the circle.\n * @param point3 - Third point on the circle.\n * @returns The circle's `radius` and `center` `{ x, y }`. (Degenerate for collinear points.)\n * @example\n * circleFromThreePoints({ x: 5, y: 0 }, { x: 0, y: 5 }, { x: -5, y: 0 });\n * // => { radius: 5, center: { x: 0, y: 0 } }\n */\nfunction circleFromThreePoints(point1, point2, point3) {\n    let x12 = point1.x - point2.x;\n    let x13 = point1.x - point3.x;\n    let y12 = point1.y - point2.y;\n    let y13 = point1.y - point3.y;\n    let x31 = point3.x - point1.x;\n    let x21 = point2.x - point1.x;\n    let y31 = point3.y - point1.y;\n    let y21 = point2.y - point1.y;\n    let sx13 = Math.pow(point1.x, 2) - Math.pow(point3.x, 2);\n    let sy13 = Math.pow(point1.y, 2) - Math.pow(point3.y, 2);\n    let sx21 = Math.pow(point2.x, 2) - Math.pow(point1.x, 2);\n    let sy21 = Math.pow(point2.y, 2) - Math.pow(point1.y, 2);\n    let f = (sx13 * x12 + sy13 * x12 + sx21 * x13 + sy21 * x13) /\n        (2 * (y31 * x12 - y21 * x13));\n    let g = (sx13 * y12 + sy13 * y12 + sx21 * y13 + sy21 * y13) /\n        (2 * (x31 * y12 - x21 * y13));\n    let c = -Math.pow(point1.x, 2) -\n        Math.pow(point1.y, 2) -\n        2 * g * point1.x -\n        2 * f * point1.y;\n    let x = -g;\n    let y = -f;\n    let sqr_of_r = x * x + y * y - c;\n    let radius = Math.sqrt(sqr_of_r);\n    return { radius, center: { x, y } };\n}",
  circleToCircle: "/**\n * Test whether two circles overlap (or touch).\n *\n * @param x1 - First circle center x.\n * @param y1 - First circle center y.\n * @param r1 - First circle radius.\n * @param x2 - Second circle center x.\n * @param y2 - Second circle center y.\n * @param r2 - Second circle radius.\n * @returns `true` if the distance between centers is `<= r1 + r2`.\n * @remarks\n * This is the flat, six-number form. For new code the **recommended** shape is the\n * object-argument {@link circleCircle}, which reads `circleCircle({ x, y, radius }, …)`.\n * Both share the same squared-distance comparison (no `sqrt`, since comparing squared\n * lengths gives the identical answer for non-negative radii).\n * @example\n * circleToCircle(0, 0, 5, 6, 0, 5); // => true\n */\nfunction circleToCircle(x1, y1, r1, x2, y2, r2) {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const radii = r1 + r2;\n    return dx * dx + dy * dy <= radii * radii;\n}",
  createKlimtSwirls: "/**\n * Create the initial state for the Klimt animation — four swirls, one per\n * canvas quadrant. Pass the returned array to `drawKlimt` each frame.\n * Self-contained (no module-level imports) so a CodePen can embed it via .toString().\n */\nfunction createKlimtSwirls(width, height) {\n    const COLORS = [\n        0xffd700, 0xdaa520, 0xb8860b,\n        0xcd853f, 0xb87333, 0xf0e68c,\n        0x8b6914, 0x191970, 0x4b0082, 0x8b0000,\n    ];\n    const CURVES = [45, -45, 135, -135];\n    const TILE_Q = 260;\n    const toCss = (n) => `rgb(${(n >> 16) & 255},${(n >> 8) & 255},${n & 255})`;\n    const randInt = (min, max) => Math.floor(Math.random() * (max + 1 - min) + min);\n    const seedXY = (q, w, h) => {\n        if (q === 'TL')\n            return { x: w * 0.25, y: h * 0.15 };\n        if (q === 'TR')\n            return { x: w * 0.75, y: h * 0.15 };\n        if (q === 'BL')\n            return { x: w * 0.75, y: h * 0.35 };\n        return { x: w * 0.25, y: h * 0.35 }; // BR\n    };\n    return ['TL', 'TR', 'BL', 'BR'].map(quadrant => {\n        const pool = [];\n        let colorCounter = 0;\n        for (let i = 0; i < TILE_Q; i++) {\n            pool.push({ x: 0, y: 0, rotation: 0, color: toCss(COLORS[colorCounter]), placed: false });\n            colorCounter = (colorCounter + 1) % COLORS.length;\n        }\n        const seed = seedXY(quadrant, width, height);\n        pool[0].x = seed.x;\n        pool[0].y = seed.y;\n        pool[0].placed = true;\n        return {\n            quadrant,\n            w: width,\n            h: height,\n            curve: CURVES[Math.floor(Math.random() * CURVES.length)],\n            interval: randInt(4, 7),\n            curveCounter: 0,\n            curveQ: randInt(85, 150),\n            poolCounter: 1,\n            testCounter: 0,\n            pool,\n        };\n    });\n}",
  criticalDamping: "/**\n * The damping coefficient that makes {@link springValue} *critically* damped — the\n * fastest approach to the target with no overshoot — for a given `stiffness` and `mass`.\n *\n * Critical damping is `c = 2·√(k·m)`. Pass less to {@link springValue}'s `damping` for\n * bounce (underdamped), more for a sluggish crawl (overdamped). With the default\n * stiffness of 170 this is `≈ 26`, which is why 26 is the default damping.\n *\n * @example\n * criticalDamping(170); // => ~26.08\n */\nfunction criticalDamping(stiffness, mass = 1) {\n    return 2 * Math.sqrt(stiffness * mass);\n}",
  deCasteljau: "/**\n * Evaluate any-degree Bézier curve at parameter `t` using de Casteljau's algorithm.\n *\n * De Casteljau works by repeatedly lerping between adjacent control points until\n * a single point remains. At each level of reduction you get the intermediate\n * \"construction\" points that make the algorithm visual — pass the returned `levels`\n * array to your canvas code to draw the animated hull the way a textbook would.\n * Works for quadratic (3 pts), cubic (4 pts), quartic (5 pts), or any degree.\n *\n * @param points - The ordered control points defining the curve (any length >= 2).\n * @param t - Position along the curve, 0–1 (0 = start, 1 = end).\n * @returns An object `{ point, levels }` where `point` is the `{ x, y }` on the\n *   curve at `t`, and `levels` is an array of point arrays — `levels[0]` is the\n *   original control polygon, each subsequent level is the next reduction step,\n *   and the last level contains only `point`.\n * @example\n * deCasteljau([{x:0,y:0},{x:0,y:50},{x:50,y:50},{x:50,y:0}], 0.5).point;\n */\nfunction deCasteljau(points, t) {\n    const levels = [points.map((p) => ({ ...p }))];\n    let current = points.map((p) => ({ ...p }));\n    while (current.length > 1) {\n        const next = [];\n        for (let i = 0; i < current.length - 1; i++) {\n            next.push({\n                x: (1 - t) * current[i].x + t * current[i + 1].x,\n                y: (1 - t) * current[i].y + t * current[i + 1].y,\n            });\n        }\n        levels.push(next);\n        current = next;\n    }\n    return { point: current[0], levels };\n}",
  dft: "/**\n * Discrete Fourier Transform of a list of 2D points (treated as complex numbers).\n *\n * Decomposes a path into a sum of rotating circles (epicycles) — the math behind\n * \"draw anything with spinning vectors\" animations. Each output entry is one\n * circle: its frequency, radius (`amp`), and starting angle (`phase`). Results are\n * sorted by amplitude so the most significant circles come first.\n *\n * @param pts - The input path as an array of points (`x` = real, `y` = imaginary).\n * @returns One `{ freq, amp, phase }` per input sample, sorted by descending `amp`.\n * @example\n * const circles = dft(pathPoints); // feed into an epicycle drawer\n */\nfunction dft(pts) {\n    const N = pts.length;\n    const X = [];\n    for (let k = 0; k < N; k++) {\n        let re = 0, im = 0;\n        for (let n = 0; n < N; n++) {\n            const phi = (2 * Math.PI * k * n) / N;\n            re += pts[n].x * Math.cos(phi) + pts[n].y * Math.sin(phi);\n            im += -pts[n].x * Math.sin(phi) + pts[n].y * Math.cos(phi);\n        }\n        re /= N;\n        im /= N;\n        X.push({ freq: k, amp: Math.sqrt(re * re + im * im), phase: Math.atan2(im, re) });\n    }\n    return X.sort((a, b) => b.amp - a.amp);\n}",
  drawAngleLerp: "function drawAngleLerp(ctx, canvasWidth, canvasHeight, naiveAngle, smartAngle, startAngle, targetAngle, t) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const r = Math.min(canvasHeight, canvasWidth) * 0.22;\n    const cy = canvasHeight / 2;\n    const lx = canvasWidth * 0.3;\n    const rx = canvasWidth * 0.7;\n    // start (orange) and target (green) reference spokes on both dials\n    [lx, rx].forEach((cx) => {\n        ctx.lineWidth = 2;\n        ctx.strokeStyle = \"rgba(249,115,22,0.35)\";\n        ctx.beginPath();\n        ctx.moveTo(cx, cy);\n        ctx.lineTo(cx + Math.cos(startAngle) * r, cy + Math.sin(startAngle) * r);\n        ctx.stroke();\n        ctx.strokeStyle = \"rgba(52,211,153,0.5)\";\n        ctx.beginPath();\n        ctx.moveTo(cx, cy);\n        ctx.lineTo(cx + Math.cos(targetAngle) * r, cy + Math.sin(targetAngle) * r);\n        ctx.stroke();\n    });\n    drawDial(ctx, lx, cy, r, naiveAngle, \"#ef4444\", \"naive lerp — spins the long way\");\n    drawDial(ctx, rx, cy, r, smartAngle, \"#6366f1\", \"lerpAngle — shortest path\");\n    ctx.fillStyle = \"rgba(255,255,255,0.4)\";\n    ctx.font = \"12px monospace\";\n    ctx.fillText(`t: ${t.toFixed(2)}    (orange = start, green = target)`, 12, canvasHeight - 14);\n}",
  drawBezierCurves: "/**\n * Standalone draw function for the Bézier curves animation.\n * Self-contained — all it needs is a 2D context, an array of control points\n * (with optional per-point `color`), and the current `t` value (0–1).\n * CodePen embeds this via `.toString()` alongside `deCasteljau.toString()`.\n */\nfunction drawBezierCurves(ctx, points, t) {\n    const defaultPalette = [\"#e74c3c\", \"#3498db\", \"#2ecc71\", \"#f39c12\", \"#9b59b6\"];\n    const POINT_RADIUS = 10;\n    ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n    const pts = points;\n    // Draw the full Bezier curve\n    ctx.strokeStyle = \"#2c3e50\";\n    ctx.lineWidth = 2.5;\n    ctx.beginPath();\n    const steps = 200;\n    for (let i = 0; i <= steps; i++) {\n        const ti = i / steps;\n        const { point } = deCasteljau(pts, ti);\n        if (i === 0)\n            ctx.moveTo(point.x, point.y);\n        else\n            ctx.lineTo(point.x, point.y);\n    }\n    ctx.stroke();\n    // Draw construction lines between control points (hull)\n    ctx.strokeStyle = \"rgba(0,0,0,0.15)\";\n    ctx.lineWidth = 1;\n    ctx.setLineDash([5, 5]);\n    ctx.beginPath();\n    ctx.moveTo(pts[0].x, pts[0].y);\n    for (let i = 1; i < pts.length; i++)\n        ctx.lineTo(pts[i].x, pts[i].y);\n    ctx.stroke();\n    ctx.setLineDash([]);\n    // Draw de Casteljau construction at current t\n    const { levels } = deCasteljau(pts, t);\n    const levelColors = [\"rgba(230,100,100,0.8)\", \"rgba(100,180,100,0.8)\", \"rgba(100,100,230,0.8)\", \"rgba(200,150,50,0.8)\"];\n    for (let lvl = 1; lvl < levels.length; lvl++) {\n        const color = levelColors[(lvl - 1) % levelColors.length];\n        ctx.strokeStyle = color;\n        ctx.lineWidth = 1.5;\n        ctx.beginPath();\n        ctx.moveTo(levels[lvl][0].x, levels[lvl][0].y);\n        for (let i = 1; i < levels[lvl].length; i++) {\n            ctx.lineTo(levels[lvl][i].x, levels[lvl][i].y);\n        }\n        ctx.stroke();\n        // Draw dots at interpolated points\n        for (const p of levels[lvl]) {\n            ctx.fillStyle = color;\n            ctx.beginPath();\n            ctx.arc(p.x, p.y, lvl === levels.length - 1 ? 6 : 4, 0, 2 * Math.PI);\n            ctx.fill();\n        }\n    }\n    // Draw final point on curve at t\n    if (levels.length > 0) {\n        const tip = levels[levels.length - 1][0];\n        ctx.fillStyle = \"#e74c3c\";\n        ctx.strokeStyle = \"#fff\";\n        ctx.lineWidth = 2;\n        ctx.beginPath();\n        ctx.arc(tip.x, tip.y, 7, 0, 2 * Math.PI);\n        ctx.fill();\n        ctx.stroke();\n    }\n    // Draw t label\n    ctx.fillStyle = \"#333\";\n    ctx.font = \"bold 14px monospace\";\n    ctx.fillText(`t = ${t.toFixed(2)}`, 12, ctx.canvas.height - 12);\n    // Draw control points\n    for (let i = 0; i < pts.length; i++) {\n        const p = pts[i];\n        ctx.fillStyle = p.color ?? defaultPalette[i % defaultPalette.length];\n        ctx.strokeStyle = \"#fff\";\n        ctx.lineWidth = 2;\n        ctx.beginPath();\n        ctx.arc(p.x, p.y, POINT_RADIUS, 0, 2 * Math.PI);\n        ctx.fill();\n        ctx.stroke();\n        ctx.fillStyle = \"#333\";\n        ctx.font = \"bold 11px monospace\";\n        ctx.fillText(`P${i}`, p.x + 13, p.y - 6);\n    }\n}",
  drawBird: "/**\n * drawBird — self-contained: draw one bird as a small triangle pointing along its\n * heading. Kept dependency-free so it can be embedded in a CodePen via .toString().\n */\nfunction drawBird(ctx, x, y, angle, size) {\n    ctx.save();\n    ctx.translate(x, y);\n    ctx.rotate(angle);\n    ctx.beginPath();\n    ctx.moveTo(size, 0);\n    ctx.lineTo(-size * 0.6, size * 0.5);\n    ctx.lineTo(-size * 0.6, -size * 0.5);\n    ctx.closePath();\n    ctx.fill();\n    ctx.restore();\n}",
  drawCenterOnParent: "function drawCenterOnParent(ctx, width, height, time) {\n    function centerOnParent(item, parent) {\n        return { x: (parent.width - item.width) / 2, y: (parent.height - item.height) / 2 };\n    }\n    ctx.fillStyle = '#0a0a0f';\n    ctx.fillRect(0, 0, width, height);\n    const parent = { x: width * 0.14, y: height * 0.16, width: width * 0.72, height: height * 0.68 };\n    const child = { width: 110 + Math.sin(time) * 45, height: 80 + Math.cos(time * 0.8) * 32 };\n    const pos = centerOnParent(child, parent);\n    ctx.strokeStyle = 'rgba(129, 140, 248, 0.55)';\n    ctx.lineWidth = 3;\n    ctx.strokeRect(parent.x, parent.y, parent.width, parent.height);\n    ctx.fillStyle = '#818cf8';\n    ctx.fillRect(parent.x + pos.x, parent.y + pos.y, child.width, child.height);\n    ctx.fillStyle = '#d8e2ff';\n    ctx.font = '14px monospace';\n    ctx.fillText('centerOnParent(child, parent) => { x: ' + pos.x.toFixed(1) + ', y: ' + pos.y.toFixed(1) + ' }', 18, 30);\n}",
  drawCircleField: "function drawCircleField(ctx, balls, width, height, detect) {\n    ctx.fillStyle = \"#0a0a12\";\n    ctx.fillRect(0, 0, width, height);\n    for (const b of balls) {\n        b.x += b.vx;\n        b.y += b.vy;\n        if (b.x - b.r < 0) {\n            b.x = b.r;\n            b.vx = Math.abs(b.vx);\n        }\n        if (b.x + b.r > width) {\n            b.x = width - b.r;\n            b.vx = -Math.abs(b.vx);\n        }\n        if (b.y - b.r < 0) {\n            b.y = b.r;\n            b.vy = Math.abs(b.vy);\n        }\n        if (b.y + b.r > height) {\n            b.y = height - b.r;\n            b.vy = -Math.abs(b.vy);\n        }\n        if (b.hit > 0)\n            b.hit--;\n    }\n    // O(n²) circleToCircle check — every pair, every frame\n    for (let i = 0; i < balls.length; i++) {\n        for (let j = i + 1; j < balls.length; j++) {\n            const a = balls[i], b = balls[j];\n            if (!detect(a.x, a.y, a.r, b.x, b.y, b.r))\n                continue;\n            const dx = b.x - a.x, dy = b.y - a.y;\n            const dist = Math.sqrt(dx * dx + dy * dy) || 0.001;\n            const nx = dx / dist, ny = dy / dist;\n            // Push apart\n            const overlap = (a.r + b.r) - dist;\n            a.x -= nx * overlap * 0.5;\n            a.y -= ny * overlap * 0.5;\n            b.x += nx * overlap * 0.5;\n            b.y += ny * overlap * 0.5;\n            // Exchange velocity along collision normal (equal mass elastic)\n            const dot = (a.vx - b.vx) * nx + (a.vy - b.vy) * ny;\n            if (dot > 0) {\n                a.vx -= dot * nx;\n                a.vy -= dot * ny;\n                b.vx += dot * nx;\n                b.vy += dot * ny;\n            }\n            a.hit = 8;\n            b.hit = 8;\n        }\n    }\n    for (const b of balls) {\n        const lit = b.hit > 0 ? 80 : 48;\n        const grd = ctx.createRadialGradient(b.x - b.r * 0.32, b.y - b.r * 0.32, 0, b.x, b.y, b.r);\n        grd.addColorStop(0, `hsl(${b.hue},80%,${lit + 18}%)`);\n        grd.addColorStop(1, `hsl(${b.hue},80%,${lit - 18}%)`);\n        ctx.beginPath();\n        ctx.arc(b.x, b.y, b.r, 0, Math.PI * 2);\n        ctx.fillStyle = grd;\n        ctx.fill();\n    }\n}",
  drawCircleFromThreePoints: "function drawCircleFromThreePoints(ctx, points, text, circleQ, textDiv) {\n    ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n    let textString = \"\";\n    text.forEach((txt, i) => {\n        textString += `${txt}`;\n    });\n    if (textDiv) {\n        textDiv.innerHTML = textString;\n    }\n    points.forEach((item) => {\n        ctx.beginPath();\n        ctx.arc(item.x, item.y, 5, 0, 2 * Math.PI);\n        ctx.stroke();\n    });\n    if (points.length === 3) {\n        let { center, radius } = CircleFromThreePointsFunc(points[0], points[1], points[2]);\n        ctx.lineWidth = 3;\n        ctx.strokeStyle = \"green\";\n        ctx.beginPath();\n        ctx.arc(center.x, center.y, radius, 0, circleQ);\n        ctx.stroke();\n    }\n}",
  drawCircleToCircle: "function drawCircleToCircle(ctx, circle1, circle2, hit, canvasWidth, time = performance.now()) {\n    const pulseLightness = 55 + 25 * Math.sin(time / 120);\n    const hitColor = `hsl(330, 95%, ${pulseLightness}%)`;\n    ctx.fillStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.beginPath();\n    ctx.arc(circle2.x, circle2.y, circle2.radius, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.fillStyle = hit ? hitColor : \"#818cf8\";\n    ctx.beginPath();\n    ctx.arc(circle1.x, circle1.y, circle1.radius, 0, 2 * Math.PI);\n    ctx.fill();\n    if (!hit)\n        return;\n    ctx.save();\n    ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n    ctx.textAlign = \"center\";\n    ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n    ctx.shadowBlur = 14;\n    ctx.fillStyle = \"#cdd3ff\";\n    ctx.fillText(\"collision detected\", canvasWidth / 2, 40);\n    ctx.restore();\n}",
  drawCircleToRectangle: "function drawCircleToRectangle(ctx, canvasWidth, canvasHeight, time, sineCurveFn, polygonCircleFn, rectangleFn) {\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const circle = { x: halfWidth, y: halfHeight, radius: 100 };\n    const x = sineCurveFn(halfWidth, 200, 0.001, time);\n    const y = sineCurveFn(halfHeight, 200, 0.001, time);\n    const rect = rectangleFn(100, 100, 0, {\n        rotate: true,\n        rotateSpeed: 2000,\n        time,\n    });\n    rect.vertices.forEach((vertex) => {\n        vertex.x += x;\n        vertex.y += y;\n    });\n    const hit = polygonCircleFn(rect, circle);\n    const hitColor = \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\";\n    ctx.fillStyle = hit ? hitColor : \"#818cf8\";\n    ctx.beginPath();\n    ctx.arc(circle.x, circle.y, circle.radius, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.fillStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.beginPath();\n    rect.vertices.forEach((vertex, index) => {\n        if (index === 0) {\n            ctx.moveTo(vertex.x, vertex.y);\n            return;\n        }\n        ctx.lineTo(vertex.x, vertex.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    if (hit) {\n        ctx.save();\n        ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n        ctx.textAlign = \"center\";\n        ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n        ctx.shadowBlur = 14;\n        ctx.fillStyle = \"#cdd3ff\";\n        ctx.fillText(\"collision detected\", halfWidth, 40);\n        ctx.restore();\n    }\n}",
  drawColorFamily: "/**\n * Pure renderer: a centered grid of rounded swatches. Library-free (CodePen-portable).\n * `topInset` reserves vertical space for the absolutely-positioned ELI5 text that the\n * site overlays on the top-left of the canvas, so swatches start beneath it.\n */\nfunction drawColorFamily(ctx, colors, family, canvasWidth, canvasHeight, topInset) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const n = colors.length;\n    const cols = Math.max(1, Math.min(n, Math.round(Math.sqrt(n * (canvasWidth / Math.max(1, canvasHeight))))));\n    const rows = Math.ceil(n / cols);\n    const padX = 24;\n    const padTop = Math.max(16, topInset); // clear the ELI5 text overlay floating over the top\n    const padBottom = 34; // room for the label at the bottom\n    const gap = 10;\n    const cellW = (canvasWidth - padX * 2 - gap * (cols - 1)) / cols;\n    const cellH = (canvasHeight - padTop - padBottom - gap * (rows - 1)) / rows;\n    const size = Math.max(8, Math.min(cellW, cellH));\n    const radius = Math.min(14, size * 0.22);\n    // Center the whole grid.\n    const gridW = cols * size + (cols - 1) * gap;\n    const gridH = rows * size + (rows - 1) * gap;\n    const startX = (canvasWidth - gridW) / 2;\n    // Center within the area below the header, but never let the grid ride up into it.\n    const startY = padTop + Math.max(0, (canvasHeight - padTop - padBottom - gridH) / 2);\n    const roundRect = (x, y, w, h, r) => {\n        ctx.beginPath();\n        ctx.moveTo(x + r, y);\n        ctx.arcTo(x + w, y, x + w, y + h, r);\n        ctx.arcTo(x + w, y + h, x, y + h, r);\n        ctx.arcTo(x, y + h, x, y, r);\n        ctx.arcTo(x, y, x + w, y, r);\n        ctx.closePath();\n    };\n    for (let i = 0; i < n; i++) {\n        const c = i % cols;\n        const rrow = Math.floor(i / cols);\n        const x = startX + c * (size + gap);\n        const y = startY + rrow * (size + gap);\n        roundRect(x, y, size, size, radius);\n        ctx.fillStyle = colors[i];\n        ctx.fill();\n        ctx.lineWidth = 1;\n        ctx.strokeStyle = \"rgba(0,0,0,0.18)\";\n        ctx.stroke();\n    }\n    // Label at the bottom, clear of the ELI5 overlay at the top.\n    ctx.fillStyle = \"rgba(255,255,255,0.55)\";\n    ctx.font = \"13px monospace\";\n    ctx.fillText(`colorFamily(\"${family}\", ${colors.length})`, 18, canvasHeight - 12);\n}",
  drawColorLerp: "/**\n * Pure renderer: two stacked gradient bars (RGB-lerp on top, HSL-lerp below) with\n * a sweeping playhead and a magnified swatch from each bar at the playhead.\n * Takes pre-sampled CSS color strings so it stays free of any library import\n * (CodePen-portable, like the other drawX functions).\n */\nfunction drawColorLerp(ctx, rgbStops, hslStops, t, canvasWidth, canvasHeight) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const margin = Math.min(70, canvasWidth * 0.08);\n    const barW = canvasWidth - margin * 2;\n    const barH = Math.min(96, canvasHeight * 0.2);\n    const gap = Math.min(96, canvasHeight * 0.16);\n    const x0 = margin;\n    const rgbY = canvasHeight / 2 - barH - gap / 2;\n    const hslY = canvasHeight / 2 + gap / 2;\n    const drawBar = (stops, y, label) => {\n        const n = stops.length;\n        const sw = barW / n;\n        for (let i = 0; i < n; i++) {\n            ctx.fillStyle = stops[i];\n            ctx.fillRect(x0 + i * sw, y, Math.ceil(sw) + 1, barH);\n        }\n        ctx.strokeStyle = \"rgba(255,255,255,0.18)\";\n        ctx.lineWidth = 1;\n        ctx.strokeRect(x0, y, barW, barH);\n        ctx.fillStyle = \"rgba(255,255,255,0.78)\";\n        ctx.font = \"13px monospace\";\n        ctx.fillText(label, x0, y - 10);\n    };\n    const drawSwatch = (cx, cy, color, caption) => {\n        const r = 26;\n        ctx.beginPath();\n        ctx.arc(cx, cy, r, 0, Math.PI * 2);\n        ctx.fillStyle = color;\n        ctx.fill();\n        ctx.lineWidth = 2;\n        ctx.strokeStyle = \"rgba(255,255,255,0.85)\";\n        ctx.stroke();\n        ctx.fillStyle = \"rgba(255,255,255,0.85)\";\n        ctx.font = \"11px monospace\";\n        ctx.textAlign = \"center\";\n        ctx.fillText(caption, cx, cy + r + 16);\n        ctx.textAlign = \"left\";\n    };\n    drawBar(rgbStops, rgbY, \"RGB lerp — straight channel blend (goes muddy)\");\n    drawBar(hslStops, hslY, \"HSL lerp — rotate hue around the wheel (stays vivid)\");\n    // Sweeping playhead spanning both bars.\n    const px = x0 + barW * t;\n    ctx.strokeStyle = \"rgba(255,255,255,0.55)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(px, rgbY - 4);\n    ctx.lineTo(px, hslY + barH + 4);\n    ctx.stroke();\n    // Magnified current color from each bar at the playhead.\n    const idx = Math.round(t * (rgbStops.length - 1));\n    const clampX = Math.max(x0 + 30, Math.min(px, x0 + barW - 30));\n    drawSwatch(clampX, rgbY - 46, rgbStops[idx], \"RGB\");\n    drawSwatch(clampX, hslY + barH + 50, hslStops[idx], \"HSL\");\n    ctx.fillStyle = \"rgba(255,255,255,0.35)\";\n    ctx.font = \"12px monospace\";\n    ctx.fillText(`t: ${t.toFixed(2)}`, x0, canvasHeight - 12);\n}",
  drawDeMystifySineCosine: "function drawDeMystifySineCosine(ctx, canvasWidth, canvasHeight, textDiv, unitCirclePointFn, radToDegFn) {\n    const now = performance.now() * 0.001;\n    const progress = (Math.sin(now) + 1) / 2;\n    const angle = progress * (Math.PI / 2);\n    const radius = Math.min(canvasWidth * 0.22, canvasHeight * 0.28, 130);\n    const centerX = canvasWidth * 0.28;\n    const centerY = canvasHeight * 0.62;\n    const point = unitCirclePointFn(centerX, centerY, radius, -angle);\n    const adjacent = point.x - centerX;\n    const opposite = centerY - point.y;\n    const degrees = radToDegFn(angle);\n    const ratioPanelX = canvasWidth * 0.58;\n    const ratioPanelWidth = canvasWidth * 0.28;\n    const ratioScale = ratioPanelWidth / radius;\n    const adjacentBarWidth = adjacent * ratioScale;\n    const oppositeBarWidth = opposite * ratioScale;\n    ctx.font = \"bold 18px Arial\";\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    ctx.strokeStyle = \"rgba(255,255,255,0.18)\";\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(centerX - radius - 28, centerY);\n    ctx.lineTo(centerX + radius + 28, centerY);\n    ctx.moveTo(centerX, centerY + 28);\n    ctx.lineTo(centerX, centerY - radius - 28);\n    ctx.stroke();\n    ctx.strokeStyle = \"rgba(255,255,255,0.35)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.arc(centerX, centerY, radius, 0, Math.PI * 2);\n    ctx.stroke();\n    ctx.strokeStyle = \"rgba(255,255,255,0.22)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(point.x, point.y);\n    ctx.lineTo(point.x, centerY);\n    ctx.stroke();\n    ctx.strokeStyle = \"#7de8ff\";\n    ctx.lineWidth = 5;\n    ctx.beginPath();\n    ctx.moveTo(centerX, centerY);\n    ctx.lineTo(point.x, point.y);\n    ctx.stroke();\n    ctx.strokeStyle = \"#86efac\";\n    ctx.beginPath();\n    ctx.moveTo(centerX, centerY);\n    ctx.lineTo(point.x, centerY);\n    ctx.stroke();\n    ctx.strokeStyle = \"#ff7a00\";\n    ctx.beginPath();\n    ctx.moveTo(point.x, centerY);\n    ctx.lineTo(point.x, point.y);\n    ctx.stroke();\n    ctx.fillStyle = \"#ffffff\";\n    ctx.beginPath();\n    ctx.arc(point.x, point.y, 5, 0, Math.PI * 2);\n    ctx.fill();\n    ctx.strokeStyle = \"rgba(125,232,255,0.65)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.arc(centerX, centerY, 28, -angle, 0);\n    ctx.stroke();\n    ctx.fillStyle = \"#ffffff\";\n    ctx.font = \"bold 18px Arial\";\n    ctx.fillText(`${degrees.toFixed(0)}deg`, centerX + 18, centerY - 10);\n    ctx.font = \"14px Arial\";\n    ctx.fillStyle = \"#86efac\";\n    ctx.fillText(\"adjacent\", centerX + adjacent / 2 - 24, centerY + 22);\n    ctx.fillStyle = \"#ff7a00\";\n    ctx.fillText(\"opposite\", point.x + 10, centerY - opposite / 2);\n    ctx.fillStyle = \"#7de8ff\";\n    ctx.fillText(\"hypotenuse\", centerX + adjacent / 2 - 30, centerY - opposite / 2 - 10);\n    ctx.fillStyle = \"rgba(255,255,255,0.92)\";\n    ctx.font = \"bold 16px Arial\";\n    ctx.fillText(\"cos(theta) = adjacent / hypotenuse\", ratioPanelX, centerY - 78);\n    ctx.fillText(\"sin(theta) = opposite / hypotenuse\", ratioPanelX, centerY + 8);\n    ctx.fillStyle = \"#86efac\";\n    ctx.fillRect(ratioPanelX, centerY - 58, adjacentBarWidth, 12);\n    ctx.strokeStyle = \"rgba(255,255,255,0.2)\";\n    ctx.strokeRect(ratioPanelX, centerY - 58, radius * ratioScale, 12);\n    ctx.fillStyle = \"#ff7a00\";\n    ctx.fillRect(ratioPanelX, centerY + 28, oppositeBarWidth, 12);\n    ctx.strokeStyle = \"rgba(255,255,255,0.2)\";\n    ctx.strokeRect(ratioPanelX, centerY + 28, radius * ratioScale, 12);\n    ctx.fillStyle = \"#86efac\";\n    ctx.fillText(`${adjacent.toFixed(1)} / ${radius.toFixed(1)} = ${(adjacent / radius).toFixed(3)}`, ratioPanelX, centerY - 22);\n    ctx.fillStyle = \"#ff7a00\";\n    ctx.fillText(`${opposite.toFixed(1)} / ${radius.toFixed(1)} = ${(opposite / radius).toFixed(3)}`, ratioPanelX, centerY + 64);\n    if (textDiv) {\n        textDiv.innerHTML = `\n      <p>As the triangle swings through the top-right quarter of the circle, the horizontal leg is the <strong>adjacent</strong> side and the vertical leg is the <strong>opposite</strong> side.</p>\n      <p><strong>cos(theta)</strong> tells you how much of the radius survives horizontally. <strong>sin(theta)</strong> tells you how much of the radius survives vertically.</p>\n      <p>When the point nears the right edge, cosine is close to 1 and sine is close to 0. As the point climbs upward, cosine shrinks and sine grows.</p>\n    `;\n    }\n}",
  drawDegreesToRadians: "function drawDegreesToRadians(ctx, width, height, degrees) {\n    function degToRad(degree) {\n        return degree * (Math.PI / 180);\n    }\n    const radians = degToRad(degrees);\n    const cx = width / 2, cy = height / 2;\n    const r = Math.min(width, height) * 0.28;\n    ctx.fillStyle = '#0a0a0f';\n    ctx.fillRect(0, 0, width, height);\n    ctx.strokeStyle = 'rgba(255,255,255,0.2)';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.arc(cx, cy, r, 0, Math.PI * 2);\n    ctx.stroke();\n    ctx.strokeStyle = '#818cf8';\n    ctx.lineWidth = 5;\n    ctx.beginPath();\n    ctx.moveTo(cx, cy);\n    ctx.lineTo(cx + Math.cos(radians) * r, cy + Math.sin(radians) * r);\n    ctx.stroke();\n    ctx.fillStyle = '#d8e2ff';\n    ctx.font = '16px monospace';\n    ctx.fillText(degrees + ' degrees = ' + radians.toFixed(3) + ' radians', 18, 32);\n}",
  drawDistributeAroundCircle: "function drawDistributeAroundCircle(ctx, canvasWidth, canvasHeight, totalItems, distributeAroundCircleFn) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    let radius = cosWave(100, 100, 0.001);\n    ctx.strokeStyle = \"rgba(255,255,255,0.3)\";\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(halfWidth, 0);\n    ctx.lineTo(halfWidth, canvasHeight);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.arc(halfWidth, halfHeight, radius, 0, 2 * Math.PI);\n    ctx.stroke();\n    let points = distributeAroundCircleFn({ x: halfWidth, y: halfHeight }, radius, totalItems);\n    points.forEach((point) => {\n        ctx.strokeStyle = \"rgba(255,255,255,0.3)\";\n        ctx.lineWidth = 2;\n        ctx.beginPath();\n        ctx.arc(point.x, point.y, 20, 0, 2 * Math.PI);\n        ctx.stroke();\n        ctx.beginPath();\n        ctx.moveTo(halfWidth, halfHeight);\n        ctx.lineTo(point.x, point.y);\n        ctx.stroke();\n    });\n}",
  drawEasing: "function drawEasing(ctx, canvasWidth, canvasHeight) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const elapsed = Date.now() % (PERIOD_MS + PAUSE_MS);\n    // t goes 0→1 during PERIOD_MS, then freezes at 1 during PAUSE_MS\n    const t = Math.min(elapsed / PERIOD_MS, 1);\n    const PAD_X = Math.max(130, canvasWidth * 0.22);\n    const PAD_RIGHT = 20;\n    const PAD_Y = 32;\n    const trackW = canvasWidth - PAD_X - PAD_RIGHT;\n    const trackH = (canvasHeight - PAD_Y * 2) / TRACKS.length;\n    const BALL_R = Math.max(6, Math.min(10, trackH * 0.28));\n    ctx.font = \"11px monospace\";\n    ctx.textAlign = \"right\";\n    TRACKS.forEach((track, i) => {\n        const cy = PAD_Y + i * trackH + trackH / 2;\n        // Track line\n        ctx.strokeStyle = \"rgba(255,255,255,0.1)\";\n        ctx.lineWidth = 1;\n        ctx.beginPath();\n        ctx.moveTo(PAD_X, cy);\n        ctx.lineTo(PAD_X + trackW, cy);\n        ctx.stroke();\n        // End-caps (dot markers for start/end positions)\n        ctx.fillStyle = \"rgba(255,255,255,0.12)\";\n        ctx.beginPath();\n        ctx.arc(PAD_X, cy, BALL_R * 0.55, 0, Math.PI * 2);\n        ctx.arc(PAD_X + trackW, cy, BALL_R * 0.55, 0, Math.PI * 2);\n        ctx.fill();\n        // Label\n        ctx.fillStyle = track.color;\n        ctx.fillText(track.name, PAD_X - 10, cy + 4);\n        // Ball\n        const easedT = track.ease(t);\n        const bx = PAD_X + easedT * trackW;\n        // Glow\n        ctx.save();\n        ctx.globalAlpha = 0.28;\n        ctx.fillStyle = track.color;\n        ctx.beginPath();\n        ctx.arc(bx, cy, BALL_R * 1.9, 0, Math.PI * 2);\n        ctx.fill();\n        ctx.restore();\n        // Ball body\n        ctx.fillStyle = track.color;\n        ctx.beginPath();\n        ctx.arc(bx, cy, BALL_R, 0, Math.PI * 2);\n        ctx.fill();\n    });\n    ctx.textAlign = \"left\";\n}",
  drawEquilateralTriangle: "function drawEquilateralTriangle(ctx, backgroundTris, angle, startPoint, endPoint, canvasWidth, canvasHeight, equilateralTriangleFn) {\n    backgroundTris.forEach((tri) => {\n        let { point1, point2, point3 } = equilateralTriangleFn(tri.radius, tri.centerPoint, angle || tri.angle);\n        ctx.strokeStyle = \"rgba(255,255,255,0.2)\";\n        ctx.lineWidth = 1;\n        ctx.beginPath();\n        ctx.moveTo(point1.x, point1.y);\n        ctx.lineTo(point2.x, point2.y);\n        ctx.lineTo(point3.x, point3.y);\n        ctx.lineTo(point1.x, point1.y);\n        ctx.stroke();\n    });\n    if (!startPoint || !endPoint)\n        return;\n    ctx.strokeStyle = \"green\";\n    ctx.lineWidth = 3;\n    let a = startPoint.x - endPoint.x;\n    let b = startPoint.y - endPoint.y;\n    let radius = Math.sqrt(a * a + b * b);\n    let { point1, point2, point3 } = equilateralTriangleFn(radius, startPoint, angle);\n    ctx.beginPath();\n    ctx.moveTo(point1.x, point1.y);\n    ctx.lineTo(point2.x, point2.y);\n    ctx.lineTo(point3.x, point3.y);\n    ctx.lineTo(point1.x, point1.y);\n    ctx.stroke();\n    [point1, point2, point3].forEach((item) => {\n        ctx.beginPath();\n        ctx.arc(item.x, item.y, 5, 0, 2 * Math.PI);\n        ctx.stroke();\n    });\n}",
  drawFlowField: "function drawFlowField(ctx, trailCtx, width, height, state) {\n    function getFlowAngleAtPoint(x, y) {\n        function fadeLocal(t) {\n            return t * t * t * (t * (t * 6 - 15) + 10);\n        }\n        function lerpLocal(a, b, t) {\n            return a + t * (b - a);\n        }\n        function gradLocal(hash, px, py) {\n            const h = hash & 3;\n            const u = h < 2 ? px : py;\n            const v = h < 2 ? py : px;\n            return ((h & 1) ? -u : u) + ((h & 2) ? -v : v);\n        }\n        let sampleX = x * state.fieldScale;\n        let sampleY = y * state.fieldScale + state.zOffset;\n        const X = Math.floor(sampleX) & 255;\n        const Y = Math.floor(sampleY) & 255;\n        sampleX -= Math.floor(sampleX);\n        sampleY -= Math.floor(sampleY);\n        const u = fadeLocal(sampleX);\n        const v = fadeLocal(sampleY);\n        const a = state.perm[X] + Y;\n        const b = state.perm[X + 1] + Y;\n        return (lerpLocal(lerpLocal(gradLocal(state.perm[a], sampleX, sampleY), gradLocal(state.perm[b], sampleX - 1, sampleY), u), lerpLocal(gradLocal(state.perm[a + 1], sampleX, sampleY - 1), gradLocal(state.perm[b + 1], sampleX - 1, sampleY - 1), u), v) *\n            Math.PI *\n            4);\n    }\n    state.zOffset += state.zSpeed;\n    for (const p of state.particles) {\n        const angle = getFlowAngleAtPoint(p.x, p.y);\n        p.vx += Math.cos(angle) * 0.3;\n        p.vy += Math.sin(angle) * 0.3;\n        const mag = Math.hypot(p.vx, p.vy);\n        if (mag > state.speed) {\n            p.vx = (p.vx / mag) * state.speed;\n            p.vy = (p.vy / mag) * state.speed;\n        }\n        p.history.push({ x: p.x, y: p.y });\n        if (p.history.length > state.trailLength)\n            p.history.shift();\n        p.x += p.vx;\n        p.y += p.vy;\n        if (p.x < 0)\n            p.x = width;\n        if (p.x > width)\n            p.x = 0;\n        if (p.y < 0)\n            p.y = height;\n        if (p.y > height)\n            p.y = 0;\n        p.hue = (p.hue + 0.3) % 360;\n    }\n    trailCtx.fillStyle = \"rgba(15, 15, 25, 0.04)\";\n    trailCtx.fillRect(0, 0, width, height);\n    for (const p of state.particles) {\n        if (p.history.length < 2)\n            continue;\n        trailCtx.beginPath();\n        trailCtx.moveTo(p.history[0].x, p.history[0].y);\n        for (let i = 1; i < p.history.length; i++) {\n            const alpha = (i / p.history.length) * p.alpha;\n            trailCtx.strokeStyle = `hsla(${p.hue},80%,60%,${alpha})`;\n            trailCtx.lineWidth = 1.5;\n            trailCtx.lineTo(p.history[i].x, p.history[i].y);\n            trailCtx.stroke();\n            trailCtx.beginPath();\n            trailCtx.moveTo(p.history[i].x, p.history[i].y);\n        }\n    }\n    ctx.clearRect(0, 0, width, height);\n    ctx.drawImage(trailCtx.canvas, 0, 0);\n    for (const p of state.particles) {\n        ctx.fillStyle = `hsla(${p.hue},90%,75%,0.9)`;\n        ctx.beginPath();\n        ctx.arc(p.x, p.y, 1.5, 0, 2 * Math.PI);\n        ctx.fill();\n    }\n}",
  drawFormatNumberWithCommas: "function drawFormatNumberWithCommas(ctx, width, height, ms) {\n    function numberWithCommas(x) {\n        return x.toString().replace(/\\B(?=(\\d{3})+(?!\\d))/g, ',');\n    }\n    const raw = Math.floor(1000 + ms * 87);\n    ctx.fillStyle = '#0a0a0f';\n    ctx.fillRect(0, 0, width, height);\n    ctx.textAlign = 'center';\n    ctx.font = '22px monospace';\n    ctx.fillStyle = 'rgba(255,255,255,0.45)';\n    ctx.fillText(String(raw), width / 2, height / 2 - 28);\n    ctx.font = 'bold 44px monospace';\n    ctx.fillStyle = '#818cf8';\n    ctx.fillText(numberWithCommas(raw), width / 2, height / 2 + 36);\n}",
  drawFourierEpicycles: "function drawFourierEpicycles(ctx, fourier, time, trail) {\n    const W = ctx.canvas.width;\n    const H = ctx.canvas.height;\n    // Dark background with slight fade for trail ghosting\n    ctx.fillStyle = \"rgba(15,15,26,0.3)\";\n    ctx.fillRect(0, 0, W, H);\n    let x = ctx.canvas.width / 2;\n    let y = ctx.canvas.height / 2;\n    const t = time;\n    for (let i = 0; i < fourier.length; i++) {\n        const { freq, amp, phase } = fourier[i];\n        const prevX = x, prevY = y;\n        const angle = freq * t * ((2 * Math.PI) / fourier.length) + phase;\n        x += amp * Math.cos(angle);\n        y += amp * Math.sin(angle);\n        // Circle outlines — muted pink/rose\n        ctx.strokeStyle = \"rgba(255,100,180,0.25)\";\n        ctx.lineWidth = 1;\n        ctx.beginPath();\n        ctx.arc(prevX, prevY, amp, 0, 2 * Math.PI);\n        ctx.stroke();\n        // Spoke lines — brighter pink\n        ctx.strokeStyle = \"rgba(255,120,200,0.7)\";\n        ctx.lineWidth = 1.5;\n        ctx.beginPath();\n        ctx.moveTo(prevX, prevY);\n        ctx.lineTo(x, y);\n        ctx.stroke();\n    }\n    trail.unshift({ x, y });\n    const maxTrail = fourier.length * 4;\n    if (trail.length > maxTrail)\n        trail.length = maxTrail;\n    // Traced path — hot pink / magenta\n    ctx.beginPath();\n    ctx.moveTo(trail[0].x, trail[0].y);\n    for (let i = 1; i < trail.length; i++) {\n        const alpha = 1 - i / trail.length;\n        ctx.strokeStyle = `rgba(255,60,180,${alpha})`;\n        ctx.lineWidth = 2.5;\n        ctx.lineTo(trail[i].x, trail[i].y);\n        ctx.stroke();\n        ctx.beginPath();\n        ctx.moveTo(trail[i].x, trail[i].y);\n    }\n    // Tip dot — bright white-pink\n    ctx.fillStyle = \"rgba(255,220,240,1)\";\n    ctx.beginPath();\n    ctx.arc(x, y, 4, 0, 2 * Math.PI);\n    ctx.fill();\n}",
  drawGameOfLife: "function drawGameOfLife(ctx, grid, cols, rows) {\n    const cell = 12;\n    ctx.fillStyle = \"#0f0f1a\";\n    ctx.fillRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n    ctx.strokeStyle = \"rgba(255,255,255,0.05)\";\n    ctx.lineWidth = 0.5;\n    for (let c = 0; c <= cols; c++) {\n        ctx.beginPath();\n        ctx.moveTo(c * cell, 0);\n        ctx.lineTo(c * cell, rows * cell);\n        ctx.stroke();\n    }\n    for (let r = 0; r <= rows; r++) {\n        ctx.beginPath();\n        ctx.moveTo(0, r * cell);\n        ctx.lineTo(cols * cell, r * cell);\n        ctx.stroke();\n    }\n    ctx.fillStyle = \"#39ff14\";\n    for (let r = 0; r < rows; r++) {\n        for (let c = 0; c < cols; c++) {\n            if (grid[r * cols + c]) {\n                ctx.fillRect(c * cell + 1, r * cell + 1, cell - 2, cell - 2);\n            }\n        }\n    }\n}",
  drawGetPointOnLine: "function drawGetPointOnLine(ctx, startPoint, endPoint, t, getPointOnLineFn) {\n    ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n    // dashed extension line — the part of the lerp that lives OUTSIDE [0,1]\n    const extStart = getPointOnLineFn(startPoint, endPoint, T_MIN);\n    const extEnd = getPointOnLineFn(startPoint, endPoint, T_MAX);\n    ctx.save();\n    ctx.setLineDash([6, 8]);\n    ctx.strokeStyle = \"rgba(244,114,182,0.35)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(extStart.x, extStart.y);\n    ctx.lineTo(extEnd.x, extEnd.y);\n    ctx.stroke();\n    ctx.restore();\n    // the actual segment A → B (t in [0,1])\n    ctx.strokeStyle = \"rgba(255,255,255,0.5)\";\n    ctx.lineWidth = 10;\n    ctx.lineCap = \"round\";\n    ctx.beginPath();\n    ctx.moveTo(startPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, endPoint.y);\n    ctx.stroke();\n    // evenly-spaced tick ghosts so the subdivision is visible\n    [0, 0.25, 0.5, 0.75, 1].forEach((tick) => {\n        const p = getPointOnLineFn(startPoint, endPoint, tick);\n        ctx.beginPath();\n        ctx.arc(p.x, p.y, 3, 0, 2 * Math.PI);\n        ctx.fillStyle = \"rgba(255,255,255,0.6)\";\n        ctx.fill();\n    });\n    // endpoint handles\n    [\n        [startPoint, \"A\"],\n        [endPoint, \"B\"],\n    ].forEach(([p, label]) => {\n        ctx.beginPath();\n        ctx.arc(p.x, p.y, 9, 0, 2 * Math.PI);\n        ctx.fillStyle = \"#818cf8\";\n        ctx.fill();\n        ctx.fillStyle = \"rgba(255,255,255,0.9)\";\n        ctx.font = \"bold 14px sans-serif\";\n        ctx.fillText(label, p.x + 12, p.y - 12);\n    });\n    // the moving marker — orange on the segment, pink when extrapolating\n    const point = getPointOnLineFn(startPoint, endPoint, t);\n    const extrapolating = t < 0 || t > 1;\n    ctx.fillStyle = extrapolating ? \"#f472b6\" : \"#f97316\";\n    ctx.beginPath();\n    ctx.arc(point.x, point.y, 8, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.fillStyle = \"rgba(255,255,255,0.95)\";\n    ctx.font = \"bold 13px sans-serif\";\n    ctx.fillText(`t = ${t.toFixed(2)}`, point.x + 12, point.y + 4);\n}",
  drawGravitationalLensing: "/** Standalone draw — self-contained, embed via `.toString()` in CodePen. */\nfunction drawGravitationalLensing(ctx, lensX, lensY, mass, numRays, time, stars) {\n    const W = ctx.canvas.width;\n    const H = ctx.canvas.height;\n    function rayExit(x0, y0, slope) {\n        const dx = W - x0;\n        const y_right = y0 + slope * dx;\n        if (y_right >= 0 && y_right <= H)\n            return { x: W, y: y_right };\n        if (slope < 0 && y0 > 0) {\n            const x_top = x0 + (-y0) / slope;\n            if (x_top >= x0 && x_top <= W)\n                return { x: x_top, y: 0 };\n        }\n        if (slope > 0 && y0 < H) {\n            const x_bot = x0 + (H - y0) / slope;\n            if (x_bot >= x0 && x_bot <= W)\n                return { x: x_bot, y: H };\n        }\n        return { x: W, y: y_right };\n    }\n    ctx.fillStyle = \"#02020c\";\n    ctx.fillRect(0, 0, W, H);\n    for (const s of stars) {\n        const twinkle = s.brightness * (0.7 + 0.3 * Math.sin(time * 0.8 + s.x));\n        ctx.beginPath();\n        ctx.arc(s.x, s.y, s.r, 0, 2 * Math.PI);\n        ctx.fillStyle = `rgba(200, 220, 255, ${twinkle})`;\n        ctx.fill();\n    }\n    const spacing = H / (numRays + 1);\n    const minImpact = 16;\n    for (let i = 1; i <= numRays; i++) {\n        const y0 = i * spacing;\n        const b = y0 - lensY;\n        if (Math.abs(b) < minImpact)\n            continue;\n        const slope = -mass / b;\n        const normDist = Math.abs(b) / (H / 2);\n        const alpha = Math.max(0.08, 0.65 - normDist * 0.45);\n        ctx.strokeStyle = `rgba(255, 195, 65, ${alpha})`;\n        ctx.lineWidth = 1;\n        ctx.beginPath();\n        ctx.moveTo(0, y0);\n        ctx.lineTo(lensX, y0);\n        const exit = rayExit(lensX, y0, slope);\n        ctx.lineTo(exit.x, exit.y);\n        ctx.stroke();\n    }\n    const pulse = 0.85 + 0.15 * Math.sin(time * 1.8);\n    const glow = ctx.createRadialGradient(lensX, lensY, 0, lensX, lensY, 60 * pulse);\n    glow.addColorStop(0, \"rgba(200, 240, 255, 0.55)\");\n    glow.addColorStop(0.3, \"rgba(120, 180, 255, 0.25)\");\n    glow.addColorStop(1, \"rgba(80,  120, 255, 0)\");\n    ctx.fillStyle = glow;\n    ctx.beginPath();\n    ctx.arc(lensX, lensY, 60 * pulse, 0, 2 * Math.PI);\n    ctx.fill();\n    const core = ctx.createRadialGradient(lensX - 3, lensY - 3, 0, lensX, lensY, 14);\n    core.addColorStop(0, \"#ffffff\");\n    core.addColorStop(0.4, \"#c8e8ff\");\n    core.addColorStop(1, \"#5080c0\");\n    ctx.beginPath();\n    ctx.arc(lensX, lensY, 14, 0, 2 * Math.PI);\n    ctx.fillStyle = core;\n    ctx.fill();\n}",
  drawKlimt: "/**\n * Step and render one frame of the Klimt animation.\n * Mutates each swirl's state in place. Call `createKlimtSwirls` once to build\n * the initial state, then call this every animation frame.\n * Self-contained (no module-level imports) so a CodePen can embed it via .toString().\n */\nfunction drawKlimt(ctx, width, height, swirls) {\n    const CURVES = [45, -45, 135, -135];\n    const BRICK_W = 18;\n    const BRICK_H = 9;\n    const BRICK_ALPHA = 0.24;\n    const deg2rad = (deg) => deg * (Math.PI / 180);\n    const randInt = (min, max) => Math.floor(Math.random() * (max + 1 - min) + min);\n    const seedXY = (q, w, h) => {\n        if (q === 'TL')\n            return { x: w * 0.25, y: h * 0.15 };\n        if (q === 'TR')\n            return { x: w * 0.75, y: h * 0.15 };\n        if (q === 'BL')\n            return { x: w * 0.75, y: h * 0.35 };\n        return { x: w * 0.25, y: h * 0.35 }; // BR\n    };\n    ctx.fillStyle = '#0a0705';\n    ctx.fillRect(0, 0, width, height);\n    for (const swirl of swirls) {\n        swirl.w = width;\n        swirl.h = height;\n        swirl.testCounter++;\n        if (swirl.testCounter % swirl.interval === 0) {\n            swirl.testCounter = 0;\n            const s = swirl.pool[swirl.poolCounter];\n            swirl.poolCounter = (swirl.poolCounter + 1) % swirl.pool.length;\n            swirl.curveCounter++;\n            swirl.curve *= 1.025;\n            s.rotation = deg2rad(swirl.curve);\n            if (swirl.curveCounter > swirl.curveQ) {\n                swirl.curve = CURVES[Math.floor(Math.random() * CURVES.length)];\n                swirl.curveCounter = 0;\n                swirl.curveQ = randInt(85, 150);\n            }\n            const prevIndex = swirl.poolCounter > 1 ? swirl.poolCounter - 2 : swirl.pool.length - 1;\n            const prev = swirl.pool[prevIndex];\n            s.x = prev.x + BRICK_H * Math.sin(prev.rotation);\n            s.y = prev.y - BRICK_H * Math.cos(prev.rotation);\n            const buffer = 1;\n            if (swirl.poolCounter === 0 || s.y < -buffer || s.x < -buffer || s.x > width + buffer || s.y > height + buffer) {\n                const np = seedXY(swirl.quadrant, width, height);\n                s.x = np.x;\n                s.y = np.y;\n            }\n            s.placed = true;\n        }\n        ctx.globalAlpha = BRICK_ALPHA;\n        for (const s of swirl.pool) {\n            if (!s.placed)\n                continue;\n            ctx.save();\n            ctx.translate(s.x, s.y);\n            ctx.rotate(s.rotation);\n            ctx.fillStyle = s.color;\n            ctx.fillRect(-BRICK_W / 2, -BRICK_H, BRICK_W, BRICK_H);\n            ctx.restore();\n        }\n        ctx.globalAlpha = 1;\n    }\n}",
  drawLerp: "function drawLerp(ctx, target, follower, lerpFactor, canvasWidth, canvasHeight) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    // Line connecting follower to target\n    ctx.strokeStyle = \"rgba(255,255,255,0.18)\";\n    ctx.lineWidth = 1.5;\n    ctx.setLineDash([5, 4]);\n    ctx.beginPath();\n    ctx.moveTo(follower.x, follower.y);\n    ctx.lineTo(target.x, target.y);\n    ctx.stroke();\n    ctx.setLineDash([]);\n    // Target (orange)\n    ctx.fillStyle = \"#f97316\";\n    ctx.beginPath();\n    ctx.arc(target.x, target.y, 9, 0, Math.PI * 2);\n    ctx.fill();\n    ctx.fillStyle = \"rgba(255,255,255,0.6)\";\n    ctx.font = \"bold 11px monospace\";\n    ctx.fillText(\"target\", target.x + 14, target.y + 4);\n    // Follower (white-indigo)\n    const gr = ctx.createRadialGradient(follower.x, follower.y, 0, follower.x, follower.y, 16);\n    gr.addColorStop(0, \"#c7d2fe\");\n    gr.addColorStop(1, \"#6366f1\");\n    ctx.fillStyle = gr;\n    ctx.beginPath();\n    ctx.arc(follower.x, follower.y, 16, 0, Math.PI * 2);\n    ctx.fill();\n    ctx.fillStyle = \"rgba(255,255,255,0.6)\";\n    ctx.fillText(\"follower\", follower.x + 20, follower.y + 4);\n    // Factor label in corner\n    ctx.fillStyle = \"rgba(255,255,255,0.35)\";\n    ctx.font = \"12px monospace\";\n    ctx.fillText(`lerp factor: ${lerpFactor.toFixed(3)}`, 12, canvasHeight - 14);\n}",
  drawLineLength: "function drawLineLength(ctx, startPoint, endPoint, canvasWidth, canvasHeight, lineLengthFn) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    ctx.font = \"bold 12px Verdana\";\n    ctx.fillStyle = \"rgba(255,255,255,0.85)\";\n    // ADJACENT\n    ctx.strokeStyle = \"rgba(255,255,255,0.55)\";\n    ctx.lineWidth = 1;\n    ctx.moveTo(startPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, startPoint.y);\n    let adjacent = lineLengthFn({\n        startPoint: { x: startPoint.x, y: startPoint.y },\n        endPoint: { x: endPoint.x, y: startPoint.y },\n    });\n    if (endPoint.x < startPoint.x)\n        adjacent *= -1;\n    ctx.fillText(`${Math.abs(adjacent)} pixels`, startPoint.x + adjacent / 2, startPoint.y - 10);\n    ctx.stroke();\n    // OPPOSITE\n    ctx.moveTo(endPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, endPoint.y);\n    let opposite = lineLengthFn({\n        startPoint: { x: endPoint.x, y: startPoint.y },\n        endPoint: { x: endPoint.x, y: endPoint.y },\n    });\n    if (endPoint.y > startPoint.y)\n        opposite *= -1;\n    let textY = startPoint.y - opposite * 0.75;\n    ctx.translate(endPoint.x, textY);\n    ctx.rotate(Math.PI / 2);\n    ctx.translate(-endPoint.x, -textY);\n    ctx.fillText(`${Math.abs(opposite)} pixels`, endPoint.x, textY + 14);\n    ctx.stroke();\n    ctx.resetTransform();\n    // HYPOTENUSE\n    let hypotenuse = Math.floor(lineLengthFn({\n        startPoint: startPoint,\n        endPoint: endPoint,\n    }));\n    ctx.strokeStyle = \"rgba(255,255,255,0.9)\";\n    ctx.lineWidth = 2;\n    ctx.moveTo(startPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, endPoint.y);\n    ctx.stroke();\n    textY = startPoint.y;\n    ctx.translate(startPoint.x, textY);\n    let angle = getHypAngle(startPoint, endPoint);\n    ctx.rotate(angle);\n    ctx.translate(-startPoint.x, -textY);\n    ctx.fillText(`${Math.abs(hypotenuse)} pixels`, startPoint.x + hypotenuse * 0.25, textY - 12);\n    ctx.stroke();\n    ctx.resetTransform();\n    ctx.beginPath();\n    ctx.strokeStyle = \"rgba(255,255,255,0.55)\";\n    ctx.lineWidth = 1;\n    ctx.arc(startPoint.x, startPoint.y, hypotenuse, 0, 2 * Math.PI);\n    ctx.stroke();\n}",
  drawLineToCircle: "function drawLineToCircle(ctx, line, circle, hit, halfWidth, time) {\n    const pulse = \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\";\n    ctx.fillStyle = hit ? pulse : \"#818cf8\";\n    ctx.beginPath();\n    ctx.arc(circle.x, circle.y, circle.radius, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.strokeStyle = hit ? pulse : \"#ff9f1c\";\n    ctx.lineWidth = 3;\n    ctx.beginPath();\n    ctx.moveTo(line.startPoint.x, line.startPoint.y);\n    ctx.lineTo(line.endPoint.x, line.endPoint.y);\n    ctx.stroke();\n    if (!hit)\n        return;\n    ctx.save();\n    ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n    ctx.textAlign = \"center\";\n    ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n    ctx.shadowBlur = 14;\n    ctx.fillStyle = \"#cdd3ff\";\n    ctx.fillText(\"collision detected\", halfWidth, 40);\n    ctx.restore();\n}",
  drawLineToLine: "function drawLineToLine(ctx, line1, line2, hit, halfWidth, time) {\n    const pulse = \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\";\n    ctx.lineWidth = 3;\n    ctx.strokeStyle = hit ? pulse : \"#ff9f1c\";\n    ctx.beginPath();\n    ctx.moveTo(line1.startPoint.x, line1.startPoint.y);\n    ctx.lineTo(line1.endPoint.x, line1.endPoint.y);\n    ctx.stroke();\n    ctx.strokeStyle = hit ? pulse : \"#818cf8\";\n    ctx.beginPath();\n    ctx.moveTo(line2.startPoint.x, line2.startPoint.y);\n    ctx.lineTo(line2.endPoint.x, line2.endPoint.y);\n    ctx.stroke();\n    if (!hit)\n        return;\n    ctx.save();\n    ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n    ctx.textAlign = \"center\";\n    ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n    ctx.shadowBlur = 14;\n    ctx.fillStyle = \"#cdd3ff\";\n    ctx.fillText(\"collision detected\", halfWidth, 40);\n    ctx.restore();\n}",
  drawLineToPoint: "function drawLineToPoint(ctx, line, point, radius, hit, canvasWidth, time = performance.now()) {\n    const pulseLightness = 55 + 25 * Math.sin(time / 120);\n    const hitColor = `hsl(330, 95%, ${pulseLightness}%)`;\n    ctx.strokeStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.lineWidth = 3;\n    ctx.beginPath();\n    ctx.moveTo(line.startPoint.x, line.startPoint.y);\n    ctx.lineTo(line.endPoint.x, line.endPoint.y);\n    ctx.stroke();\n    ctx.fillStyle = hit ? hitColor : \"#818cf8\";\n    ctx.beginPath();\n    ctx.arc(point.x, point.y, radius, 0, 2 * Math.PI);\n    ctx.fill();\n    if (!hit)\n        return;\n    ctx.save();\n    ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n    ctx.textAlign = \"center\";\n    ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n    ctx.shadowBlur = 14;\n    ctx.fillStyle = \"#cdd3ff\";\n    ctx.fillText(\"collision detected\", canvasWidth / 2, 40);\n    ctx.restore();\n}",
  drawLineToRectangle: "function drawLineToRectangle(ctx, polygon, line, hit, canvasWidth, time = performance.now()) {\n    const pulseLightness = 55 + 25 * Math.sin(time / 120);\n    const hitColor = `hsl(330, 95%, ${pulseLightness}%)`;\n    ctx.fillStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.beginPath();\n    polygon.vertices.forEach((vertex, i) => {\n        if (i === 0)\n            ctx.moveTo(vertex.x, vertex.y);\n        else\n            ctx.lineTo(vertex.x, vertex.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    ctx.strokeStyle = hit ? hitColor : \"#818cf8\";\n    ctx.lineWidth = 3;\n    ctx.beginPath();\n    ctx.moveTo(line.startPoint.x, line.startPoint.y);\n    ctx.lineTo(line.endPoint.x, line.endPoint.y);\n    ctx.stroke();\n    if (!hit)\n        return;\n    ctx.save();\n    ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n    ctx.textAlign = \"center\";\n    ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n    ctx.shadowBlur = 14;\n    ctx.fillStyle = \"#cdd3ff\";\n    ctx.fillText(\"collision detected\", canvasWidth / 2, 40);\n    ctx.restore();\n}",
  drawMoveItemAroundCircle: "function drawMoveItemAroundCircle(ctx, halfWidth, halfHeight, canvasWidth, canvasHeight, i, findPointAroundCircleFn) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    ctx.strokeStyle = \"green\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(0, halfHeight);\n    ctx.lineTo(canvasWidth, halfHeight);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.moveTo(halfWidth, 0);\n    ctx.lineTo(halfWidth, canvasHeight);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.arc(halfWidth, halfHeight, 200, 0, 2 * Math.PI);\n    ctx.stroke();\n    let point = findPointAroundCircleFn({ x: halfWidth, y: halfHeight }, 200, i);\n    ctx.beginPath();\n    ctx.arc(point.x, point.y, 20, 0, 2 * Math.PI);\n    ctx.stroke();\n    ctx.strokeStyle = \"rgba(255,255,255,0.2)\";\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(halfWidth, halfHeight);\n    ctx.lineTo(point.x, point.y);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.moveTo(point.x, point.y);\n    ctx.lineTo(point.x, halfHeight);\n    ctx.stroke();\n}",
  drawMoveToDestination: "function drawMoveToDestination(ctx, canvas, dot, dotNew, arrowPoint, ratio, img) {\n    if (!canvas)\n        return;\n    ctx.clearRect(0, 0, canvas.width, canvas.height);\n    ctx.strokeStyle = \"green\";\n    ctx.lineWidth = 10;\n    let newDotPoint = moveAlongLine(dot, dotNew, ratio);\n    dot.x = newDotPoint.x;\n    dot.y = newDotPoint.y;\n    ctx.beginPath();\n    ctx.arc(dot.x, dot.y, 20, 0, 2 * Math.PI);\n    ctx.stroke();\n    let newPoint = moveAlongLine(arrowPoint, dot, ratio);\n    arrowPoint.x = newPoint.x;\n    arrowPoint.y = newPoint.y;\n    let angle = getRotation(newPoint, dot);\n    ctx.translate(newPoint.x, newPoint.y);\n    ctx.rotate(angle);\n    ctx.translate(-newPoint.x, -newPoint.y);\n    ctx.drawImage(img, arrowPoint.x - 50, arrowPoint.y - 25);\n    ctx.resetTransform();\n}",
  drawOrbitalMotion: "function drawOrbitalMotion(ctx, canvasWidth, canvasHeight, pct, sunRadius, orbiterRadius, orbitRadius, findPointAroundCircleFn, sphereLightingFn) {\n    const cx = canvasWidth / 2;\n    const cy = canvasHeight / 2;\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    // faint orbit path\n    ctx.beginPath();\n    ctx.strokeStyle = \"rgba(255,255,255,0.12)\";\n    ctx.lineWidth = 1;\n    ctx.arc(cx, cy, orbitRadius, 0, Math.PI * 2);\n    ctx.stroke();\n    // sun\n    const sunGr = ctx.createRadialGradient(cx - sunRadius * 0.3, cy - sunRadius * 0.3, 0, cx, cy, sunRadius);\n    sunGr.addColorStop(0, \"#fff7a1\");\n    sunGr.addColorStop(0.35, \"#fde047\");\n    sunGr.addColorStop(0.75, \"#f97316\");\n    sunGr.addColorStop(1, \"#7c2d12\");\n    ctx.fillStyle = sunGr;\n    ctx.beginPath();\n    ctx.arc(cx, cy, sunRadius, 0, Math.PI * 2);\n    ctx.fill();\n    // corona glow\n    const coronaGr = ctx.createRadialGradient(cx, cy, sunRadius * 0.9, cx, cy, sunRadius * 1.6);\n    coronaGr.addColorStop(0, \"rgba(251,191,36,0.35)\");\n    coronaGr.addColorStop(1, \"rgba(251,191,36,0)\");\n    ctx.fillStyle = coronaGr;\n    ctx.beginPath();\n    ctx.arc(cx, cy, sunRadius * 1.6, 0, Math.PI * 2);\n    ctx.fill();\n    // orbiter position — reuses the existing findPointAroundCircle formula\n    const { x: ox, y: oy } = findPointAroundCircleFn({ x: cx, y: cy }, orbitRadius, pct);\n    // highlight position — the new SphereLighting formula does the real work\n    const highlight = sphereLightingFn({ x: ox, y: oy, radius: orbiterRadius }, { x: cx, y: cy });\n    const orbGr = ctx.createRadialGradient(highlight.x, highlight.y, 0, ox, oy, orbiterRadius);\n    orbGr.addColorStop(0, \"#bfdbfe\");\n    orbGr.addColorStop(0.45, \"#3b82f6\");\n    orbGr.addColorStop(1, \"#1e1b4b\");\n    ctx.fillStyle = orbGr;\n    ctx.beginPath();\n    ctx.arc(ox, oy, orbiterRadius, 0, Math.PI * 2);\n    ctx.fill();\n}",
  drawOrbitalPrecession: "/** Standalone draw — steps physics AND draws. Embed with `newtonAccel` and `grAccel` via `.toString()`. */\nfunction drawOrbitalPrecession(ctx, newton, gr, epsilon, stepsPerFrame) {\n    const GM = 400;\n    const MAX_TRAIL = 2400;\n    const TRAIL_CHUNKS = 12;\n    const W = ctx.canvas.width;\n    const H = ctx.canvas.height;\n    const sunX = W / 2;\n    const sunY = H / 2;\n    for (let i = 0; i < stepsPerFrame; i++) {\n        const [nax, nay] = newtonAccel(newton.x, newton.y, sunX, sunY, GM);\n        newton.vx += nax;\n        newton.vy += nay;\n        newton.x += newton.vx;\n        newton.y += newton.vy;\n        newton.trail.push({ x: newton.x, y: newton.y });\n        if (newton.trail.length > MAX_TRAIL)\n            newton.trail.shift();\n        const [gax, gay] = grAccel(gr.x, gr.y, sunX, sunY, GM, epsilon);\n        gr.vx += gax;\n        gr.vy += gay;\n        gr.x += gr.vx;\n        gr.y += gr.vy;\n        gr.trail.push({ x: gr.x, y: gr.y });\n        if (gr.trail.length > MAX_TRAIL)\n            gr.trail.shift();\n    }\n    ctx.fillStyle = \"#04040e\";\n    ctx.fillRect(0, 0, W, H);\n    function drawTrail(trail, r, g, b) {\n        if (trail.length < 2)\n            return;\n        const chunkSize = Math.max(1, Math.floor(trail.length / TRAIL_CHUNKS));\n        for (let c = 0; c < TRAIL_CHUNKS; c++) {\n            const start = c * chunkSize;\n            if (start >= trail.length)\n                break;\n            const end = Math.min(start + chunkSize + 1, trail.length);\n            const alpha = ((c + 1) / TRAIL_CHUNKS) * 0.75;\n            ctx.strokeStyle = `rgba(${r},${g},${b},${alpha})`;\n            ctx.lineWidth = 1.2;\n            ctx.beginPath();\n            ctx.moveTo(trail[start].x, trail[start].y);\n            for (let i = start + 1; i < end; i++)\n                ctx.lineTo(trail[i].x, trail[i].y);\n            ctx.stroke();\n        }\n    }\n    drawTrail(newton.trail, 96, 165, 250);\n    drawTrail(gr.trail, 251, 191, 36);\n    const R = 22;\n    const glow = ctx.createRadialGradient(sunX, sunY, R * 0.8, sunX, sunY, R * 2.2);\n    glow.addColorStop(0, \"rgba(253,186,36,0.3)\");\n    glow.addColorStop(1, \"rgba(253,186,36,0)\");\n    ctx.fillStyle = glow;\n    ctx.beginPath();\n    ctx.arc(sunX, sunY, R * 2.2, 0, 2 * Math.PI);\n    ctx.fill();\n    const grad = ctx.createRadialGradient(sunX - R * 0.3, sunY - R * 0.3, 0, sunX, sunY, R);\n    grad.addColorStop(0, \"#fff7a1\");\n    grad.addColorStop(0.4, \"#fde047\");\n    grad.addColorStop(0.8, \"#f97316\");\n    grad.addColorStop(1, \"#7c2d12\");\n    ctx.fillStyle = grad;\n    ctx.beginPath();\n    ctx.arc(sunX, sunY, R, 0, 2 * Math.PI);\n    ctx.fill();\n    function drawPlanet(body, color, radius) {\n        ctx.beginPath();\n        ctx.arc(body.x, body.y, radius, 0, 2 * Math.PI);\n        ctx.fillStyle = color;\n        ctx.fill();\n        ctx.strokeStyle = \"rgba(255,255,255,0.5)\";\n        ctx.lineWidth = 1;\n        ctx.stroke();\n    }\n    drawPlanet(newton, \"#60a5fa\", 9);\n    drawPlanet(gr, \"#fbbf24\", 9);\n    ctx.font = \"bold 11px monospace\";\n    ctx.textAlign = \"right\";\n    ctx.fillStyle = \"rgba(96,165,250,0.85)\";\n    ctx.fillText(\"Newtonian\", W - 16, H - 32);\n    ctx.fillStyle = \"rgba(251,191,36,0.85)\";\n    ctx.fillText(\"General Relativity\", W - 16, H - 16);\n    ctx.textAlign = \"left\";\n}",
  drawPhyllotaxis: "function drawPhyllotaxis(ctx, width, height, state) {\n    const getSpiralCounts = () => {\n        const fibs = [1, 1];\n        while (fibs[fibs.length - 1] < state.numSeeds) {\n            const n = fibs.length;\n            fibs.push(fibs[n - 1] + fibs[n - 2]);\n        }\n        let cwCount = 1;\n        let ccwCount = 1;\n        for (let i = 0; i + 1 < fibs.length; i++) {\n            if (fibs[i + 1] <= state.numSeeds) {\n                cwCount = fibs[i];\n                ccwCount = fibs[i + 1];\n            }\n            else {\n                break;\n            }\n        }\n        return { cwCount, ccwCount };\n    };\n    ctx.fillStyle = \"#0a0a0f\";\n    ctx.fillRect(0, 0, width, height);\n    ctx.save();\n    ctx.translate(width / 2, height / 2);\n    ctx.rotate((state.rotationAngleDeg * Math.PI) / 180);\n    let cwCount = 1;\n    let ccwCount = 1;\n    if (state.showSpirals) {\n        ({ cwCount, ccwCount } = getSpiralCounts());\n        const drawFamily = (step, color) => {\n            ctx.strokeStyle = color;\n            ctx.lineWidth = 1;\n            ctx.setLineDash([3, 3]);\n            for (let i = 0; i + step < state.numSeeds; i++) {\n                const [x0, y0] = phyllotaxisPoint(i, state.angleDeg, state.scale);\n                const [x1, y1] = phyllotaxisPoint(i + step, state.angleDeg, state.scale);\n                ctx.beginPath();\n                ctx.moveTo(x0, y0);\n                ctx.lineTo(x1, y1);\n                ctx.stroke();\n            }\n            ctx.setLineDash([]);\n        };\n        drawFamily(cwCount, \"rgba(100,220,255,0.35)\");\n        drawFamily(ccwCount, \"rgba(255,180,80,0.35)\");\n    }\n    for (let n = 0; n < state.numSeeds; n++) {\n        const [x, y] = phyllotaxisPoint(n, state.angleDeg, state.scale);\n        let color;\n        if (state.colorMode === \"gradient\") {\n            const t = n / Math.max(1, state.numSeeds - 1);\n            const hue = 30 + t * 200;\n            const sat = 80 + t * 15;\n            const lit = 45 + t * 25;\n            color = `hsl(${hue},${sat}%,${lit}%)`;\n        }\n        else {\n            color = \"#f0e6c8\";\n        }\n        ctx.beginPath();\n        ctx.arc(x, y, state.seedRadius, 0, 2 * Math.PI);\n        ctx.fillStyle = color;\n        ctx.fill();\n    }\n    ctx.restore();\n    if (state.showSpirals) {\n        ctx.fillStyle = \"#e0f0ff\";\n        ctx.font = \"bold 12px monospace\";\n        ctx.textAlign = \"left\";\n        ctx.fillText(`Spirals: ${cwCount} CW (cyan)  ·  ${ccwCount} CCW (orange)  — both Fibonacci`, 14, height - 14);\n    }\n}",
  drawPointToCircle: "function drawPointToCircle(ctx, canvasWidth, canvasHeight, time, sineCurveFn, pointCircleFn) {\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const circle = { x: halfWidth, y: halfHeight, radius: 100 };\n    const x = sineCurveFn(halfWidth, 200, 0.001, time);\n    const y = sineCurveFn(halfHeight, 200, 0.001, time);\n    const hit = pointCircleFn({ x, y }, circle);\n    ctx.fillStyle = hit ? \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\" : \"#ff9f1c\";\n    ctx.beginPath();\n    ctx.arc(circle.x, circle.y, circle.radius, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.beginPath();\n    ctx.fillStyle = hit ? \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\" : \"#818cf8\";\n    ctx.arc(x, y, 5, 0, 2 * Math.PI);\n    ctx.fill();\n    if (hit) {\n        ctx.save();\n        ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n        ctx.textAlign = \"center\";\n        ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n        ctx.shadowBlur = 14;\n        ctx.fillStyle = \"#cdd3ff\";\n        ctx.fillText(\"collision detected\", halfWidth, 40);\n        ctx.restore();\n    }\n}",
  drawPointToRectangle: "function drawPointToRectangle(ctx, canvasWidth, canvasHeight, time, sineCurveFn, polygonPointFn, rectangleFn) {\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    ctx.fillStyle = \"transparent\";\n    const x = sineCurveFn(halfWidth, 200, 0.001, time);\n    const y = sineCurveFn(halfHeight, 200, 0.001, time);\n    const rect = rectangleFn(100, 100, 0, {\n        rotate: true,\n        rotateSpeed: 2000,\n        time,\n    });\n    rect.vertices.forEach((vertex) => {\n        vertex.x += halfWidth;\n        vertex.y += halfHeight;\n    });\n    const hit = polygonPointFn(rect, { x, y });\n    const hitColor = \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\";\n    ctx.fillStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.beginPath();\n    rect.vertices.forEach((vertex, index) => {\n        if (index === 0) {\n            ctx.moveTo(vertex.x, vertex.y);\n            return;\n        }\n        ctx.lineTo(vertex.x, vertex.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    ctx.fillStyle = hit ? hitColor : \"#818cf8\";\n    ctx.beginPath();\n    ctx.arc(x, y, 5, 0, 2 * Math.PI);\n    ctx.fill();\n    if (hit) {\n        ctx.save();\n        ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n        ctx.textAlign = \"center\";\n        ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n        ctx.shadowBlur = 14;\n        ctx.fillStyle = \"#cdd3ff\";\n        ctx.fillText(\"collision detected\", halfWidth, 40);\n        ctx.restore();\n    }\n}",
  drawPointTowards: "function drawPointTowards(ctx, canvasWidth, canvasHeight, i, img) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    ctx.strokeStyle = \"rgba(255,255,255,0.35)\";\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(0, halfHeight);\n    ctx.lineTo(canvasWidth, halfHeight);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.moveTo(halfWidth, 0);\n    ctx.lineTo(halfWidth, canvasHeight);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.arc(halfWidth, halfHeight, 200, 0, 2 * Math.PI);\n    ctx.stroke();\n    ctx.strokeStyle = \"rgba(255,255,255,0.4)\";\n    ctx.lineWidth = 2;\n    let point = pointsAroundCircle({ x: halfWidth, y: halfHeight }, i, 200, 360);\n    ctx.beginPath();\n    ctx.arc(point.x, point.y, 20, 0, 2 * Math.PI);\n    ctx.stroke();\n    let angle = getRotation({\n        x: halfWidth,\n        y: halfHeight,\n    }, point);\n    ctx.translate(halfWidth, halfHeight);\n    ctx.rotate(angle);\n    ctx.translate(-halfWidth, -halfHeight);\n    ctx.drawImage(img, halfWidth - 50, halfHeight - 25);\n    ctx.resetTransform();\n}",
  drawPolygon: "function drawPolygon(ctx, rect, halfWidth, halfHeight) {\n    ctx.strokeStyle = \"rgba(255,255,255,0.85)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    rect.vertices.forEach((corner, i) => {\n        let { x, y } = corner;\n        x += halfWidth;\n        y += halfHeight;\n        if (i === 0) {\n            ctx.moveTo(x, y);\n        }\n        else {\n            ctx.lineTo(x, y);\n        }\n    });\n    ctx.closePath();\n    ctx.stroke();\n}",
  drawPolygonToPolygon: "function drawPolygonToPolygon(ctx, canvasWidth, canvasHeight, time, sineCurveFn, polygonPolygonFn, starFn) {\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    ctx.fillStyle = \"#1e1b4b\";\n    ctx.rect(0, 0, canvasWidth, canvasHeight);\n    ctx.fill();\n    // small 5-spike moving star\n    const star1 = starFn(5, 50, 25, 0, { rotate: true, rotateSpeed: 2000, time });\n    // large 9-spike center star\n    const star2 = starFn(9, 150, 25, 0, { rotate: true, rotateSpeed: 2800, time });\n    const vertices1 = [];\n    star2.vertices.forEach((pt) => {\n        vertices1.push({ x: halfWidth + pt.x, y: halfHeight + pt.y });\n    });\n    const x = sineCurveFn(halfWidth, 200, 0.001, time);\n    const y = sineCurveFn(halfHeight, 200, 0.001, time);\n    const vertices2 = [];\n    star1.vertices.forEach((pt) => {\n        vertices2.push({ x: x + pt.x, y: y + pt.y });\n    });\n    const hit = polygonPolygonFn({ vertices: vertices1 }, { vertices: vertices2 });\n    const hitColor = \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\";\n    // large center star\n    ctx.fillStyle = hit ? hitColor : \"#818cf8\";\n    ctx.beginPath();\n    vertices1.forEach((pt, i) => {\n        if (i === 0)\n            ctx.moveTo(pt.x, pt.y);\n        else\n            ctx.lineTo(pt.x, pt.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    // small moving star\n    ctx.fillStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.beginPath();\n    vertices2.forEach((pt, i) => {\n        if (i === 0)\n            ctx.moveTo(pt.x, pt.y);\n        else\n            ctx.lineTo(pt.x, pt.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    if (hit) {\n        ctx.save();\n        ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n        ctx.textAlign = \"center\";\n        ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n        ctx.shadowBlur = 14;\n        ctx.fillStyle = \"#cdd3ff\";\n        ctx.fillText(\"collision detected\", halfWidth, 40);\n        ctx.restore();\n    }\n}",
  drawQuadraticBezier: "function drawQuadraticBezier(ctx, p0, p1, p2, dragTarget, quadraticBezierFn) {\n    ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n    // ── Dashed control polygon  P0 → P1 → P2 ──────────────────────────────\n    ctx.save();\n    ctx.strokeStyle = \"rgba(255,255,255,0.18)\";\n    ctx.lineWidth = 1.2;\n    ctx.setLineDash([6, 5]);\n    ctx.beginPath();\n    ctx.moveTo(p0.x, p0.y);\n    ctx.lineTo(p1.x, p1.y);\n    ctx.lineTo(p2.x, p2.y);\n    ctx.stroke();\n    ctx.restore();\n    // ── Bezier curve (100 steps) ──────────────────────────────────────────\n    ctx.strokeStyle = \"#818cf8\";\n    ctx.lineWidth = 3;\n    ctx.beginPath();\n    for (let i = 0; i <= 100; i++) {\n        const pt = quadraticBezierFn(i / 100, p0, p1, p2);\n        i === 0 ? ctx.moveTo(pt.x, pt.y) : ctx.lineTo(pt.x, pt.y);\n    }\n    ctx.stroke();\n    // ── Moving dot (oscillates along the curve) ───────────────────────────\n    const tDot = (Math.sin(Date.now() * 0.0009) + 1) / 2;\n    const dotPt = quadraticBezierFn(tDot, p0, p1, p2);\n    ctx.fillStyle = \"#f97316\";\n    ctx.beginPath();\n    ctx.arc(dotPt.x, dotPt.y, 7, 0, Math.PI * 2);\n    ctx.fill();\n    // ── Handles ───────────────────────────────────────────────────────────\n    const handles = [\n        { key: \"p0\", pt: p0, label: \"P0\", color: \"#34d399\" },\n        { key: \"p1\", pt: p1, label: \"P1 (control)\", color: \"#f472b6\" },\n        { key: \"p2\", pt: p2, label: \"P2\", color: \"#34d399\" },\n    ];\n    ctx.font = \"bold 10px monospace\";\n    ctx.textAlign = \"center\";\n    handles.forEach(({ key, pt, label, color }) => {\n        // Glow on active drag\n        if (dragTarget === key) {\n            ctx.save();\n            ctx.globalAlpha = 0.3;\n            ctx.fillStyle = color;\n            ctx.beginPath();\n            ctx.arc(pt.x, pt.y, HANDLE_RADIUS * 1.8, 0, Math.PI * 2);\n            ctx.fill();\n            ctx.restore();\n        }\n        // Circle\n        ctx.fillStyle = color;\n        ctx.beginPath();\n        ctx.arc(pt.x, pt.y, HANDLE_RADIUS, 0, Math.PI * 2);\n        ctx.fill();\n        // Label inside handle\n        ctx.fillStyle = \"rgba(0,0,0,0.75)\";\n        ctx.fillText(label.length > 2 ? \"P1\" : label, pt.x, pt.y + 4);\n        // Full label below\n        ctx.fillStyle = \"rgba(255,255,255,0.5)\";\n        ctx.font = \"10px monospace\";\n        ctx.fillText(label, pt.x, pt.y + HANDLE_RADIUS + 14);\n        ctx.font = \"bold 10px monospace\";\n    });\n    ctx.textAlign = \"left\";\n}",
  drawRadiansToDegrees: "function drawRadiansToDegrees(ctx, width, height, radians) {\n    function radToDeg(rad) {\n        return (rad * 180) / Math.PI;\n    }\n    const degrees = radToDeg(radians);\n    const cx = width / 2, cy = height / 2;\n    const radius = Math.min(width, height) * 0.28;\n    ctx.fillStyle = '#0a0a0f';\n    ctx.fillRect(0, 0, width, height);\n    ctx.strokeStyle = 'rgba(255,255,255,0.2)';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.arc(cx, cy, radius, 0, Math.PI * 2);\n    ctx.stroke();\n    ctx.strokeStyle = '#a78bfa';\n    ctx.lineWidth = 5;\n    ctx.beginPath();\n    ctx.moveTo(cx, cy);\n    ctx.lineTo(cx + Math.cos(radians) * radius, cy + Math.sin(radians) * radius);\n    ctx.stroke();\n    ctx.fillStyle = '#d8e2ff';\n    ctx.font = '16px monospace';\n    ctx.fillText(radians.toFixed(3) + ' radians = ' + degrees.toFixed(1) + ' degrees', 18, 32);\n}",
  drawRainbowBall: "/**\n * Pure per-ball renderer — a rainbow radial-gradient sphere with a glossy\n * highlight. References nothing outside its args, so it ports straight to CodePen\n * via `.toString()`. Color comes from the ball's `hue` as a CSS `hsl()` string.\n */\nfunction drawRainbowBall(ctx, ball) {\n    const gr = ctx.createRadialGradient(ball.x - ball.radius * 0.32, ball.y - ball.radius * 0.32, 0, ball.x, ball.y, ball.radius);\n    gr.addColorStop(0, `hsl(${ball.hue}, 90%, 78%)`);\n    gr.addColorStop(0.55, `hsl(${ball.hue}, 85%, 60%)`);\n    gr.addColorStop(1, `hsl(${ball.hue}, 80%, 38%)`);\n    ctx.fillStyle = gr;\n    ctx.beginPath();\n    ctx.arc(ball.x, ball.y, ball.radius, 0, Math.PI * 2);\n    ctx.fill();\n    // glossy highlight\n    ctx.fillStyle = \"rgba(255,255,255,0.35)\";\n    ctx.beginPath();\n    ctx.arc(ball.x - ball.radius * 0.3, ball.y - ball.radius * 0.35, ball.radius * 0.18, 0, Math.PI * 2);\n    ctx.fill();\n}",
  drawRandomIntegerBetween: "function drawRandomIntegerBetween(ctx, width, height, values) {\n    ctx.fillStyle = '#0a0a0f';\n    ctx.fillRect(0, 0, width, height);\n    const size = Math.min(58, width / 10);\n    const startX = (width - size * 7) / 2;\n    values.forEach((v, i) => {\n        const x = startX + (i % 7) * size;\n        const y = height / 2 - size * 2 + Math.floor(i / 7) * size;\n        ctx.fillStyle = '#818cf8';\n        ctx.fillRect(x + 5, y + 5, size - 10, size - 10);\n        ctx.fillStyle = '#fff';\n        ctx.font = 'bold 20px monospace';\n        ctx.textAlign = 'center';\n        ctx.fillText(String(v), x + size / 2, y + size / 2 + 7);\n    });\n}",
  drawRandomNumberBetween: "function drawRandomNumberBetween(ctx, width, height, samples, min, max) {\n    const lineY = Math.round(height * 0.62);\n    const left = 32;\n    const right = width - 32;\n    const usableWidth = Math.max(1, right - left);\n    const latest = samples[0] ?? min;\n    ctx.fillStyle = '#0a0a0f';\n    ctx.fillRect(0, 0, width, height);\n    ctx.textAlign = 'left';\n    ctx.fillStyle = '#d8e2ff';\n    ctx.font = '14px monospace';\n    ctx.fillText(`randomNumberBetween(${min}, ${max})`, 18, 28);\n    ctx.font = '12px monospace';\n    ctx.fillStyle = 'rgba(216,226,255,0.72)';\n    ctx.fillText('returns a decimal anywhere in this range', 18, 48);\n    ctx.strokeStyle = 'rgba(255,255,255,0.22)';\n    ctx.lineWidth = 4;\n    ctx.beginPath();\n    ctx.moveTo(left, lineY);\n    ctx.lineTo(right, lineY);\n    ctx.stroke();\n    ctx.fillStyle = '#d8e2ff';\n    ctx.font = '13px monospace';\n    ctx.textAlign = 'center';\n    ctx.fillText(String(min), left, lineY + 26);\n    ctx.fillText(String(max), right, lineY + 26);\n    samples.slice(1).forEach((sample, i) => {\n        const x = left + ((sample - min) / (max - min)) * usableWidth;\n        const alpha = Math.max(0.12, 0.55 - i * 0.07);\n        const y = lineY - 26 - Math.min(i, 5) * 10;\n        ctx.fillStyle = `rgba(129, 140, 248, ${alpha})`;\n        ctx.beginPath();\n        ctx.arc(x, y, 3, 0, Math.PI * 2);\n        ctx.fill();\n    });\n    const latestX = left + ((latest - min) / (max - min)) * usableWidth;\n    ctx.strokeStyle = '#818cf8';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(latestX, lineY - 34);\n    ctx.lineTo(latestX, lineY + 6);\n    ctx.stroke();\n    ctx.fillStyle = '#818cf8';\n    ctx.beginPath();\n    ctx.arc(latestX, lineY, 8, 0, Math.PI * 2);\n    ctx.fill();\n    ctx.fillStyle = '#ffffff';\n    ctx.font = 'bold 22px monospace';\n    ctx.textAlign = 'center';\n    ctx.fillText(latest.toFixed(2), width / 2, lineY - 64);\n}",
  drawRectToRect: "function drawRectToRect(ctx, canvasWidth, canvasHeight, time, sineCurveFn, polygonPolygonFn, rectangleFn) {\n    const halfWidth = canvasWidth / 2;\n    const halfHeight = canvasHeight / 2;\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const x = sineCurveFn(halfWidth, 200, 0.001, time);\n    const y = sineCurveFn(halfHeight, 200, 0.001, time);\n    const rect1 = rectangleFn(100, 100, 0, {\n        rotate: true,\n        rotateSpeed: 2000,\n        time,\n    });\n    const rect2 = rectangleFn(100, 100, 0, {\n        rotate: true,\n        rotateSpeed: 2000,\n        time,\n    });\n    rect1.vertices.forEach((vertex) => {\n        vertex.x += x;\n        vertex.y += y;\n    });\n    rect2.vertices.forEach((vertex) => {\n        vertex.x += halfWidth;\n        vertex.y += halfHeight;\n    });\n    const hit = polygonPolygonFn(rect1, rect2);\n    const hitColor = \"hsl(330, 95%, \" + (55 + 25 * Math.sin(time / 120)) + \"%)\";\n    ctx.fillStyle = hit ? hitColor : \"#ff9f1c\";\n    ctx.beginPath();\n    rect1.vertices.forEach((vertex, index) => {\n        if (index === 0) {\n            ctx.moveTo(vertex.x, vertex.y);\n            return;\n        }\n        ctx.lineTo(vertex.x, vertex.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    ctx.fillStyle = hit ? hitColor : \"#818cf8\";\n    ctx.beginPath();\n    rect2.vertices.forEach((vertex, index) => {\n        if (index === 0) {\n            ctx.moveTo(vertex.x, vertex.y);\n            return;\n        }\n        ctx.lineTo(vertex.x, vertex.y);\n    });\n    ctx.closePath();\n    ctx.fill();\n    if (hit) {\n        ctx.save();\n        ctx.font = \"600 16px ui-monospace, 'Courier New', monospace\";\n        ctx.textAlign = \"center\";\n        ctx.shadowColor = \"rgba(129, 140, 248, 0.9)\";\n        ctx.shadowBlur = 14;\n        ctx.fillStyle = \"#cdd3ff\";\n        ctx.fillText(\"collision detected\", halfWidth, 40);\n        ctx.restore();\n    }\n}",
  drawSierpinski: "/**\n * Step and render one frame of the infinite-zoom Sierpinski animation.\n * Mutates `state` in place. Embed `sierpinskiMidpoints.toString()` in the\n * same scope before calling this from a CodePen.\n */\nfunction drawSierpinski(ctx, width, height, state) {\n    const ratioIncrease = 0.01;\n    const expandIncrease = Math.pow(2, ratioIncrease);\n    const threshold = Math.max(width, height) * 2;\n    state.rotation += 0.01;\n    ctx.fillStyle = '#000000';\n    ctx.fillRect(0, 0, width, height);\n    ctx.save();\n    ctx.translate(width / 2, height / 2);\n    ctx.rotate(state.rotation);\n    for (const t of state.triangles) {\n        t.radius *= expandIncrease;\n        const [m1, m2, m3] = sierpinskiMidpoints(t.radius, t.phase);\n        ctx.fillStyle = '#ffffff';\n        for (const pt of [m1, m2, m3]) {\n            ctx.beginPath();\n            ctx.arc(pt.x, pt.y, 2, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        ctx.strokeStyle = '#ffd900';\n        ctx.lineWidth = 1;\n        const drawEdge = (a, b) => {\n            ctx.beginPath();\n            ctx.moveTo(a.x, a.y);\n            ctx.lineTo(a.x + t.ratio * (b.x - a.x), a.y + t.ratio * (b.y - a.y));\n            ctx.stroke();\n        };\n        drawEdge(m1, m2);\n        drawEdge(m2, m3);\n        drawEdge(m3, m1);\n        if (t.ratio < 1) {\n            t.ratio = Math.min(1, t.ratio + ratioIncrease);\n        }\n        else if (!t.hasSpawned && t.radius / 2 >= 1) {\n            t.hasSpawned = true;\n            state.triangles.push({ radius: t.radius / 2, phase: t.phase + Math.PI / 3, ratio: 0, hasSpawned: false });\n        }\n    }\n    state.triangles = state.triangles.filter(t => t.radius <= threshold);\n    ctx.restore();\n}",
  drawSineCurve: "function drawSineCurve(ctx, canvasWidth, canvasHeight, startValue, differential, speed, sineCurveFn) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    ctx.strokeStyle = \"green\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(0, canvasHeight / 2);\n    ctx.lineTo(canvasWidth, canvasHeight / 2);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.moveTo(canvasWidth / 2, 0);\n    ctx.lineTo(canvasWidth / 2, canvasHeight);\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.arc(canvasWidth / 2, canvasHeight / 2, 200, 0, 2 * Math.PI);\n    ctx.stroke();\n    let val = sineCurveFn(startValue, differential, speed, performance.now());\n    ctx.beginPath();\n    ctx.arc(canvasWidth / 2, canvasHeight / 2 + val, 20, 0, 2 * Math.PI);\n    ctx.stroke();\n}",
  drawSpring: "function drawSpring(ctx, canvasWidth, canvasHeight, target, lanes) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    // the moving target — a vertical line every lane chases\n    ctx.strokeStyle = \"rgba(52,211,153,0.6)\";\n    ctx.setLineDash([6, 6]);\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(target, 0);\n    ctx.lineTo(target, canvasHeight);\n    ctx.stroke();\n    ctx.setLineDash([]);\n    ctx.fillStyle = \"rgba(52,211,153,0.7)\";\n    ctx.font = \"12px monospace\";\n    ctx.fillText(\"target\", target + 8, 18);\n    for (const lane of lanes) {\n        // rail\n        ctx.strokeStyle = \"rgba(255,255,255,0.12)\";\n        ctx.lineWidth = 2;\n        ctx.beginPath();\n        ctx.moveTo(40, lane.y);\n        ctx.lineTo(canvasWidth - 40, lane.y);\n        ctx.stroke();\n        // the spring \"coil\" from the rail start to the follower\n        ctx.strokeStyle = \"rgba(255,255,255,0.18)\";\n        ctx.lineWidth = 1.5;\n        ctx.beginPath();\n        const coils = 14;\n        const x0 = 40;\n        const span = lane.pos - x0;\n        ctx.moveTo(x0, lane.y);\n        for (let i = 1; i <= coils; i++) {\n            const t = i / coils;\n            const x = x0 + span * t;\n            const y = lane.y + (i % 2 === 0 ? -8 : 8) * (i === coils ? 0 : 1);\n            ctx.lineTo(x, y);\n        }\n        ctx.stroke();\n        // follower\n        ctx.fillStyle = lane.color;\n        ctx.beginPath();\n        ctx.arc(lane.pos, lane.y, 13, 0, Math.PI * 2);\n        ctx.fill();\n        ctx.fillStyle = lane.color;\n        ctx.font = \"bold 13px monospace\";\n        ctx.fillText(lane.label, 40, lane.y - 24);\n    }\n}",
  drawStar: "function drawStar(ctx, star, halfWidth, halfHeight) {\n    ctx.strokeStyle = \"rgba(255,255,255,0.85)\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    star.vertices.forEach((vertex, i) => {\n        if (i === 0) {\n            ctx.moveTo(halfWidth + vertex.x, halfHeight + vertex.y);\n        }\n        else {\n            ctx.lineTo(halfWidth + vertex.x, halfHeight + vertex.y);\n        }\n    });\n    ctx.closePath();\n    ctx.stroke();\n}",
  drawTriangleDataFromLine: "function drawTriangleDataFromLine(ctx, startPoint, endPoint, textDiv) {\n    ctx.clearRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n    ctx.fillStyle = \"rgba(255,255,255,0.85)\";\n    ctx.beginPath();\n    ctx.strokeStyle = \"green\";\n    ctx.lineWidth = 3;\n    ctx.moveTo(startPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, endPoint.y);\n    ctx.stroke();\n    ctx.fillText(\"A\", startPoint.x, startPoint.y);\n    ctx.strokeStyle = \"rgba(255,255,255,0.65)\";\n    ctx.lineWidth = 1;\n    ctx.moveTo(startPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, startPoint.y);\n    ctx.stroke();\n    ctx.fillText(\"C\", endPoint.x, startPoint.y);\n    ctx.moveTo(endPoint.x, startPoint.y);\n    ctx.lineTo(endPoint.x, endPoint.y);\n    ctx.stroke();\n    ctx.fillText(\"B\", endPoint.x, endPoint.y);\n    let radius = distanceBetweenPoints(startPoint, endPoint);\n    let { angleInDegrees, remainingAngle, hypotenuse, opposite, adjacent } = calculateTriangleData(startPoint, endPoint);\n    if (textDiv) {\n        textDiv.innerHTML = `\n        <h3>click and drag to draw line and get data.</h3>\n        <h5>the hypotenuse  is ${Math.floor(hypotenuse)} pixels long.</h5>\n        <h5>the adjacent  is ${Math.floor(adjacent)} pixels long.</h5>\n        <h5>the opposite  is ${Math.floor(opposite)} pixels long.</h5>\n        <h5>angle \"A\" is ${Math.floor(angleInDegrees)} degrees.</h5>\n        <h5>angle \"B\" is ${Math.floor(remainingAngle)} degrees.</h5>\n        <h5>angle \"C\" is 90 degrees.</h3>\n        `;\n    }\n    ctx.beginPath();\n    ctx.strokeStyle = \"rgba(255,255,255,0.55)\";\n    ctx.lineWidth = 1;\n    ctx.arc(startPoint.x, startPoint.y, radius, 0, 2 * Math.PI);\n    ctx.stroke();\n}",
  drawVectorReflect: "function drawVectorReflect(ctx, cx, cy, incoming, normal, reflected, wallDir, canvasWidth, canvasHeight) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    const L = 150;\n    const wlen = Math.max(canvasWidth, canvasHeight);\n    // The wall\n    ctx.strokeStyle = \"rgba(255,255,255,0.5)\";\n    ctx.lineWidth = 4;\n    ctx.beginPath();\n    ctx.moveTo(cx - wallDir.x * wlen, cy - wallDir.y * wlen);\n    ctx.lineTo(cx + wallDir.x * wlen, cy + wallDir.y * wlen);\n    ctx.stroke();\n    // Surface normal (dashed, green)\n    ctx.setLineDash([5, 5]);\n    drawArrow(ctx, cx, cy, cx + normal.x * L * 0.6, cy + normal.y * L * 0.6, \"rgba(52,211,153,0.95)\", \"normal\");\n    ctx.setLineDash([]);\n    // Incoming (orange, arriving at center) and reflected (indigo, leaving)\n    drawArrow(ctx, cx - incoming.x * L, cy - incoming.y * L, cx, cy, \"#f97316\", \"incoming\");\n    drawArrow(ctx, cx, cy, cx + reflected.x * L, cy + reflected.y * L, \"#6366f1\", \"reflected\");\n    ctx.fillStyle = \"#fff\";\n    ctx.beginPath();\n    ctx.arc(cx, cy, 4, 0, Math.PI * 2);\n    ctx.fill();\n}",
  drawVectorRotate: "function drawVectorRotate(ctx, cx, cy, basePoints, angle, canvasWidth, canvasHeight, rotateFn) {\n    ctx.clearRect(0, 0, canvasWidth, canvasHeight);\n    // faint center cross\n    ctx.strokeStyle = \"rgba(255,255,255,0.12)\";\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(0, cy);\n    ctx.lineTo(canvasWidth, cy);\n    ctx.moveTo(cx, 0);\n    ctx.lineTo(cx, canvasHeight);\n    ctx.stroke();\n    const rotated = basePoints.map((p) => rotateFn(p, angle));\n    rotated.forEach((p, i) => {\n        const x = cx + p.x;\n        const y = cy + p.y;\n        ctx.strokeStyle = \"rgba(99,102,241,0.5)\";\n        ctx.lineWidth = 2;\n        ctx.beginPath();\n        ctx.moveTo(cx, cy);\n        ctx.lineTo(x, y);\n        ctx.stroke();\n        ctx.fillStyle = i === 0 ? \"#f97316\" : \"#c7d2fe\";\n        ctx.beginPath();\n        ctx.arc(x, y, i === 0 ? 8 : 5, 0, Math.PI * 2);\n        ctx.fill();\n    });\n    ctx.fillStyle = \"#fff\";\n    ctx.beginPath();\n    ctx.arc(cx, cy, 4, 0, Math.PI * 2);\n    ctx.fill();\n    ctx.fillStyle = \"rgba(255,255,255,0.4)\";\n    ctx.font = \"12px monospace\";\n    ctx.fillText(`angle: ${(angle % (Math.PI * 2)).toFixed(2)} rad`, 12, canvasHeight - 14);\n}",
  drawWaveInterference: "/** Standalone draw — embed via `.toString()` in CodePen alongside nothing else (self-contained). */\nfunction drawWaveInterference(ctx, sources, wavelength, _speed, time) {\n    const W = ctx.canvas.width;\n    const H = ctx.canvas.height;\n    const k = (2 * Math.PI) / wavelength;\n    const N = sources.length;\n    const dark = [0, 6, 28];\n    const bright = [190, 238, 255];\n    const dotColors = [\"#ff6b6b\", \"#69d2e7\", \"#ffd93d\"];\n    const dotRadius = 10;\n    function waveAmp(px, py) {\n        let amp = 0;\n        for (const src of sources) {\n            const r = Math.hypot(px - src.x, py - src.y);\n            amp += Math.cos(k * r - time);\n        }\n        return amp;\n    }\n    const imgData = ctx.createImageData(W, H);\n    const data = imgData.data;\n    for (let py = 0; py < H; py += 2) {\n        for (let px = 0; px < W; px += 2) {\n            const amp = waveAmp(px, py);\n            const n = (amp / N + 1) / 2;\n            const b = Math.pow(Math.max(0, Math.min(1, n)), 1.4);\n            const rv = Math.round(dark[0] + b * (bright[0] - dark[0]));\n            const gv = Math.round(dark[1] + b * (bright[1] - dark[1]));\n            const bv = Math.round(dark[2] + b * (bright[2] - dark[2]));\n            for (let dy = 0; dy < 2; dy++) {\n                for (let dx = 0; dx < 2; dx++) {\n                    if (py + dy >= H || px + dx >= W)\n                        continue;\n                    const i = ((py + dy) * W + (px + dx)) * 4;\n                    data[i] = rv;\n                    data[i + 1] = gv;\n                    data[i + 2] = bv;\n                    data[i + 3] = 255;\n                }\n            }\n        }\n    }\n    ctx.putImageData(imgData, 0, 0);\n    sources.forEach((src, i) => {\n        ctx.beginPath();\n        ctx.arc(src.x, src.y, dotRadius, 0, 2 * Math.PI);\n        ctx.fillStyle = dotColors[i % dotColors.length];\n        ctx.fill();\n        ctx.strokeStyle = \"rgba(255,255,255,0.8)\";\n        ctx.lineWidth = 2;\n        ctx.stroke();\n    });\n}",
  equilateralTriangle: "/**\n * Compute the three vertices of an equilateral triangle inscribed in a circle.\n *\n * @param radius - Distance from the center to each vertex (the circumradius).\n * @param centerPoint - Center of the triangle.\n * @param angle - Rotation in radians; `point1` sits at this angle, the others 120° apart.\n * @returns The three corners as `{ point1, point2, point3 }`.\n * @example\n * equilateralTriangle(5, { x: 0, y: 0 }, 0); // point1 at { x: 5, y: 0 }\n */\nfunction equilateralTriangle(radius, centerPoint, angle) {\n    let allRadiansInACircle = 2 * Math.PI;\n    let point1 = {\n        x: radius * Math.cos(angle) + centerPoint.x,\n        y: radius * Math.sin(angle) + centerPoint.y,\n    };\n    let point2 = {\n        x: radius * Math.cos(angle + (1 / 3) * allRadiansInACircle) + centerPoint.x,\n        y: radius * Math.sin(angle + (1 / 3) * allRadiansInACircle) + centerPoint.y,\n    };\n    let point3 = {\n        x: radius * Math.cos(angle + (2 / 3) * allRadiansInACircle) + centerPoint.x,\n        y: radius * Math.sin(angle + (2 / 3) * allRadiansInACircle) + centerPoint.y,\n    };\n    return { point1, point2, point3 };\n}",
  findPointAroundCircle: "/**\n * Find the point a given percentage of the way around a circle.\n *\n * @param circleCenter - Center of the circle.\n * @param radius - Radius of the circle.\n * @param percentageAroundCircle - Position around the circle, `0`–`100`. 0 is at 3 o'clock; 25 is at 6 o'clock.\n * @returns The `{ x, y }` point on the circle at that percentage.\n * @example\n * findPointAroundCircle({ x: 0, y: 0 }, 10, 25); // => { x: 0, y: 10 }\n */\nfunction findPointAroundCircle(circleCenter, radius, percentageAroundCircle) {\n    let totalCircleRadians = Math.PI * 2;\n    let percent = percentageAroundCircle / 100;\n    const x = circleCenter.x + radius * Math.cos(totalCircleRadians * percent);\n    const y = circleCenter.y + radius * Math.sin(totalCircleRadians * percent);\n    return { x, y };\n}",
  gameOfLifeStep: "/**\n * Compute the next generation of Conway's Game of Life.\n *\n * Applies the classic rules to every cell — a live cell survives with 2 or 3 live\n * neighbours, a dead cell is born with exactly 3 — using a toroidal (wrap-around)\n * grid so the edges connect. Returns a new grid rather than mutating the input.\n *\n * @param grid - The current generation as a flat row-major array of `0`/`1` (length `cols * rows`).\n * @param cols - Number of columns.\n * @param rows - Number of rows.\n * @returns A new `Uint8Array` holding the next generation.\n * @example\n * grid = gameOfLifeStep(grid, cols, rows); // advance one tick\n */\nfunction gameOfLifeStep(grid, cols, rows) {\n    let next = new Uint8Array(cols * rows);\n    for (let y = 0; y < rows; y++) {\n        for (let x = 0; x < cols; x++) {\n            let i = y * cols + x;\n            let n = countNeighbors(grid, x, y, cols, rows);\n            if (grid[i] && (n === 2 || n === 3))\n                next[i] = 1;\n            else if (!grid[i] && n === 3)\n                next[i] = 1;\n        }\n    }\n    return next;\n}",
  getRotation: "/**\n * Get the angle (in radians) pointing from one point toward another.\n *\n * Handy for rotating a sprite so it \"faces\" a target. Uses `Math.atan2`, so the\n * result is in the range `-π`…`π`, measured clockwise from the positive x-axis.\n *\n * @param currentPoint - The point doing the looking.\n * @param destinationPoint - The point being looked at.\n * @returns The heading angle in radians.\n * @example\n * getRotation({ x: 0, y: 0 }, { x: 0, y: 1 }); // => Math.PI / 2\n */\nfunction getRotation(currentPoint, destinationPoint) {\n    return Math.atan2(destinationPoint.y - currentPoint.y, destinationPoint.x - currentPoint.x);\n}",
  grAccel: "function grAccel(bx, by, sunX, sunY, GM, epsilon) {\n    const dx = bx - sunX;\n    const dy = by - sunY;\n    const r2 = dx * dx + dy * dy;\n    const r3 = r2 * Math.sqrt(r2);\n    const f = -GM / r3 * (1 + epsilon / r2);\n    return [f * dx, f * dy];\n}",
  heartPath: "function heartPath(n) {\n    const pts = [];\n    for (let i = 0; i < n; i++) {\n        const t = (i / n) * 2 * Math.PI;\n        const x = 16 * Math.pow(Math.sin(t), 3);\n        const y = -(13 * Math.cos(t) - 5 * Math.cos(2 * t) - 2 * Math.cos(3 * t) - Math.cos(4 * t));\n        pts.push({ x: x * 9, y: y * 9 });\n    }\n    return pts;\n}",
  hslToRgb: "/**\n * Convert an HSL color back to RGB.\n *\n * Inverse of {@link rgbToHsl}. Hue wraps, so values outside 0–360 are accepted.\n *\n * @param hsl - `h` in degrees (wraps), `s`/`l` 0–100.\n * @returns Channels rounded to integers 0–255.\n * @example\n * hslToRgb({ h: 120, s: 100, l: 50 }); // => { r: 0, g: 255, b: 0 }\n */\nfunction hslToRgb({ h, s, l }) {\n    const sn = s / 100;\n    const ln = l / 100;\n    const c = (1 - Math.abs(2 * ln - 1)) * sn;\n    const hp = ((((h % 360) + 360) % 360)) / 60;\n    const x = c * (1 - Math.abs((hp % 2) - 1));\n    let r1 = 0;\n    let g1 = 0;\n    let b1 = 0;\n    if (hp < 1)\n        [r1, g1, b1] = [c, x, 0];\n    else if (hp < 2)\n        [r1, g1, b1] = [x, c, 0];\n    else if (hp < 3)\n        [r1, g1, b1] = [0, c, x];\n    else if (hp < 4)\n        [r1, g1, b1] = [0, x, c];\n    else if (hp < 5)\n        [r1, g1, b1] = [x, 0, c];\n    else\n        [r1, g1, b1] = [c, 0, x];\n    const m = ln - c / 2;\n    return {\n        r: Math.round((r1 + m) * 255),\n        g: Math.round((g1 + m) * 255),\n        b: Math.round((b1 + m) * 255),\n    };\n}",
  lerp: "/**\n * Linearly interpolate between two numbers.\n *\n * @param a - Start value, returned when `t` is 0.\n * @param b - End value, returned when `t` is 1.\n * @param t - Interpolation factor. 0–1 stays within the range; values outside extrapolate.\n * @returns The interpolated value `a + (b - a) * t`.\n * @example\n * lerp(0, 100, 0.5); // => 50\n */\nfunction lerp(a, b, t) {\n    return a + (b - a) * t;\n}",
  lerpAngle: "/**\n * Interpolate between two angles along the shortest arc (no spinning the long way around).\n *\n * The angle-aware cousin of {@link lerp} — use it to smoothly rotate something\n * toward a target heading without sudden flips at the 0/360° boundary.\n *\n * @param a - Start angle in radians, returned when `t` is 0.\n * @param b - Target angle in radians, returned when `t` is 1.\n * @param t - Interpolation factor, 0–1.\n * @returns The interpolated angle in radians.\n * @example\n * lerpAngle(0, Math.PI / 2, 0.5); // => Math.PI / 4\n */\nfunction lerpAngle(a, b, t) {\n    return a + shortestAngleBetween(a, b) * t;\n}",
  lineLength: "/**\n * Get the length of a line segment.\n *\n * @param line - A line with `startPoint` and `endPoint`.\n * @returns The straight-line distance between the two endpoints.\n * @remarks\n * This is the segment-shaped spelling of Euclidean distance — the same measurement as\n * {@link distance} on two loose points, computed with `Math.hypot`.\n * @example\n * lineLength({ startPoint: { x: 0, y: 0 }, endPoint: { x: 3, y: 4 } }); // => 5\n */\nfunction lineLength(line) {\n    const { startPoint, endPoint } = line;\n    return Math.hypot(startPoint.x - endPoint.x, startPoint.y - endPoint.y);\n}",
  lineLine: "/**\n * Test whether two line segments cross, and report where (object-argument form).\n *\n * Unlike the boolean `lineToLine`, this returns the intersection coordinates when\n * the segments meet — useful for drawing a marker at the crossing.\n *\n * Parallel or collinear segments return `{ hit: false }` in this implementation.\n *\n * @param line1 - First segment (`startPoint`, `endPoint`).\n * @param line2 - Second segment (`startPoint`, `endPoint`).\n * @returns `{ hit: true, intersectionX, intersectionY }` if they cross, else `{ hit: false }`.\n * @remarks\n * Use this when you need the intersection *point*. If a plain yes/no is enough, the flat\n * {@link lineToLine} returns a `boolean`.\n * @example\n * const r = lineLine(a, b);\n * if (r.hit) drawDot(r.intersectionX, r.intersectionY);\n */\nfunction lineLine(line1, line2) {\n    const denominator = (line2.endPoint.y - line2.startPoint.y) * (line1.endPoint.x - line1.startPoint.x) -\n        (line2.endPoint.x - line2.startPoint.x) * (line1.endPoint.y - line1.startPoint.y);\n    if (Math.abs(denominator) < 0.0001) {\n        return { hit: false };\n    }\n    const uA = ((line2.endPoint.x - line2.startPoint.x) * (line1.startPoint.y - line2.startPoint.y) -\n        (line2.endPoint.y - line2.startPoint.y) * (line1.startPoint.x - line2.startPoint.x)) /\n        denominator;\n    const uB = ((line1.endPoint.x - line1.startPoint.x) * (line1.startPoint.y - line2.startPoint.y) -\n        (line1.endPoint.y - line1.startPoint.y) * (line1.startPoint.x - line2.startPoint.x)) /\n        denominator;\n    if (uA >= 0 && uA <= 1 && uB >= 0 && uB <= 1) {\n        const intersectionX = line1.startPoint.x + uA * (line1.endPoint.x - line1.startPoint.x);\n        const intersectionY = line1.startPoint.y + uA * (line1.endPoint.y - line1.startPoint.y);\n        return {\n            hit: true,\n            intersectionX,\n            intersectionY,\n        };\n    }\n    return { hit: false };\n}",
  lineToCircle: "/**\n * Test whether a line segment intersects a circle.\n *\n * Finds the closest point on the segment to the circle's center and checks\n * whether it lies within the radius. This also handles degenerate zero-length\n * segments safely.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param cr - Circle radius.\n * @returns `true` if the segment touches or crosses the circle.\n * @remarks\n * Flat, seven-number form. For new code the **recommended** shape is the object-argument\n * {@link lineCircle} (`lineCircle({ startPoint, endPoint }, { x, y, radius })`); both run\n * the identical closest-point-on-segment test.\n * @example\n * lineToCircle(0, 0, 10, 0, 5, 0, 2); // => true\n */\nfunction lineToCircle(x1, y1, x2, y2, cx, cy, cr) {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const lengthSquared = dx * dx + dy * dy;\n    if (lengthSquared === 0) {\n        const pointDx = cx - x1;\n        const pointDy = cy - y1;\n        return pointDx * pointDx + pointDy * pointDy <= cr * cr;\n    }\n    const projection = ((cx - x1) * dx + (cy - y1) * dy) / lengthSquared;\n    const t = Math.max(0, Math.min(1, projection));\n    const px = x1 + t * dx;\n    const py = y1 + t * dy;\n    const ddx = cx - px;\n    const ddy = cy - py;\n    return ddx * ddx + ddy * ddy <= cr * cr;\n}",
  lineToLine: "/**\n * Test whether two line segments cross.\n *\n * Parallel or collinear segments return `false` in this implementation.\n *\n * @param x1 - First segment start x.\n * @param y1 - First segment start y.\n * @param x2 - First segment end x.\n * @param y2 - First segment end y.\n * @param x3 - Second segment start x.\n * @param y3 - Second segment start y.\n * @param x4 - Second segment end x.\n * @param y4 - Second segment end y.\n * @returns `true` if the two segments intersect (parallel segments return `false`).\n * @remarks\n * Flat, eight-number form that answers only \"do they cross?\" If you also need *where*\n * they cross, use the object-argument {@link lineLine}, which returns\n * `{ hit, intersectionX, intersectionY }`.\n * @example\n * lineToLine(0, 0, 10, 10, 0, 10, 10, 0); // => true (an X)\n */\nfunction lineToLine(x1, y1, x2, y2, x3, y3, x4, y4) {\n    const denominator = (x1 - x2) * (y3 - y4) - (y1 - y2) * (x3 - x4);\n    if (Math.abs(denominator) < 0.0001)\n        return false;\n    const t = ((x1 - x3) * (y3 - y4) - (y1 - y3) * (x3 - x4)) / denominator;\n    const u = -((x1 - x2) * (y1 - y3) - (y1 - y2) * (x1 - x3)) / denominator;\n    return t >= 0 && t <= 1 && u >= 0 && u <= 1;\n}",
  lineToPoint: "/**\n * Test whether a point lies on a line segment, within a tolerance.\n *\n * @param x1 - Segment start x.\n * @param y1 - Segment start y.\n * @param x2 - Segment end x.\n * @param y2 - Segment end y.\n * @param px - Point x.\n * @param py - Point y.\n * @param threshold - How close (in units) the point must be to count as \"on the line\".\n * @returns `true` if the point is within `threshold` of the segment.\n * @remarks\n * This flat form lets you choose the `threshold`. The object-argument {@link linePoint}\n * (`linePoint({ startPoint, endPoint }, { x, y })`) is the same closest-point test with a\n * fixed `0.1` tolerance baked in — reach for it when the default is fine and you want the\n * tidier call site.\n * @example\n * lineToPoint(0, 0, 10, 0, 5, 0, 0.5); // => true\n */\nfunction lineToPoint(x1, y1, x2, y2, px, py, threshold) {\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const t = Math.max(0, Math.min(1, ((px - x1) * dx + (py - y1) * dy) / (dx * dx + dy * dy)));\n    const lx = x1 + t * dx;\n    const ly = y1 + t * dy;\n    const ddx = px - lx;\n    const ddy = py - ly;\n    return ddx * ddx + ddy * ddy <= threshold * threshold;\n}",
  moveAlongLine: "/**\n * Find the point a given fraction of the way from one point to another.\n *\n * @param origin - Start point, returned when `ratio` is 0.\n * @param destination - End point, returned when `ratio` is 1.\n * @param ratio - Fraction of the way along; `0.5` is the midpoint.\n * @returns The interpolated `{ x, y }` point.\n * @example\n * moveAlongLine({ x: 0, y: 0 }, { x: 10, y: 20 }, 0.5); // => { x: 5, y: 10 }\n */\nfunction moveAlongLine(origin, destination, ratio) {\n    let x = origin.x + ratio * (destination.x - origin.x);\n    let y = origin.y + ratio * (destination.y - origin.y);\n    return { x, y };\n}",
  newtonAccel: "function newtonAccel(bx, by, sunX, sunY, GM) {\n    const dx = bx - sunX;\n    const dy = by - sunY;\n    const r2 = dx * dx + dy * dy;\n    const r3 = r2 * Math.sqrt(r2);\n    const f = -GM / r3;\n    return [f * dx, f * dy];\n}",
  phyllotaxisPoint: "function phyllotaxisPoint(n, angleDeg, scale) {\n    const theta = (n * angleDeg * Math.PI) / 180;\n    const r = scale * Math.sqrt(n);\n    return [r * Math.cos(theta), r * Math.sin(theta)];\n}",
  pointCircle: "/**\n * Test whether a point falls inside or on a circle (object-argument form of `pointToCircle`).\n *\n * @param point - The point to test.\n * @param circle - The circle (`x`, `y`, `radius`).\n * @returns `true` if the point is within `radius` of the circle center.\n * @remarks\n * This object-argument form is the **recommended** one; {@link pointToCircle} is the\n * flat, five-number equivalent running the same squared-distance comparison.\n * @example\n * pointCircle({ x: 1, y: 1 }, { x: 0, y: 0, radius: 5 }); // => true\n */\nfunction pointCircle(point, circle) {\n    const dx = point.x - circle.x;\n    const dy = point.y - circle.y;\n    return dx * dx + dy * dy <= circle.radius * circle.radius;\n}",
  polygonPoint: "/**\n * Test whether a point is inside a polygon (ray-casting / even-odd rule).\n *\n * Casts a horizontal ray from the point and counts how many polygon edges it\n * crosses — an odd count means the point is inside. Works for both convex and\n * concave polygons.\n *\n * Boundary points are not special-cased. A point on an edge or vertex follows\n * the raw parity result from the ray test, so edge cases may differ depending\n * on which boundary segment is hit.\n *\n * @param polygon - The polygon (`vertices`).\n * @param point - The point to test.\n * @returns `true` if the point is inside the polygon.\n * @remarks\n * This object-argument form is the **recommended** one; {@link pointToPolygon} is the flat\n * `(px, py, vertices[])` equivalent running the same ray-cast.\n * @example\n * polygonPoint({ vertices: square }, { x: 5, y: 5 }); // => true\n */\nfunction polygonPoint(polygon, point) {\n    const { vertices } = polygon;\n    let inside = false;\n    for (let current = 0, previous = vertices.length - 1; current < vertices.length; previous = current++) {\n        const { x: currentX, y: currentY } = vertices[current];\n        const { x: previousX, y: previousY } = vertices[previous];\n        if ((currentY > point.y) !== (previousY > point.y) &&\n            point.x < ((previousX - currentX) * (point.y - currentY)) / (previousY - currentY) + currentX) {\n            inside = !inside;\n        }\n    }\n    return inside;\n}",
  quadraticBezier: "/**\n * Find the point at parameter `t` along a quadratic Bézier curve (one control point).\n *\n * Uses the closed-form quadratic Bézier formula\n * `(1-t)²·p0 + 2(1-t)t·p1 + t²·p2`.\n *\n * @param t - Position along the curve, 0–1.\n * @param p0 - Start anchor point (returned at `t = 0`).\n * @param p1 - The single control handle that bends the curve.\n * @param p2 - End anchor point (returned at `t = 1`).\n * @returns The `{ x, y }` point on the curve at `t`.\n * @example\n * quadraticBezier(0.5, {x:0,y:0}, {x:50,y:100}, {x:100,y:0}); // => { x: 50, y: 50 }\n */\nfunction quadraticBezier(t, p0, p1, p2) {\n    const mt = 1 - t;\n    return {\n        x: mt * mt * p0.x + 2 * mt * t * p1.x + t * t * p2.x,\n        y: mt * mt * p0.y + 2 * mt * t * p1.y + t * t * p2.y,\n    };\n}",
  radToDeg: "/**\n * Convert an angle from radians to degrees.\n *\n * @param radians - Angle in radians.\n * @returns The equivalent angle in degrees.\n * @example\n * radToDeg(Math.PI); // => 180\n */\nfunction radToDeg(radians) {\n    return (radians * 180) / Math.PI;\n}",
  rgbToCss: "/**\n * Format an RGB color as a CSS `rgb()` string, rounding channels to integers.\n *\n * @param rgb - Channels 0–255 (fractional values are rounded).\n * @returns e.g. `\"rgb(255, 128, 0)\"`.\n * @example\n * rgbToCss({ r: 255, g: 127.5, b: 0 }); // => \"rgb(255, 128, 0)\"\n */\nfunction rgbToCss({ r, g, b }) {\n    return `rgb(${Math.round(r)}, ${Math.round(g)}, ${Math.round(b)})`;\n}",
  shortestAngleBetween: "/**\n * The smallest signed angle to rotate from `a` to `b`, taking the short way around.\n *\n * Solves the classic \"360° seam\" problem: rotating from 350° to 10° should be\n * `+20°`, not `-340°`. Positive results turn counter-clockwise.\n *\n * @param a - Start angle in radians.\n * @param b - Target angle in radians.\n * @returns The shortest signed delta in `(-π, π]`.\n * @example\n * shortestAngleBetween(0, Math.PI / 2); // => Math.PI / 2\n */\nfunction shortestAngleBetween(a, b) {\n    return wrapAngle(b - a);\n}",
  sierpinskiMidpoints: "/**\n * sierpinskiMidpoints — the heart of the fractal: given an equilateral triangle\n * of circumradius `radius` at orientation `phase` (radians), return the midpoints\n * of its three edges. Connecting these midpoints is the Sierpinski subdivision\n * step, and each midpoint triangle is the next, half-size generation.\n * Self-contained (no imports referenced) so a CodePen can embed it via .toString().\n */\nfunction sierpinskiMidpoints(radius, phase) {\n    const TAU = 2 * Math.PI;\n    const vertex = (k) => ({\n        x: radius * Math.cos(phase + (k * TAU) / 3),\n        y: radius * Math.sin(phase + (k * TAU) / 3),\n    });\n    const v1 = vertex(0);\n    const v2 = vertex(1);\n    const v3 = vertex(2);\n    return [\n        { x: (v1.x + v2.x) / 2, y: (v1.y + v2.y) / 2 },\n        { x: (v2.x + v3.x) / 2, y: (v2.y + v3.y) / 2 },\n        { x: (v1.x + v3.x) / 2, y: (v1.y + v3.y) / 2 },\n    ];\n}",
  sineCurve: "/**\n * Oscillate a value smoothly around a baseline using a sine wave.\n *\n * Pure and deterministic: pass the current `time` (e.g. `performance.now()`) and the\n * same inputs always produce the same output — so it's easy to test and reproduce.\n *\n * @param startingValue - The baseline the result oscillates around.\n * @param differential - The amplitude (how far above/below the baseline it swings).\n * @param speed - Oscillation speed multiplier applied to `time`.\n * @param time - The current time in milliseconds (e.g. `performance.now()`).\n * @returns The oscillating value at the given moment.\n * @example\n * const y = sineCurve(200, 50, 0.002, performance.now()); // bob around y=200 by ±50\n */\nfunction sineCurve(startingValue, differential, speed, time) {\n    return startingValue + Math.sin(time * speed) * differential;\n}",
  sphereLighting: "/**\n * Find where the specular highlight sits on a 2D sphere, given a light source.\n *\n * Points from the sphere's center toward the light and walks part of the way out\n * along the radius — a cheap way to fake a 3D-lit highlight on a flat circle.\n *\n * @param sphere - The sphere's center and radius.\n * @param lightSource - Position of the light.\n * @param highlightReach - How far toward the edge the highlight sits, 0–1. Defaults to `0.4`.\n * @returns The `{ x, y }` position to draw the highlight.\n * @example\n * const hi = sphereLighting({ x: 100, y: 100, radius: 40 }, { x: 0, y: 0 });\n */\nfunction sphereLighting(sphere, lightSource, highlightReach = 0.4) {\n    const angleToLight = Math.atan2(lightSource.y - sphere.y, lightSource.x - sphere.x);\n    return {\n        x: sphere.x + Math.cos(angleToLight) * sphere.radius * highlightReach,\n        y: sphere.y + Math.sin(angleToLight) * sphere.radius * highlightReach,\n    };\n}",
  springValue: "/**\n * Advance a damped spring one fixed simulation step.\n *\n * @example\n * let s = { value: 0, velocity: 0 };\n * s = springValue(s, 100); // => state nudged toward 100; call each frame\n */\nfunction springValue(state, target, { stiffness = 170, damping = 26, mass = 1, deltaSeconds = 1 / 60, } = {}) {\n    const force = -stiffness * (state.value - target);\n    const damper = -damping * state.velocity;\n    const acceleration = (force + damper) / mass;\n    const velocity = state.velocity + acceleration * deltaSeconds;\n    const value = state.value + velocity * deltaSeconds;\n    return { value, velocity };\n}",
  starVertices: "/**\n * Compute the vertices of a star polygon, alternating between an outer and inner radius.\n *\n * @param spikes - Number of points on the star.\n * @param innerRadius - Radius of the inner (valley) vertices.\n * @param outerRadius - Radius of the outer (tip) vertices.\n * @param angle - Starting rotation in radians. Defaults to `0`.\n * @param options - Optional spin animation: `{ rotate, rotateSpeed, clockwise, time }`. When\n *   `rotate` is true the angle advances with `options.time` (ms, e.g. `performance.now()`),\n *   keeping the function pure — pass the time in rather than reading the clock here.\n * @returns `{ vertices }` — `spikes * 2` points, alternating outer then inner.\n * @example\n * starVertices(5, 5, 10).vertices.length; // => 10\n */\nfunction starVertices(spikes, innerRadius, outerRadius, angle = 0, options = { rotate: false, rotateSpeed: 1000, clockwise: true }) {\n    let vertices = [];\n    let rot = 0;\n    let step = Math.PI / spikes;\n    const rotate = options.rotate ?? false;\n    const rotateSpeed = options.rotateSpeed ?? 1000;\n    const clockwise = options.clockwise ?? true;\n    const time = options.time ?? 0;\n    let rotateQ = rotate\n        ? time / rotateSpeed\n        : 0;\n    if (!clockwise)\n        rotateQ *= -1;\n    for (let i = 0; i < spikes; i++) {\n        let x = Math.cos(angle + rot + rotateQ) * outerRadius;\n        let y = Math.sin(angle + rot + rotateQ) * outerRadius;\n        vertices.push({ x, y });\n        rot += step;\n        x = Math.cos(angle + rot + rotateQ) * innerRadius;\n        y = Math.sin(angle + rot + rotateQ) * innerRadius;\n        vertices.push({ x, y });\n        rot += step;\n    }\n    return { vertices };\n}",
  unitCirclePoint: "/**\n * Get the point on a circle at a given angle, along with the raw cosine/sine.\n *\n * Returning `cos`/`sin` too saves the caller recomputing them (e.g. to orient\n * something tangent to the circle).\n *\n * @param cx - Circle center x.\n * @param cy - Circle center y.\n * @param radius - Circle radius.\n * @param time - Angle in radians (named `time` since it's usually driven by a clock).\n * @returns `{ x, y, cos, sin }` — the point and the trig components used to find it.\n * @example\n * unitCirclePoint(100, 100, 50, 0); // => { x: 150, y: 100, cos: 1, sin: 0 }\n */\nfunction unitCirclePoint(cx, cy, radius, time) {\n    let cos = Math.cos(time), sin = Math.sin(time);\n    return { x: cx + radius * cos, y: cy + radius * sin, cos, sin };\n}",
  vecNormalize: "/**\n * Normalize a vector to length 1, preserving its direction (a \"unit vector\").\n *\n * @param v - The vector to normalize.\n * @returns A new unit vector, or `{ x: 0, y: 0 }` if `v` has zero length.\n * @example\n * vecNormalize({ x: 0, y: 5 }); // => { x: 0, y: 1 }\n */\nfunction vecNormalize(v) {\n    const m = Math.hypot(v.x, v.y);\n    return m === 0 ? { x: 0, y: 0 } : { x: v.x / m, y: v.y / m };\n}",
  vecPerpendicular: "/**\n * A vector perpendicular to the given one (rotated 90° counter-clockwise).\n *\n * Handy for finding a surface normal from an edge.\n *\n * @param v - The vector.\n * @returns A new vector `{ x: -v.y, y: v.x }`.\n * @example\n * vecPerpendicular({ x: 1, y: 0 }); // => { x: 0, y: 1 }\n */\nfunction vecPerpendicular(v) {\n    return { x: -v.y, y: v.x };\n}",
  vecReflect: "/**\n * Reflect a vector off a surface, like a ball bouncing off a wall.\n *\n * @param v - The incoming vector (e.g. a velocity).\n * @param normal - The surface normal. Should be a unit vector; normalize it first if unsure.\n * @returns The reflected vector, `v - 2 (v · normal) normal`.\n * @example\n * vecReflect({ x: 1, y: -1 }, { x: 0, y: 1 }); // => { x: 1, y: 1 } (bounce off the floor)\n */\nfunction vecReflect(v, normal) {\n    const d = 2 * (v.x * normal.x + v.y * normal.y);\n    return { x: v.x - d * normal.x, y: v.y - d * normal.y };\n}",
  vecRotate: "/**\n * Rotate a vector by an angle (counter-clockwise in standard math coordinates).\n *\n * @param v - The vector to rotate.\n * @param radians - The rotation angle in radians.\n * @returns A new, rotated vector of the same length.\n * @example\n * vecRotate({ x: 1, y: 0 }, Math.PI / 2); // => { x: ~0, y: 1 }\n */\nfunction vecRotate(v, radians) {\n    const c = Math.cos(radians);\n    const s = Math.sin(radians);\n    return { x: v.x * c - v.y * s, y: v.x * s + v.y * c };\n}",
  wrapAngle: "/**\n * Normalize an angle to the range `(-π, π]`.\n *\n * Collapses any angle (including ones that have wound past a full turn) to its\n * canonical equivalent, so `3π` and `π` are treated as the same direction.\n *\n * @param radians - The angle to normalize, in radians.\n * @returns The equivalent angle in `(-π, π]`.\n * @example\n * wrapAngle(3 * Math.PI); // => ~3.14159 (π)\n */\nfunction wrapAngle(radians) {\n    return Math.atan2(Math.sin(radians), Math.cos(radians));\n}",
};
